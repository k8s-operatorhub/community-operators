apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  creationTimestamp: null
  name: neo4jenterprisestandalones.neo4j.neo4j.com
spec:
  group: neo4j.neo4j.com
  names:
    kind: Neo4jEnterpriseStandalone
    listKind: Neo4jEnterpriseStandaloneList
    plural: neo4jenterprisestandalones
    singular: neo4jenterprisestandalone
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.version
      name: Version
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: Neo4jEnterpriseStandalone is the Schema for the neo4jenterprisestandalones
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Neo4jEnterpriseStandaloneSpec defines the desired state of
              Neo4jEnterpriseStandalone
            properties:
              affinity:
                description: Affinity rules for pod scheduling
                properties:
                  nodeAffinity:
                    description: Describes node affinity scheduling rules for the
                      pod.
                    properties:
                      preferredDuringSchedulingIgnoredDuringExecution:
                        description: |-
                          The scheduler will prefer to schedule pods to nodes that satisfy
                          the affinity expressions specified by this field, but it may choose
                          a node that violates one or more of the expressions. The node that is
                          most preferred is the one with the greatest sum of weights, i.e.
                          for each node that meets all of the scheduling requirements (resource
                          request, requiredDuringScheduling affinity expressions, etc.),
                          compute a sum by iterating through the elements of this field and adding
                          "weight" to the sum if the node matches the corresponding matchExpressions; the
                          node(s) with the highest sum are the most preferred.
                        items:
                          description: |-
                            An empty preferred scheduling term matches all objects with implicit weight 0
                            (i.e. it's a no-op). A null preferred scheduling term matches no objects (i.e. is also a no-op).
                          properties:
                            preference:
                              description: A node selector term, associated with the
                                corresponding weight.
                              properties:
                                matchExpressions:
                                  description: A list of node selector requirements
                                    by node's labels.
                                  items:
                                    description: |-
                                      A node selector requirement is a selector that contains values, a key, and an operator
                                      that relates the key and values.
                                    properties:
                                      key:
                                        description: The label key that the selector
                                          applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          Represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                        type: string
                                      values:
                                        description: |-
                                          An array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. If the operator is Gt or Lt, the values
                                          array must have a single element, which will be interpreted as an integer.
                                          This array is replaced during a strategic merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                matchFields:
                                  description: A list of node selector requirements
                                    by node's fields.
                                  items:
                                    description: |-
                                      A node selector requirement is a selector that contains values, a key, and an operator
                                      that relates the key and values.
                                    properties:
                                      key:
                                        description: The label key that the selector
                                          applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          Represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                        type: string
                                      values:
                                        description: |-
                                          An array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. If the operator is Gt or Lt, the values
                                          array must have a single element, which will be interpreted as an integer.
                                          This array is replaced during a strategic merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                              type: object
                              x-kubernetes-map-type: atomic
                            weight:
                              description: Weight associated with matching the corresponding
                                nodeSelectorTerm, in the range 1-100.
                              format: int32
                              type: integer
                          required:
                          - preference
                          - weight
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      requiredDuringSchedulingIgnoredDuringExecution:
                        description: |-
                          If the affinity requirements specified by this field are not met at
                          scheduling time, the pod will not be scheduled onto the node.
                          If the affinity requirements specified by this field cease to be met
                          at some point during pod execution (e.g. due to an update), the system
                          may or may not try to eventually evict the pod from its node.
                        properties:
                          nodeSelectorTerms:
                            description: Required. A list of node selector terms.
                              The terms are ORed.
                            items:
                              description: |-
                                A null or empty node selector term matches no objects. The requirements of
                                them are ANDed.
                                The TopologySelectorTerm type implements a subset of the NodeSelectorTerm.
                              properties:
                                matchExpressions:
                                  description: A list of node selector requirements
                                    by node's labels.
                                  items:
                                    description: |-
                                      A node selector requirement is a selector that contains values, a key, and an operator
                                      that relates the key and values.
                                    properties:
                                      key:
                                        description: The label key that the selector
                                          applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          Represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                        type: string
                                      values:
                                        description: |-
                                          An array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. If the operator is Gt or Lt, the values
                                          array must have a single element, which will be interpreted as an integer.
                                          This array is replaced during a strategic merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                matchFields:
                                  description: A list of node selector requirements
                                    by node's fields.
                                  items:
                                    description: |-
                                      A node selector requirement is a selector that contains values, a key, and an operator
                                      that relates the key and values.
                                    properties:
                                      key:
                                        description: The label key that the selector
                                          applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          Represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                        type: string
                                      values:
                                        description: |-
                                          An array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. If the operator is Gt or Lt, the values
                                          array must have a single element, which will be interpreted as an integer.
                                          This array is replaced during a strategic merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                              type: object
                              x-kubernetes-map-type: atomic
                            type: array
                            x-kubernetes-list-type: atomic
                        required:
                        - nodeSelectorTerms
                        type: object
                        x-kubernetes-map-type: atomic
                    type: object
                  podAffinity:
                    description: Describes pod affinity scheduling rules (e.g. co-locate
                      this pod in the same node, zone, etc. as some other pod(s)).
                    properties:
                      preferredDuringSchedulingIgnoredDuringExecution:
                        description: |-
                          The scheduler will prefer to schedule pods to nodes that satisfy
                          the affinity expressions specified by this field, but it may choose
                          a node that violates one or more of the expressions. The node that is
                          most preferred is the one with the greatest sum of weights, i.e.
                          for each node that meets all of the scheduling requirements (resource
                          request, requiredDuringScheduling affinity expressions, etc.),
                          compute a sum by iterating through the elements of this field and adding
                          "weight" to the sum if the node has pods which matches the corresponding podAffinityTerm; the
                          node(s) with the highest sum are the most preferred.
                        items:
                          description: The weights of all of the matched WeightedPodAffinityTerm
                            fields are added per-node to find the most preferred node(s)
                          properties:
                            podAffinityTerm:
                              description: Required. A pod affinity term, associated
                                with the corresponding weight.
                              properties:
                                labelSelector:
                                  description: |-
                                    A label query over a set of resources, in this case pods.
                                    If it's null, this PodAffinityTerm matches with no Pods.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                matchLabelKeys:
                                  description: |-
                                    MatchLabelKeys is a set of pod label keys to select which pods will
                                    be taken into consideration. The keys are used to lookup values from the
                                    incoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`
                                    to select the group of existing pods which pods will be taken into consideration
                                    for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                    pod labels will be ignored. The default value is empty.
                                    The same key is forbidden to exist in both matchLabelKeys and labelSelector.
                                    Also, matchLabelKeys cannot be set when labelSelector isn't set.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                mismatchLabelKeys:
                                  description: |-
                                    MismatchLabelKeys is a set of pod label keys to select which pods will
                                    be taken into consideration. The keys are used to lookup values from the
                                    incoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`
                                    to select the group of existing pods which pods will be taken into consideration
                                    for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                    pod labels will be ignored. The default value is empty.
                                    The same key is forbidden to exist in both mismatchLabelKeys and labelSelector.
                                    Also, mismatchLabelKeys cannot be set when labelSelector isn't set.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                namespaceSelector:
                                  description: |-
                                    A label query over the set of namespaces that the term applies to.
                                    The term is applied to the union of the namespaces selected by this field
                                    and the ones listed in the namespaces field.
                                    null selector and null or empty namespaces list means "this pod's namespace".
                                    An empty selector ({}) matches all namespaces.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                namespaces:
                                  description: |-
                                    namespaces specifies a static list of namespace names that the term applies to.
                                    The term is applied to the union of the namespaces listed in this field
                                    and the ones selected by namespaceSelector.
                                    null or empty namespaces list and null namespaceSelector means "this pod's namespace".
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                topologyKey:
                                  description: |-
                                    This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching
                                    the labelSelector in the specified namespaces, where co-located is defined as running on a node
                                    whose value of the label with key topologyKey matches that of any node on which any of the
                                    selected pods is running.
                                    Empty topologyKey is not allowed.
                                  type: string
                              required:
                              - topologyKey
                              type: object
                            weight:
                              description: |-
                                weight associated with matching the corresponding podAffinityTerm,
                                in the range 1-100.
                              format: int32
                              type: integer
                          required:
                          - podAffinityTerm
                          - weight
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      requiredDuringSchedulingIgnoredDuringExecution:
                        description: |-
                          If the affinity requirements specified by this field are not met at
                          scheduling time, the pod will not be scheduled onto the node.
                          If the affinity requirements specified by this field cease to be met
                          at some point during pod execution (e.g. due to a pod label update), the
                          system may or may not try to eventually evict the pod from its node.
                          When there are multiple elements, the lists of nodes corresponding to each
                          podAffinityTerm are intersected, i.e. all terms must be satisfied.
                        items:
                          description: |-
                            Defines a set of pods (namely those matching the labelSelector
                            relative to the given namespace(s)) that this pod should be
                            co-located (affinity) or not co-located (anti-affinity) with,
                            where co-located is defined as running on a node whose value of
                            the label with key <topologyKey> matches that of any node on which
                            a pod of the set of pods is running
                          properties:
                            labelSelector:
                              description: |-
                                A label query over a set of resources, in this case pods.
                                If it's null, this PodAffinityTerm matches with no Pods.
                              properties:
                                matchExpressions:
                                  description: matchExpressions is a list of label
                                    selector requirements. The requirements are ANDed.
                                  items:
                                    description: |-
                                      A label selector requirement is a selector that contains values, a key, and an operator that
                                      relates the key and values.
                                    properties:
                                      key:
                                        description: key is the label key that the
                                          selector applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          operator represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists and DoesNotExist.
                                        type: string
                                      values:
                                        description: |-
                                          values is an array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. This array is replaced during a strategic
                                          merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                matchLabels:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                    map is equivalent to an element of matchExpressions, whose key field is "key", the
                                    operator is "In", and the values array contains only "value". The requirements are ANDed.
                                  type: object
                              type: object
                              x-kubernetes-map-type: atomic
                            matchLabelKeys:
                              description: |-
                                MatchLabelKeys is a set of pod label keys to select which pods will
                                be taken into consideration. The keys are used to lookup values from the
                                incoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`
                                to select the group of existing pods which pods will be taken into consideration
                                for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                pod labels will be ignored. The default value is empty.
                                The same key is forbidden to exist in both matchLabelKeys and labelSelector.
                                Also, matchLabelKeys cannot be set when labelSelector isn't set.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                            mismatchLabelKeys:
                              description: |-
                                MismatchLabelKeys is a set of pod label keys to select which pods will
                                be taken into consideration. The keys are used to lookup values from the
                                incoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`
                                to select the group of existing pods which pods will be taken into consideration
                                for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                pod labels will be ignored. The default value is empty.
                                The same key is forbidden to exist in both mismatchLabelKeys and labelSelector.
                                Also, mismatchLabelKeys cannot be set when labelSelector isn't set.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                            namespaceSelector:
                              description: |-
                                A label query over the set of namespaces that the term applies to.
                                The term is applied to the union of the namespaces selected by this field
                                and the ones listed in the namespaces field.
                                null selector and null or empty namespaces list means "this pod's namespace".
                                An empty selector ({}) matches all namespaces.
                              properties:
                                matchExpressions:
                                  description: matchExpressions is a list of label
                                    selector requirements. The requirements are ANDed.
                                  items:
                                    description: |-
                                      A label selector requirement is a selector that contains values, a key, and an operator that
                                      relates the key and values.
                                    properties:
                                      key:
                                        description: key is the label key that the
                                          selector applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          operator represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists and DoesNotExist.
                                        type: string
                                      values:
                                        description: |-
                                          values is an array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. This array is replaced during a strategic
                                          merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                matchLabels:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                    map is equivalent to an element of matchExpressions, whose key field is "key", the
                                    operator is "In", and the values array contains only "value". The requirements are ANDed.
                                  type: object
                              type: object
                              x-kubernetes-map-type: atomic
                            namespaces:
                              description: |-
                                namespaces specifies a static list of namespace names that the term applies to.
                                The term is applied to the union of the namespaces listed in this field
                                and the ones selected by namespaceSelector.
                                null or empty namespaces list and null namespaceSelector means "this pod's namespace".
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                            topologyKey:
                              description: |-
                                This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching
                                the labelSelector in the specified namespaces, where co-located is defined as running on a node
                                whose value of the label with key topologyKey matches that of any node on which any of the
                                selected pods is running.
                                Empty topologyKey is not allowed.
                              type: string
                          required:
                          - topologyKey
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                    type: object
                  podAntiAffinity:
                    description: Describes pod anti-affinity scheduling rules (e.g.
                      avoid putting this pod in the same node, zone, etc. as some
                      other pod(s)).
                    properties:
                      preferredDuringSchedulingIgnoredDuringExecution:
                        description: |-
                          The scheduler will prefer to schedule pods to nodes that satisfy
                          the anti-affinity expressions specified by this field, but it may choose
                          a node that violates one or more of the expressions. The node that is
                          most preferred is the one with the greatest sum of weights, i.e.
                          for each node that meets all of the scheduling requirements (resource
                          request, requiredDuringScheduling anti-affinity expressions, etc.),
                          compute a sum by iterating through the elements of this field and subtracting
                          "weight" from the sum if the node has pods which matches the corresponding podAffinityTerm; the
                          node(s) with the highest sum are the most preferred.
                        items:
                          description: The weights of all of the matched WeightedPodAffinityTerm
                            fields are added per-node to find the most preferred node(s)
                          properties:
                            podAffinityTerm:
                              description: Required. A pod affinity term, associated
                                with the corresponding weight.
                              properties:
                                labelSelector:
                                  description: |-
                                    A label query over a set of resources, in this case pods.
                                    If it's null, this PodAffinityTerm matches with no Pods.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                matchLabelKeys:
                                  description: |-
                                    MatchLabelKeys is a set of pod label keys to select which pods will
                                    be taken into consideration. The keys are used to lookup values from the
                                    incoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`
                                    to select the group of existing pods which pods will be taken into consideration
                                    for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                    pod labels will be ignored. The default value is empty.
                                    The same key is forbidden to exist in both matchLabelKeys and labelSelector.
                                    Also, matchLabelKeys cannot be set when labelSelector isn't set.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                mismatchLabelKeys:
                                  description: |-
                                    MismatchLabelKeys is a set of pod label keys to select which pods will
                                    be taken into consideration. The keys are used to lookup values from the
                                    incoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`
                                    to select the group of existing pods which pods will be taken into consideration
                                    for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                    pod labels will be ignored. The default value is empty.
                                    The same key is forbidden to exist in both mismatchLabelKeys and labelSelector.
                                    Also, mismatchLabelKeys cannot be set when labelSelector isn't set.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                namespaceSelector:
                                  description: |-
                                    A label query over the set of namespaces that the term applies to.
                                    The term is applied to the union of the namespaces selected by this field
                                    and the ones listed in the namespaces field.
                                    null selector and null or empty namespaces list means "this pod's namespace".
                                    An empty selector ({}) matches all namespaces.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                namespaces:
                                  description: |-
                                    namespaces specifies a static list of namespace names that the term applies to.
                                    The term is applied to the union of the namespaces listed in this field
                                    and the ones selected by namespaceSelector.
                                    null or empty namespaces list and null namespaceSelector means "this pod's namespace".
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                topologyKey:
                                  description: |-
                                    This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching
                                    the labelSelector in the specified namespaces, where co-located is defined as running on a node
                                    whose value of the label with key topologyKey matches that of any node on which any of the
                                    selected pods is running.
                                    Empty topologyKey is not allowed.
                                  type: string
                              required:
                              - topologyKey
                              type: object
                            weight:
                              description: |-
                                weight associated with matching the corresponding podAffinityTerm,
                                in the range 1-100.
                              format: int32
                              type: integer
                          required:
                          - podAffinityTerm
                          - weight
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      requiredDuringSchedulingIgnoredDuringExecution:
                        description: |-
                          If the anti-affinity requirements specified by this field are not met at
                          scheduling time, the pod will not be scheduled onto the node.
                          If the anti-affinity requirements specified by this field cease to be met
                          at some point during pod execution (e.g. due to a pod label update), the
                          system may or may not try to eventually evict the pod from its node.
                          When there are multiple elements, the lists of nodes corresponding to each
                          podAffinityTerm are intersected, i.e. all terms must be satisfied.
                        items:
                          description: |-
                            Defines a set of pods (namely those matching the labelSelector
                            relative to the given namespace(s)) that this pod should be
                            co-located (affinity) or not co-located (anti-affinity) with,
                            where co-located is defined as running on a node whose value of
                            the label with key <topologyKey> matches that of any node on which
                            a pod of the set of pods is running
                          properties:
                            labelSelector:
                              description: |-
                                A label query over a set of resources, in this case pods.
                                If it's null, this PodAffinityTerm matches with no Pods.
                              properties:
                                matchExpressions:
                                  description: matchExpressions is a list of label
                                    selector requirements. The requirements are ANDed.
                                  items:
                                    description: |-
                                      A label selector requirement is a selector that contains values, a key, and an operator that
                                      relates the key and values.
                                    properties:
                                      key:
                                        description: key is the label key that the
                                          selector applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          operator represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists and DoesNotExist.
                                        type: string
                                      values:
                                        description: |-
                                          values is an array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. This array is replaced during a strategic
                                          merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                matchLabels:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                    map is equivalent to an element of matchExpressions, whose key field is "key", the
                                    operator is "In", and the values array contains only "value". The requirements are ANDed.
                                  type: object
                              type: object
                              x-kubernetes-map-type: atomic
                            matchLabelKeys:
                              description: |-
                                MatchLabelKeys is a set of pod label keys to select which pods will
                                be taken into consideration. The keys are used to lookup values from the
                                incoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`
                                to select the group of existing pods which pods will be taken into consideration
                                for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                pod labels will be ignored. The default value is empty.
                                The same key is forbidden to exist in both matchLabelKeys and labelSelector.
                                Also, matchLabelKeys cannot be set when labelSelector isn't set.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                            mismatchLabelKeys:
                              description: |-
                                MismatchLabelKeys is a set of pod label keys to select which pods will
                                be taken into consideration. The keys are used to lookup values from the
                                incoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`
                                to select the group of existing pods which pods will be taken into consideration
                                for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                pod labels will be ignored. The default value is empty.
                                The same key is forbidden to exist in both mismatchLabelKeys and labelSelector.
                                Also, mismatchLabelKeys cannot be set when labelSelector isn't set.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                            namespaceSelector:
                              description: |-
                                A label query over the set of namespaces that the term applies to.
                                The term is applied to the union of the namespaces selected by this field
                                and the ones listed in the namespaces field.
                                null selector and null or empty namespaces list means "this pod's namespace".
                                An empty selector ({}) matches all namespaces.
                              properties:
                                matchExpressions:
                                  description: matchExpressions is a list of label
                                    selector requirements. The requirements are ANDed.
                                  items:
                                    description: |-
                                      A label selector requirement is a selector that contains values, a key, and an operator that
                                      relates the key and values.
                                    properties:
                                      key:
                                        description: key is the label key that the
                                          selector applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          operator represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists and DoesNotExist.
                                        type: string
                                      values:
                                        description: |-
                                          values is an array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. This array is replaced during a strategic
                                          merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                matchLabels:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                    map is equivalent to an element of matchExpressions, whose key field is "key", the
                                    operator is "In", and the values array contains only "value". The requirements are ANDed.
                                  type: object
                              type: object
                              x-kubernetes-map-type: atomic
                            namespaces:
                              description: |-
                                namespaces specifies a static list of namespace names that the term applies to.
                                The term is applied to the union of the namespaces listed in this field
                                and the ones selected by namespaceSelector.
                                null or empty namespaces list and null namespaceSelector means "this pod's namespace".
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                            topologyKey:
                              description: |-
                                This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching
                                the labelSelector in the specified namespaces, where co-located is defined as running on a node
                                whose value of the label with key topologyKey matches that of any node on which any of the
                                selected pods is running.
                                Empty topologyKey is not allowed.
                              type: string
                          required:
                          - topologyKey
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                    type: object
                type: object
              auraFleetManagement:
                description: |-
                  AuraFleetManagement enables integration with Neo4j Aura Fleet Management
                  for monitoring this standalone deployment from the Aura console.
                  See: https://neo4j.com/docs/aura/fleet-management/
                properties:
                  enabled:
                    default: false
                    description: |-
                      Enabled activates Aura Fleet Management integration.
                      When true, the fleet-management plugin is installed automatically
                      (from the pre-bundled jar in the Neo4j Enterprise image) and the
                      registration token is applied after the cluster becomes ready.
                    type: boolean
                  tokenSecretRef:
                    description: |-
                      TokenSecretRef references a Kubernetes Secret containing the Aura
                      Fleet Management registration token obtained from the Aura console wizard.

                      The Secret must be in the same namespace as the cluster and contain
                      the key specified by tokenSecretRef.key (defaults to "token").

                      Example:
                        kubectl create secret generic aura-fleet-token --from-literal=token='<token-from-aura>'
                    properties:
                      key:
                        description: Key within the Secret. The default depends on
                          context (e.g., "ca.crt" for TrustStore, "token" for fleet
                          management).
                        type: string
                      name:
                        description: Name of the Kubernetes Secret
                        type: string
                    required:
                    - name
                    type: object
                type: object
              auth:
                description: |-
                  AuthSpec defines authentication and authorization configuration.
                  Supports multi-provider setups (e.g., ldap+native) and typed configuration
                  for LDAP, OIDC/SSO, JWT, and Kerberos that generates correct neo4j.conf entries.
                properties:
                  adminSecret:
                    description: 'AdminSecret is the name of the Secret containing
                      initial admin credentials (keys: username, password)'
                    type: string
                  authCacheTTL:
                    description: |-
                      AuthCacheTTL sets dbms.security.auth_cache_ttl (e.g., "10m", "600s").
                      Controls how long authentication/authorization results are cached.
                    type: string
                  authenticationProviders:
                    description: |-
                      AuthenticationProviders is an ordered list of authentication providers.
                      Neo4j evaluates them in order during login. Valid values: native, ldap, oidc-<name>, jwt, kerberos.
                      For OIDC providers, use the format "oidc-<name>" where <name> matches a key in the OIDC map.
                      Defaults to ["native"] if empty.
                    items:
                      type: string
                    type: array
                  authorizationProviders:
                    description: |-
                      AuthorizationProviders is an ordered list of authorization providers.
                      Valid values: native, ldap, oidc-<name>, jwt, kerberos.
                      Defaults to ["native"] if empty.
                    items:
                      type: string
                    type: array
                  externalSecrets:
                    description: External Secrets configuration for auth secrets
                    properties:
                      data:
                        description: Data mapping from external secret store
                        items:
                          description: ExternalSecretData defines data mapping from
                            external secret store
                          properties:
                            remoteRef:
                              description: Remote reference to the secret in external
                                store
                              properties:
                                key:
                                  type: string
                                property:
                                  description: Property within the secret (for JSON/YAML
                                    secrets)
                                  type: string
                                version:
                                  description: Version of the secret
                                  type: string
                              required:
                              - key
                              type: object
                            secretKey:
                              type: string
                          required:
                          - secretKey
                          type: object
                        type: array
                      enabled:
                        default: false
                        description: Enable External Secrets Operator integration
                        type: boolean
                      refreshInterval:
                        default: 1h
                        description: Refresh interval for external secrets
                        type: string
                      secretStoreRef:
                        description: SecretStore reference for External Secrets
                        properties:
                          kind:
                            default: SecretStore
                            enum:
                            - SecretStore
                            - ClusterSecretStore
                            type: string
                          name:
                            type: string
                        required:
                        - name
                        type: object
                    type: object
                  jwt:
                    description: JWT configuration for JWT auth provider
                    properties:
                      claimsMapping:
                        additionalProperties:
                          type: string
                        description: Claims mapping
                        type: object
                      validation:
                        description: JWT validation settings
                        properties:
                          audience:
                            description: JWT audience
                            items:
                              type: string
                            type: array
                          issuer:
                            description: JWT issuer
                            type: string
                          jwksUrl:
                            description: JWKS endpoint URL
                            type: string
                        type: object
                    type: object
                  kerberos:
                    description: Kerberos configuration for Kerberos auth provider
                    properties:
                      keytab:
                        description: Keytab secret reference. Name is the Secret name;
                          Key defaults to "keytab" if omitted.
                        properties:
                          key:
                            description: Key within the Secret. The default depends
                              on context (e.g., "ca.crt" for TrustStore, "token" for
                              fleet management).
                            type: string
                          name:
                            description: Name of the Kubernetes Secret
                            type: string
                        required:
                        - name
                        type: object
                      realm:
                        description: Kerberos realm
                        type: string
                      servicePrincipal:
                        description: Service principal name
                        type: string
                    type: object
                  ldap:
                    description: LDAP configures LDAP authentication and authorization
                    properties:
                      authentication:
                        description: Authentication configures how users authenticate
                          against LDAP
                        properties:
                          attribute:
                            description: |-
                              Attribute is the LDAP attribute to search when SearchForAttribute is true (e.g., "samaccountname")
                              Maps to: dbms.security.ldap.authentication.attribute
                            type: string
                          cacheEnabled:
                            description: |-
                              CacheEnabled enables caching of LDAP authentication results
                              Maps to: dbms.security.ldap.authentication.cache_enabled
                            type: boolean
                          searchForAttribute:
                            description: |-
                              SearchForAttribute enables attribute-based user lookup instead of DN template binding.
                              When true, the operator uses Attribute to find the user before binding.
                              Maps to: dbms.security.ldap.authentication.search_for_attribute
                            type: boolean
                          userDNTemplate:
                            description: |-
                              UserDNTemplate is the DN template for binding users. Use {0} as the username placeholder.
                              Examples: "uid={0},ou=users,dc=example,dc=com" or "{0}@example.com" (Active Directory UPN)
                              Maps to: dbms.security.ldap.authentication.user_dn_template
                            type: string
                        type: object
                      authorization:
                        description: Authorization configures how Neo4j resolves roles/groups
                          from LDAP
                        properties:
                          accessPermittedGroup:
                            description: |-
                              AccessPermittedGroup restricts LDAP authentication to members of this group only.
                              Users with valid LDAP credentials but not in this group are denied access.
                              Maps to: dbms.security.ldap.authorization.access_permitted_group
                            type: string
                          groupMembershipAttributes:
                            description: |-
                              GroupMembershipAttributes are the user object attributes containing group membership (e.g., ["memberOf"])
                              Maps to: dbms.security.ldap.authorization.group_membership_attributes (comma-separated)
                            items:
                              type: string
                            type: array
                          groupToRoleMapping:
                            additionalProperties:
                              type: string
                            description: |-
                              GroupToRoleMapping maps LDAP group DNs to Neo4j roles.
                              Key: LDAP group DN, Value: comma-separated Neo4j roles (e.g., "admin,architect").
                              Built-in roles: admin, architect, publisher, editor, reader.
                              Maps to: dbms.security.ldap.authorization.group_to_role_mapping (semicolon-separated)
                            type: object
                          nestedGroupsEnabled:
                            description: |-
                              NestedGroupsEnabled enables recursive group membership resolution.
                              Maps to: dbms.security.ldap.authorization.nested_groups_enabled
                            type: boolean
                          nestedGroupsSearchFilter:
                            description: |-
                              NestedGroupsSearchFilter is the LDAP filter for resolving nested groups. {0} is replaced by the user DN.
                              For Active Directory, use: "(&(objectclass=group)(member:1.2.840.113556.1.4.1941:={0}))"
                              Maps to: dbms.security.ldap.authorization.nested_groups_search_filter
                            type: string
                          systemAccountSecretRef:
                            description: |-
                              SystemAccountSecretRef is the name of a Secret containing the LDAP system account credentials.
                              The Secret must have keys "username" (full DN) and "password".
                              These are injected as env vars, never stored in the ConfigMap.
                              Required when UseSystemAccount is true.
                            type: string
                          useSystemAccount:
                            description: |-
                              UseSystemAccount enables using a dedicated system account for LDAP authorization queries
                              instead of the authenticating user's own credentials.
                              Maps to: dbms.security.ldap.authorization.use_system_account
                            type: boolean
                          userSearchBase:
                            description: |-
                              UserSearchBase is the base DN for searching user objects (e.g., "ou=users,dc=example,dc=com")
                              Maps to: dbms.security.ldap.authorization.user_search_base
                            type: string
                          userSearchFilter:
                            description: |-
                              UserSearchFilter is the LDAP filter for finding user objects. {0} is replaced by the username.
                              Example: "(&(objectClass=user)(sAMAccountName={0}))"
                              Maps to: dbms.security.ldap.authorization.user_search_filter
                            type: string
                        type: object
                      debugGroupLogging:
                        description: |-
                          DebugGroupLogging enables debug-level logging for LDAP group lookups.
                          WARNING: Disable in production as it logs sensitive group information.
                          Maps to: dbms.security.logs.ldap.groups_at_debug_level_enabled
                        type: boolean
                      host:
                        description: |-
                          Host is the LDAP server URL (e.g., "ldap://ldap.example.com:389" or "ldaps://ldap.example.com:636")
                          Maps to: dbms.security.ldap.host
                        type: string
                      useStartTLS:
                        description: |-
                          UseStartTLS enables STARTTLS on the LDAP connection (use with ldap:// scheme, not ldaps://)
                          Maps to: dbms.security.ldap.use_starttls
                        type: boolean
                    required:
                    - host
                    type: object
                  oidc:
                    additionalProperties:
                      description: |-
                        Neo4jOIDCProviderSpec configures a single OIDC/SSO provider.
                        The map key in AuthSpec.OIDC becomes the <provider> in dbms.security.oidc.<provider>.*.
                      properties:
                        audience:
                          description: |-
                            Audience is the expected JWT "aud" claim value (typically your OIDC client ID)
                            Maps to: dbms.security.oidc.<name>.audience
                          type: string
                        authEndpoint:
                          description: |-
                            AuthEndpoint is the authorization endpoint (manual override, auto-populated from discovery)
                            Maps to: dbms.security.oidc.<name>.auth_endpoint
                          type: string
                        authFlow:
                          default: pkce
                          description: |-
                            AuthFlow selects the OAuth2 flow: "pkce" (recommended) or "implicit"
                            Maps to: dbms.security.oidc.<name>.auth_flow
                          enum:
                          - pkce
                          - implicit
                          type: string
                        authParams:
                          description: |-
                            AuthParams are additional parameters sent to the authorization endpoint (semicolon-separated key=value)
                            Maps to: dbms.security.oidc.<name>.auth_params
                          type: string
                        claims:
                          description: Claims configures JWT claim mapping for username
                            and group extraction
                          properties:
                            groups:
                              description: |-
                                Groups is the JWT claim containing roles/groups for authorization mapping
                                Maps to: dbms.security.oidc.<name>.claims.groups
                              type: string
                            username:
                              description: |-
                                Username is the JWT claim used as the Neo4j database username (default: "sub")
                                Maps to: dbms.security.oidc.<name>.claims.username
                              type: string
                          type: object
                        displayName:
                          description: |-
                            DisplayName shown on the Neo4j Browser/Bloom login screen
                            Maps to: dbms.security.oidc.<name>.display_name
                          type: string
                        getGroupsFromUserInfo:
                          description: |-
                            GetGroupsFromUserInfo fetches the groups claim from the UserInfo endpoint instead of the JWT
                            Maps to: dbms.security.oidc.<name>.get_groups_from_user_info
                          type: boolean
                        getUsernameFromUserInfo:
                          description: |-
                            GetUsernameFromUserInfo fetches the username claim from the UserInfo endpoint instead of the JWT
                            Maps to: dbms.security.oidc.<name>.get_username_from_user_info
                          type: boolean
                        groupToRoleMapping:
                          additionalProperties:
                            type: string
                          description: |-
                            GroupToRoleMapping maps OIDC groups/roles to Neo4j roles.
                            Key: IdP group name, Value: comma-separated Neo4j roles.
                            Maps to: dbms.security.oidc.<name>.authorization.group_to_role_mapping
                          type: object
                        issuer:
                          description: |-
                            Issuer identifier, validated against the JWT "iss" claim (manual override)
                            Maps to: dbms.security.oidc.<name>.issuer
                          type: string
                        jwksURI:
                          description: |-
                            JWKSURI is the JSON Web Key Set endpoint for JWT signature verification (manual override)
                            Maps to: dbms.security.oidc.<name>.jwks_uri
                          type: string
                        tokenEndpoint:
                          description: |-
                            TokenEndpoint is the token endpoint (manual override)
                            Maps to: dbms.security.oidc.<name>.token_endpoint
                          type: string
                        tokenParams:
                          description: |-
                            TokenParams are additional parameters sent to the token endpoint (semicolon-separated key=value)
                            Maps to: dbms.security.oidc.<name>.token_params
                          type: string
                        userInfoURI:
                          description: |-
                            UserInfoURI is the UserInfo endpoint (manual override)
                            Maps to: dbms.security.oidc.<name>.user_info_uri
                          type: string
                        wellKnownDiscoveryURI:
                          description: |-
                            WellKnownDiscoveryURI is the OIDC discovery endpoint that auto-configures other endpoints.
                            Either this or manual endpoints (AuthEndpoint, TokenEndpoint, JWKSURI, Issuer) must be set.
                            Maps to: dbms.security.oidc.<name>.well_known_discovery_uri
                          type: string
                      required:
                      - audience
                      type: object
                    description: |-
                      OIDC configures one or more OIDC/SSO providers. Map keys become the provider name
                      in Neo4j config (dbms.security.oidc.<name>.*) and in the authentication_providers list (oidc-<name>).
                    type: object
                  passwordPolicy:
                    description: PasswordPolicy configures Neo4j password policy requirements
                    properties:
                      minLength:
                        default: 8
                        description: Minimum password length
                        minimum: 1
                        type: integer
                      requireLowercase:
                        default: true
                        description: Require lowercase characters
                        type: boolean
                      requireNumbers:
                        default: true
                        description: Require numeric characters
                        type: boolean
                      requireSpecialChars:
                        default: false
                        description: Require special characters
                        type: boolean
                      requireUppercase:
                        default: true
                        description: Require uppercase characters
                        type: boolean
                    type: object
                  trustStore:
                    description: |-
                      TrustStore configures a custom JVM truststore for LDAPS or OIDC with internal CAs.
                      The operator mounts the CA certificate and creates a JKS truststore via an init container.
                      Name is the Secret name; Key defaults to "ca.crt" if omitted.
                    properties:
                      key:
                        description: Key within the Secret. The default depends on
                          context (e.g., "ca.crt" for TrustStore, "token" for fleet
                          management).
                        type: string
                      name:
                        description: Name of the Kubernetes Secret
                        type: string
                    required:
                    - name
                    type: object
                type: object
              backups:
                description: BackupsSpec defines default backup configuration
                properties:
                  cloud:
                    description: CloudBlock defines cloud provider configuration
                    properties:
                      credentialsSecretRef:
                        description: |-
                          CredentialsSecretRef is the name of a Kubernetes Secret containing
                          cloud provider credentials as environment variables. Optional when
                          using workload identity / IAM instance profiles.
                          For S3:    keys AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_REGION
                          For GCS:   key  GOOGLE_APPLICATION_CREDENTIALS_JSON (base64 service-account JSON)
                          For Azure: keys AZURE_STORAGE_ACCOUNT, AZURE_STORAGE_KEY
                        type: string
                      endpointURL:
                        description: |-
                          EndpointURL overrides the S3 API endpoint URL. Use this to target
                          S3-compatible stores such as MinIO, Ceph RGW, or Cloudflare R2.
                          Example: "http://minio.minio-ns.svc:9000"
                          Only applies to the "aws" provider; ignored for gcp and azure.
                        type: string
                      forcePathStyle:
                        description: |-
                          ForcePathStyle forces S3 path-style addressing, where the bucket name
                          appears in the URL path (e.g. http://endpoint/bucket/key) rather than
                          the subdomain (e.g. http://bucket.endpoint/key).
                          Required for MinIO and most self-hosted S3-compatible stores.
                          Only effective when EndpointURL is set.
                        type: boolean
                      identity:
                        description: CloudIdentity defines cloud identity configuration
                        properties:
                          autoCreate:
                            description: AutoCreateSpec defines auto-creation of service
                              accounts
                            properties:
                              annotations:
                                additionalProperties:
                                  type: string
                                type: object
                              enabled:
                                default: true
                                type: boolean
                            type: object
                          provider:
                            enum:
                            - aws
                            - gcp
                            - azure
                            type: string
                          serviceAccount:
                            type: string
                        required:
                        - provider
                        type: object
                      provider:
                        enum:
                        - aws
                        - gcp
                        - azure
                        type: string
                    type: object
                  defaultStorage:
                    description: StorageLocation defines storage location for backups
                    properties:
                      bucket:
                        type: string
                      cloud:
                        description: Cloud provider configuration
                        properties:
                          credentialsSecretRef:
                            description: |-
                              CredentialsSecretRef is the name of a Kubernetes Secret containing
                              cloud provider credentials as environment variables. Optional when
                              using workload identity / IAM instance profiles.
                              For S3:    keys AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_REGION
                              For GCS:   key  GOOGLE_APPLICATION_CREDENTIALS_JSON (base64 service-account JSON)
                              For Azure: keys AZURE_STORAGE_ACCOUNT, AZURE_STORAGE_KEY
                            type: string
                          endpointURL:
                            description: |-
                              EndpointURL overrides the S3 API endpoint URL. Use this to target
                              S3-compatible stores such as MinIO, Ceph RGW, or Cloudflare R2.
                              Example: "http://minio.minio-ns.svc:9000"
                              Only applies to the "aws" provider; ignored for gcp and azure.
                            type: string
                          forcePathStyle:
                            description: |-
                              ForcePathStyle forces S3 path-style addressing, where the bucket name
                              appears in the URL path (e.g. http://endpoint/bucket/key) rather than
                              the subdomain (e.g. http://bucket.endpoint/key).
                              Required for MinIO and most self-hosted S3-compatible stores.
                              Only effective when EndpointURL is set.
                            type: boolean
                          identity:
                            description: CloudIdentity defines cloud identity configuration
                            properties:
                              autoCreate:
                                description: AutoCreateSpec defines auto-creation
                                  of service accounts
                                properties:
                                  annotations:
                                    additionalProperties:
                                      type: string
                                    type: object
                                  enabled:
                                    default: true
                                    type: boolean
                                type: object
                              provider:
                                enum:
                                - aws
                                - gcp
                                - azure
                                type: string
                              serviceAccount:
                                type: string
                            required:
                            - provider
                            type: object
                          provider:
                            enum:
                            - aws
                            - gcp
                            - azure
                            type: string
                        type: object
                      path:
                        type: string
                      pvc:
                        description: PVC configuration
                        properties:
                          name:
                            description: Name of the PVC to use (for referencing existing
                              PVCs)
                            type: string
                          size:
                            type: string
                          storageClassName:
                            type: string
                        type: object
                      type:
                        enum:
                        - s3
                        - gcs
                        - azure
                        - pvc
                        type: string
                    required:
                    - type
                    type: object
                type: object
              config:
                additionalProperties:
                  type: string
                description: Custom configuration for Neo4j (single mode only)
                type: object
              env:
                description: Environment variables for Neo4j pod
                items:
                  description: EnvVar represents an environment variable present in
                    a Container.
                  properties:
                    name:
                      description: |-
                        Name of the environment variable.
                        May consist of any printable ASCII characters except '='.
                      type: string
                    value:
                      description: |-
                        Variable references $(VAR_NAME) are expanded
                        using the previously defined environment variables in the container and
                        any service environment variables. If a variable cannot be resolved,
                        the reference in the input string will be unchanged. Double $$ are reduced
                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                        Escaped references will never be expanded, regardless of whether the variable
                        exists or not.
                        Defaults to "".
                      type: string
                    valueFrom:
                      description: Source for the environment variable's value. Cannot
                        be used if value is not empty.
                      properties:
                        configMapKeyRef:
                          description: Selects a key of a ConfigMap.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the ConfigMap or its key
                                must be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        fieldRef:
                          description: |-
                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                          properties:
                            apiVersion:
                              description: Version of the schema the FieldPath is
                                written in terms of, defaults to "v1".
                              type: string
                            fieldPath:
                              description: Path of the field to select in the specified
                                API version.
                              type: string
                          required:
                          - fieldPath
                          type: object
                          x-kubernetes-map-type: atomic
                        fileKeyRef:
                          description: |-
                            FileKeyRef selects a key of the env file.
                            Requires the EnvFiles feature gate to be enabled.
                          properties:
                            key:
                              description: |-
                                The key within the env file. An invalid key will prevent the pod from starting.
                                The keys defined within a source may consist of any printable ASCII characters except '='.
                                During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                              type: string
                            optional:
                              default: false
                              description: |-
                                Specify whether the file or its key must be defined. If the file or key
                                does not exist, then the env var is not published.
                                If optional is set to true and the specified key does not exist,
                                the environment variable will not be set in the Pod's containers.

                                If optional is set to false and the specified key does not exist,
                                an error will be returned during Pod creation.
                              type: boolean
                            path:
                              description: |-
                                The path within the volume from which to select the file.
                                Must be relative and may not contain the '..' path or start with '..'.
                              type: string
                            volumeName:
                              description: The name of the volume mount containing
                                the env file.
                              type: string
                          required:
                          - key
                          - path
                          - volumeName
                          type: object
                          x-kubernetes-map-type: atomic
                        resourceFieldRef:
                          description: |-
                            Selects a resource of the container: only resources limits and requests
                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                          properties:
                            containerName:
                              description: 'Container name: required for volumes,
                                optional for env vars'
                              type: string
                            divisor:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Specifies the output format of the exposed
                                resources, defaults to "1"
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            resource:
                              description: 'Required: resource to select'
                              type: string
                          required:
                          - resource
                          type: object
                          x-kubernetes-map-type: atomic
                        secretKeyRef:
                          description: Selects a key of a secret in the pod's namespace
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                  required:
                  - name
                  type: object
                type: array
              image:
                description: ImageSpec defines the Neo4j image configuration
                properties:
                  pullPolicy:
                    default: IfNotPresent
                    type: string
                  pullSecrets:
                    items:
                      type: string
                    type: array
                  repo:
                    type: string
                  tag:
                    type: string
                required:
                - repo
                - tag
                type: object
              mcp:
                description: MCP server configuration for this standalone deployment
                properties:
                  auth:
                    description: |-
                      Auth allows overriding the Neo4j credentials used by the MCP server.
                      Only effective for stdio transport: in STDIO mode the operator injects
                      NEO4J_USERNAME and NEO4J_PASSWORD from this secret. When not set, the
                      cluster/standalone admin secret is used.

                      In HTTP mode credentials come from each client request (Basic Auth or
                      Bearer token) and this field is ignored.
                    properties:
                      passwordKey:
                        default: password
                        type: string
                      secretName:
                        description: SecretName references a secret with username/password
                          keys.
                        type: string
                      usernameKey:
                        default: username
                        type: string
                    type: object
                  database:
                    description: Database is the default Neo4j database name for MCP
                      queries.
                    type: string
                  enabled:
                    default: false
                    description: Enable MCP server deployment.
                    type: boolean
                  env:
                    description: |-
                      Environment variables injected into MCP pods.
                      The following operator-managed variables are silently ignored if present:
                      NEO4J_URI, NEO4J_USERNAME, NEO4J_PASSWORD, NEO4J_DATABASE, NEO4J_READ_ONLY,
                      NEO4J_SCHEMA_SAMPLE_SIZE, NEO4J_TELEMETRY, NEO4J_LOG_LEVEL, NEO4J_LOG_FORMAT,
                      NEO4J_TRANSPORT_MODE, NEO4J_MCP_HTTP_HOST, NEO4J_MCP_HTTP_PORT,
                      NEO4J_MCP_HTTP_TLS_ENABLED, NEO4J_MCP_HTTP_TLS_CERT_FILE,
                      NEO4J_MCP_HTTP_TLS_KEY_FILE, NEO4J_AUTH_HEADER_NAME.
                    items:
                      description: EnvVar represents an environment variable present
                        in a Container.
                      properties:
                        name:
                          description: |-
                            Name of the environment variable.
                            May consist of any printable ASCII characters except '='.
                          type: string
                        value:
                          description: |-
                            Variable references $(VAR_NAME) are expanded
                            using the previously defined environment variables in the container and
                            any service environment variables. If a variable cannot be resolved,
                            the reference in the input string will be unchanged. Double $$ are reduced
                            to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                            "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                            Escaped references will never be expanded, regardless of whether the variable
                            exists or not.
                            Defaults to "".
                          type: string
                        valueFrom:
                          description: Source for the environment variable's value.
                            Cannot be used if value is not empty.
                          properties:
                            configMapKeyRef:
                              description: Selects a key of a ConfigMap.
                              properties:
                                key:
                                  description: The key to select.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the ConfigMap or its
                                    key must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                            fieldRef:
                              description: |-
                                Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                              properties:
                                apiVersion:
                                  description: Version of the schema the FieldPath
                                    is written in terms of, defaults to "v1".
                                  type: string
                                fieldPath:
                                  description: Path of the field to select in the
                                    specified API version.
                                  type: string
                              required:
                              - fieldPath
                              type: object
                              x-kubernetes-map-type: atomic
                            fileKeyRef:
                              description: |-
                                FileKeyRef selects a key of the env file.
                                Requires the EnvFiles feature gate to be enabled.
                              properties:
                                key:
                                  description: |-
                                    The key within the env file. An invalid key will prevent the pod from starting.
                                    The keys defined within a source may consist of any printable ASCII characters except '='.
                                    During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                  type: string
                                optional:
                                  default: false
                                  description: |-
                                    Specify whether the file or its key must be defined. If the file or key
                                    does not exist, then the env var is not published.
                                    If optional is set to true and the specified key does not exist,
                                    the environment variable will not be set in the Pod's containers.

                                    If optional is set to false and the specified key does not exist,
                                    an error will be returned during Pod creation.
                                  type: boolean
                                path:
                                  description: |-
                                    The path within the volume from which to select the file.
                                    Must be relative and may not contain the '..' path or start with '..'.
                                  type: string
                                volumeName:
                                  description: The name of the volume mount containing
                                    the env file.
                                  type: string
                              required:
                              - key
                              - path
                              - volumeName
                              type: object
                              x-kubernetes-map-type: atomic
                            resourceFieldRef:
                              description: |-
                                Selects a resource of the container: only resources limits and requests
                                (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                              properties:
                                containerName:
                                  description: 'Container name: required for volumes,
                                    optional for env vars'
                                  type: string
                                divisor:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  description: Specifies the output format of the
                                    exposed resources, defaults to "1"
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                resource:
                                  description: 'Required: resource to select'
                                  type: string
                              required:
                              - resource
                              type: object
                              x-kubernetes-map-type: atomic
                            secretKeyRef:
                              description: Selects a key of a secret in the pod's
                                namespace
                              properties:
                                key:
                                  description: The key of the secret to select from.  Must
                                    be a valid secret key.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the Secret or its key
                                    must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  http:
                    description: HTTP settings (only used when transport=http).
                    properties:
                      authHeaderName:
                        description: |-
                          AuthHeaderName is the name of the HTTP header to read credentials from.
                          Defaults to "Authorization" (standard Basic Auth / Bearer token header).
                          Override when a reverse-proxy rewrites the Authorization header to a custom name.
                        type: string
                      host:
                        description: Host to bind the HTTP server to (defaults to
                          0.0.0.0).
                        type: string
                      port:
                        description: |-
                          Port to bind the HTTP server to.
                          Defaults to 8080 (no TLS) or 8443 (TLS) to avoid privileged ports in containers.
                          The mcp/neo4j image's own default is 80/443; we override to Kubernetes-friendly ports.
                        format: int32
                        type: integer
                      service:
                        description: Service exposure settings for HTTP transport.
                        properties:
                          annotations:
                            additionalProperties:
                              type: string
                            description: Annotations to add to the service.
                            type: object
                          externalTrafficPolicy:
                            description: 'External traffic policy: Cluster or Local.'
                            enum:
                            - Cluster
                            - Local
                            type: string
                          ingress:
                            description: Ingress configuration.
                            properties:
                              annotations:
                                additionalProperties:
                                  type: string
                                type: object
                              className:
                                type: string
                              enabled:
                                type: boolean
                              host:
                                type: string
                              tlsSecretName:
                                type: string
                            type: object
                          loadBalancerIP:
                            description: LoadBalancer specific configuration.
                            type: string
                          loadBalancerSourceRanges:
                            items:
                              type: string
                            type: array
                          port:
                            description: Port to expose for MCP HTTP server.
                            format: int32
                            type: integer
                          route:
                            description: Route configuration (OpenShift only).
                            properties:
                              annotations:
                                additionalProperties:
                                  type: string
                                description: Annotations to add to the Route
                                type: object
                              enabled:
                                description: Enable Route exposure (OpenShift only)
                                type: boolean
                              host:
                                description: Hostname for the Route (optional; generated
                                  by OpenShift if empty)
                                type: string
                              path:
                                description: Path for the Route (defaults to \"/\")
                                type: string
                              targetPort:
                                description: Target port on the service (defaults
                                  to HTTP port 7474)
                                format: int32
                                type: integer
                              tls:
                                description: TLS configuration for the Route
                                properties:
                                  insecureEdgeTerminationPolicy:
                                    enum:
                                    - None
                                    - Allow
                                    - Redirect
                                    type: string
                                  secretName:
                                    description: Secret containing the certificate
                                      (for reencrypt/passthrough)
                                    type: string
                                  termination:
                                    enum:
                                    - edge
                                    - reencrypt
                                    - passthrough
                                    type: string
                                type: object
                            type: object
                          type:
                            default: ClusterIP
                            description: 'Service type: ClusterIP, NodePort, LoadBalancer.'
                            enum:
                            - ClusterIP
                            - NodePort
                            - LoadBalancer
                            type: string
                        type: object
                      tls:
                        description: |-
                          TLS enables HTTPS using the official image's built-in TLS termination.
                          When set, the referenced Kubernetes TLS secret is mounted and
                          NEO4J_MCP_HTTP_TLS_ENABLED=true is injected with the cert/key paths.
                          When not set, TLS should be handled at the Ingress or LoadBalancer level instead.
                        properties:
                          certKey:
                            default: tls.crt
                            description: CertKey is the key in the secret that contains
                              the certificate file.
                            type: string
                          keyKey:
                            default: tls.key
                            description: KeyKey is the key in the secret that contains
                              the private key file.
                            type: string
                          secretName:
                            description: |-
                              SecretName is a Kubernetes TLS secret (type kubernetes.io/tls) containing the
                              certificate and private key. Required when TLS is enabled.
                            type: string
                        required:
                        - secretName
                        type: object
                    type: object
                  image:
                    description: |-
                      Image configuration for the MCP server.
                      Defaults to mcp/neo4j:latest (the official Docker Hub image by Neo4j).
                    properties:
                      pullPolicy:
                        default: IfNotPresent
                        type: string
                      pullSecrets:
                        items:
                          type: string
                        type: array
                      repo:
                        type: string
                      tag:
                        type: string
                    required:
                    - repo
                    - tag
                    type: object
                  logFormat:
                    description: LogFormat controls log output format.
                    enum:
                    - text
                    - json
                    type: string
                  logLevel:
                    description: LogLevel sets the server log verbosity.
                    enum:
                    - debug
                    - info
                    - notice
                    - warning
                    - error
                    - critical
                    - alert
                    - emergency
                    type: string
                  readOnly:
                    default: true
                    description: ReadOnly disables the write_neo4j_cypher tool when
                      true.
                    type: boolean
                  replicas:
                    description: Replicas controls the number of MCP server pods (for
                      HTTP mode).
                    format: int32
                    type: integer
                  resources:
                    description: Resource requirements for MCP pods.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  schemaSampleSize:
                    description: |-
                      SchemaSampleSize controls the sample size used by get-schema.
                      Lower values are faster; use -1 to sample the entire graph.
                    format: int32
                    minimum: -1
                    type: integer
                  securityContext:
                    description: SecurityContext allows overriding pod/container security
                      settings.
                    properties:
                      containerSecurityContext:
                        description: Container-level security settings for Neo4j containers
                        properties:
                          allowPrivilegeEscalation:
                            description: |-
                              AllowPrivilegeEscalation controls whether a process can gain more
                              privileges than its parent process. This bool directly controls if
                              the no_new_privs flag will be set on the container process.
                              AllowPrivilegeEscalation is true always when the container is:
                              1) run as Privileged
                              2) has CAP_SYS_ADMIN
                              Note that this field cannot be set when spec.os.name is windows.
                            type: boolean
                          appArmorProfile:
                            description: |-
                              appArmorProfile is the AppArmor options to use by this container. If set, this profile
                              overrides the pod's appArmorProfile.
                              Note that this field cannot be set when spec.os.name is windows.
                            properties:
                              localhostProfile:
                                description: |-
                                  localhostProfile indicates a profile loaded on the node that should be used.
                                  The profile must be preconfigured on the node to work.
                                  Must match the loaded name of the profile.
                                  Must be set if and only if type is "Localhost".
                                type: string
                              type:
                                description: |-
                                  type indicates which kind of AppArmor profile will be applied.
                                  Valid options are:
                                    Localhost - a profile pre-loaded on the node.
                                    RuntimeDefault - the container runtime's default profile.
                                    Unconfined - no AppArmor enforcement.
                                type: string
                            required:
                            - type
                            type: object
                          capabilities:
                            description: |-
                              The capabilities to add/drop when running containers.
                              Defaults to the default set of capabilities granted by the container runtime.
                              Note that this field cannot be set when spec.os.name is windows.
                            properties:
                              add:
                                description: Added capabilities
                                items:
                                  description: Capability represent POSIX capabilities
                                    type
                                  type: string
                                type: array
                                x-kubernetes-list-type: atomic
                              drop:
                                description: Removed capabilities
                                items:
                                  description: Capability represent POSIX capabilities
                                    type
                                  type: string
                                type: array
                                x-kubernetes-list-type: atomic
                            type: object
                          privileged:
                            description: |-
                              Run container in privileged mode.
                              Processes in privileged containers are essentially equivalent to root on the host.
                              Defaults to false.
                              Note that this field cannot be set when spec.os.name is windows.
                            type: boolean
                          procMount:
                            description: |-
                              procMount denotes the type of proc mount to use for the containers.
                              The default value is Default which uses the container runtime defaults for
                              readonly paths and masked paths.
                              This requires the ProcMountType feature flag to be enabled.
                              Note that this field cannot be set when spec.os.name is windows.
                            type: string
                          readOnlyRootFilesystem:
                            description: |-
                              Whether this container has a read-only root filesystem.
                              Default is false.
                              Note that this field cannot be set when spec.os.name is windows.
                            type: boolean
                          runAsGroup:
                            description: |-
                              The GID to run the entrypoint of the container process.
                              Uses runtime default if unset.
                              May also be set in PodSecurityContext.  If set in both SecurityContext and
                              PodSecurityContext, the value specified in SecurityContext takes precedence.
                              Note that this field cannot be set when spec.os.name is windows.
                            format: int64
                            type: integer
                          runAsNonRoot:
                            description: |-
                              Indicates that the container must run as a non-root user.
                              If true, the Kubelet will validate the image at runtime to ensure that it
                              does not run as UID 0 (root) and fail to start the container if it does.
                              If unset or false, no such validation will be performed.
                              May also be set in PodSecurityContext.  If set in both SecurityContext and
                              PodSecurityContext, the value specified in SecurityContext takes precedence.
                            type: boolean
                          runAsUser:
                            description: |-
                              The UID to run the entrypoint of the container process.
                              Defaults to user specified in image metadata if unspecified.
                              May also be set in PodSecurityContext.  If set in both SecurityContext and
                              PodSecurityContext, the value specified in SecurityContext takes precedence.
                              Note that this field cannot be set when spec.os.name is windows.
                            format: int64
                            type: integer
                          seLinuxOptions:
                            description: |-
                              The SELinux context to be applied to the container.
                              If unspecified, the container runtime will allocate a random SELinux context for each
                              container.  May also be set in PodSecurityContext.  If set in both SecurityContext and
                              PodSecurityContext, the value specified in SecurityContext takes precedence.
                              Note that this field cannot be set when spec.os.name is windows.
                            properties:
                              level:
                                description: Level is SELinux level label that applies
                                  to the container.
                                type: string
                              role:
                                description: Role is a SELinux role label that applies
                                  to the container.
                                type: string
                              type:
                                description: Type is a SELinux type label that applies
                                  to the container.
                                type: string
                              user:
                                description: User is a SELinux user label that applies
                                  to the container.
                                type: string
                            type: object
                          seccompProfile:
                            description: |-
                              The seccomp options to use by this container. If seccomp options are
                              provided at both the pod & container level, the container options
                              override the pod options.
                              Note that this field cannot be set when spec.os.name is windows.
                            properties:
                              localhostProfile:
                                description: |-
                                  localhostProfile indicates a profile defined in a file on the node should be used.
                                  The profile must be preconfigured on the node to work.
                                  Must be a descending path, relative to the kubelet's configured seccomp profile location.
                                  Must be set if type is "Localhost". Must NOT be set for any other type.
                                type: string
                              type:
                                description: |-
                                  type indicates which kind of seccomp profile will be applied.
                                  Valid options are:

                                  Localhost - a profile defined in a file on the node should be used.
                                  RuntimeDefault - the container runtime default profile should be used.
                                  Unconfined - no profile should be applied.
                                type: string
                            required:
                            - type
                            type: object
                          windowsOptions:
                            description: |-
                              The Windows specific settings applied to all containers.
                              If unspecified, the options from the PodSecurityContext will be used.
                              If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence.
                              Note that this field cannot be set when spec.os.name is linux.
                            properties:
                              gmsaCredentialSpec:
                                description: |-
                                  GMSACredentialSpec is where the GMSA admission webhook
                                  (https://github.com/kubernetes-sigs/windows-gmsa) inlines the contents of the
                                  GMSA credential spec named by the GMSACredentialSpecName field.
                                type: string
                              gmsaCredentialSpecName:
                                description: GMSACredentialSpecName is the name of
                                  the GMSA credential spec to use.
                                type: string
                              hostProcess:
                                description: |-
                                  HostProcess determines if a container should be run as a 'Host Process' container.
                                  All of a Pod's containers must have the same effective HostProcess value
                                  (it is not allowed to have a mix of HostProcess containers and non-HostProcess containers).
                                  In addition, if HostProcess is true then HostNetwork must also be set to true.
                                type: boolean
                              runAsUserName:
                                description: |-
                                  The UserName in Windows to run the entrypoint of the container process.
                                  Defaults to the user specified in image metadata if unspecified.
                                  May also be set in PodSecurityContext. If set in both SecurityContext and
                                  PodSecurityContext, the value specified in SecurityContext takes precedence.
                                type: string
                            type: object
                        type: object
                      podSecurityContext:
                        description: Pod-level security settings to apply to all Neo4j
                          pods
                        properties:
                          appArmorProfile:
                            description: |-
                              appArmorProfile is the AppArmor options to use by the containers in this pod.
                              Note that this field cannot be set when spec.os.name is windows.
                            properties:
                              localhostProfile:
                                description: |-
                                  localhostProfile indicates a profile loaded on the node that should be used.
                                  The profile must be preconfigured on the node to work.
                                  Must match the loaded name of the profile.
                                  Must be set if and only if type is "Localhost".
                                type: string
                              type:
                                description: |-
                                  type indicates which kind of AppArmor profile will be applied.
                                  Valid options are:
                                    Localhost - a profile pre-loaded on the node.
                                    RuntimeDefault - the container runtime's default profile.
                                    Unconfined - no AppArmor enforcement.
                                type: string
                            required:
                            - type
                            type: object
                          fsGroup:
                            description: |-
                              A special supplemental group that applies to all containers in a pod.
                              Some volume types allow the Kubelet to change the ownership of that volume
                              to be owned by the pod:

                              1. The owning GID will be the FSGroup
                              2. The setgid bit is set (new files created in the volume will be owned by FSGroup)
                              3. The permission bits are OR'd with rw-rw----

                              If unset, the Kubelet will not modify the ownership and permissions of any volume.
                              Note that this field cannot be set when spec.os.name is windows.
                            format: int64
                            type: integer
                          fsGroupChangePolicy:
                            description: |-
                              fsGroupChangePolicy defines behavior of changing ownership and permission of the volume
                              before being exposed inside Pod. This field will only apply to
                              volume types which support fsGroup based ownership(and permissions).
                              It will have no effect on ephemeral volume types such as: secret, configmaps
                              and emptydir.
                              Valid values are "OnRootMismatch" and "Always". If not specified, "Always" is used.
                              Note that this field cannot be set when spec.os.name is windows.
                            type: string
                          runAsGroup:
                            description: |-
                              The GID to run the entrypoint of the container process.
                              Uses runtime default if unset.
                              May also be set in SecurityContext.  If set in both SecurityContext and
                              PodSecurityContext, the value specified in SecurityContext takes precedence
                              for that container.
                              Note that this field cannot be set when spec.os.name is windows.
                            format: int64
                            type: integer
                          runAsNonRoot:
                            description: |-
                              Indicates that the container must run as a non-root user.
                              If true, the Kubelet will validate the image at runtime to ensure that it
                              does not run as UID 0 (root) and fail to start the container if it does.
                              If unset or false, no such validation will be performed.
                              May also be set in SecurityContext.  If set in both SecurityContext and
                              PodSecurityContext, the value specified in SecurityContext takes precedence.
                            type: boolean
                          runAsUser:
                            description: |-
                              The UID to run the entrypoint of the container process.
                              Defaults to user specified in image metadata if unspecified.
                              May also be set in SecurityContext.  If set in both SecurityContext and
                              PodSecurityContext, the value specified in SecurityContext takes precedence
                              for that container.
                              Note that this field cannot be set when spec.os.name is windows.
                            format: int64
                            type: integer
                          seLinuxChangePolicy:
                            description: |-
                              seLinuxChangePolicy defines how the container's SELinux label is applied to all volumes used by the Pod.
                              It has no effect on nodes that do not support SELinux or to volumes does not support SELinux.
                              Valid values are "MountOption" and "Recursive".

                              "Recursive" means relabeling of all files on all Pod volumes by the container runtime.
                              This may be slow for large volumes, but allows mixing privileged and unprivileged Pods sharing the same volume on the same node.

                              "MountOption" mounts all eligible Pod volumes with `-o context` mount option.
                              This requires all Pods that share the same volume to use the same SELinux label.
                              It is not possible to share the same volume among privileged and unprivileged Pods.
                              Eligible volumes are in-tree FibreChannel and iSCSI volumes, and all CSI volumes
                              whose CSI driver announces SELinux support by setting spec.seLinuxMount: true in their
                              CSIDriver instance. Other volumes are always re-labelled recursively.
                              "MountOption" value is allowed only when SELinuxMount feature gate is enabled.

                              If not specified and SELinuxMount feature gate is enabled, "MountOption" is used.
                              If not specified and SELinuxMount feature gate is disabled, "MountOption" is used for ReadWriteOncePod volumes
                              and "Recursive" for all other volumes.

                              This field affects only Pods that have SELinux label set, either in PodSecurityContext or in SecurityContext of all containers.

                              All Pods that use the same volume should use the same seLinuxChangePolicy, otherwise some pods can get stuck in ContainerCreating state.
                              Note that this field cannot be set when spec.os.name is windows.
                            type: string
                          seLinuxOptions:
                            description: |-
                              The SELinux context to be applied to all containers.
                              If unspecified, the container runtime will allocate a random SELinux context for each
                              container.  May also be set in SecurityContext.  If set in
                              both SecurityContext and PodSecurityContext, the value specified in SecurityContext
                              takes precedence for that container.
                              Note that this field cannot be set when spec.os.name is windows.
                            properties:
                              level:
                                description: Level is SELinux level label that applies
                                  to the container.
                                type: string
                              role:
                                description: Role is a SELinux role label that applies
                                  to the container.
                                type: string
                              type:
                                description: Type is a SELinux type label that applies
                                  to the container.
                                type: string
                              user:
                                description: User is a SELinux user label that applies
                                  to the container.
                                type: string
                            type: object
                          seccompProfile:
                            description: |-
                              The seccomp options to use by the containers in this pod.
                              Note that this field cannot be set when spec.os.name is windows.
                            properties:
                              localhostProfile:
                                description: |-
                                  localhostProfile indicates a profile defined in a file on the node should be used.
                                  The profile must be preconfigured on the node to work.
                                  Must be a descending path, relative to the kubelet's configured seccomp profile location.
                                  Must be set if type is "Localhost". Must NOT be set for any other type.
                                type: string
                              type:
                                description: |-
                                  type indicates which kind of seccomp profile will be applied.
                                  Valid options are:

                                  Localhost - a profile defined in a file on the node should be used.
                                  RuntimeDefault - the container runtime default profile should be used.
                                  Unconfined - no profile should be applied.
                                type: string
                            required:
                            - type
                            type: object
                          supplementalGroups:
                            description: |-
                              A list of groups applied to the first process run in each container, in
                              addition to the container's primary GID and fsGroup (if specified).  If
                              the SupplementalGroupsPolicy feature is enabled, the
                              supplementalGroupsPolicy field determines whether these are in addition
                              to or instead of any group memberships defined in the container image.
                              If unspecified, no additional groups are added, though group memberships
                              defined in the container image may still be used, depending on the
                              supplementalGroupsPolicy field.
                              Note that this field cannot be set when spec.os.name is windows.
                            items:
                              format: int64
                              type: integer
                            type: array
                            x-kubernetes-list-type: atomic
                          supplementalGroupsPolicy:
                            description: |-
                              Defines how supplemental groups of the first container processes are calculated.
                              Valid values are "Merge" and "Strict". If not specified, "Merge" is used.
                              (Alpha) Using the field requires the SupplementalGroupsPolicy feature gate to be enabled
                              and the container runtime must implement support for this feature.
                              Note that this field cannot be set when spec.os.name is windows.
                            type: string
                          sysctls:
                            description: |-
                              Sysctls hold a list of namespaced sysctls used for the pod. Pods with unsupported
                              sysctls (by the container runtime) might fail to launch.
                              Note that this field cannot be set when spec.os.name is windows.
                            items:
                              description: Sysctl defines a kernel parameter to be
                                set
                              properties:
                                name:
                                  description: Name of a property to set
                                  type: string
                                value:
                                  description: Value of a property to set
                                  type: string
                              required:
                              - name
                              - value
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                          windowsOptions:
                            description: |-
                              The Windows specific settings applied to all containers.
                              If unspecified, the options within a container's SecurityContext will be used.
                              If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence.
                              Note that this field cannot be set when spec.os.name is linux.
                            properties:
                              gmsaCredentialSpec:
                                description: |-
                                  GMSACredentialSpec is where the GMSA admission webhook
                                  (https://github.com/kubernetes-sigs/windows-gmsa) inlines the contents of the
                                  GMSA credential spec named by the GMSACredentialSpecName field.
                                type: string
                              gmsaCredentialSpecName:
                                description: GMSACredentialSpecName is the name of
                                  the GMSA credential spec to use.
                                type: string
                              hostProcess:
                                description: |-
                                  HostProcess determines if a container should be run as a 'Host Process' container.
                                  All of a Pod's containers must have the same effective HostProcess value
                                  (it is not allowed to have a mix of HostProcess containers and non-HostProcess containers).
                                  In addition, if HostProcess is true then HostNetwork must also be set to true.
                                type: boolean
                              runAsUserName:
                                description: |-
                                  The UserName in Windows to run the entrypoint of the container process.
                                  Defaults to the user specified in image metadata if unspecified.
                                  May also be set in PodSecurityContext. If set in both SecurityContext and
                                  PodSecurityContext, the value specified in SecurityContext takes precedence.
                                type: string
                            type: object
                        type: object
                    type: object
                  telemetry:
                    description: |-
                      Telemetry controls anonymous usage telemetry sent to Neo4j.
                      Defaults to the server's own default (true).
                      Set to false to disable telemetry.
                    type: boolean
                  transport:
                    default: http
                    description: 'Transport mode for the MCP server: http or stdio.'
                    enum:
                    - http
                    - stdio
                    type: string
                type: object
              monitoring:
                description: Monitoring configuration (Prometheus metrics, query logging,
                  diagnostics)
                properties:
                  enabled:
                    default: true
                    description: Enable query monitoring
                    type: boolean
                  explainPlan:
                    default: false
                    description: |-
                      Enable query plan explanation in logs. WARNING: enabling this has measurable
                      performance impact on high-throughput workloads. Recommended false in production.
                    type: boolean
                  metricsExport:
                    description: Metrics export configuration
                    properties:
                      customEndpoint:
                        description: Export to custom endpoint
                        type: string
                      interval:
                        description: Export interval
                        type: string
                      prometheus:
                        description: Export to Prometheus
                        type: boolean
                    type: object
                  metricsFilter:
                    description: |-
                      Glob pattern to select which Neo4j metrics are active (maps to server.metrics.filter).
                      Only a subset of metrics is enabled by default. Use "*" to enable all metrics,
                      or specific patterns like "*bolt*,*transaction*,*page_cache*".
                    type: string
                  metricsPrefix:
                    description: |-
                      Custom prefix for all Neo4j metric names (maps to server.metrics.prefix).
                      Useful when multiple Neo4j deployments share one Prometheus instance.
                    type: string
                  obfuscateLiterals:
                    default: false
                    description: |-
                      Obfuscate literal values in query logs. Recommended true in production
                      to avoid leaking sensitive data (passwords, PII) into log files.
                    type: boolean
                  queryLogLevel:
                    default: INFO
                    description: |-
                      Query log verbosity level. Use OFF to disable query logging, INFO to
                      log only slow queries (those exceeding slowQueryThreshold), and
                      VERBOSE to log all queries regardless of duration.
                    enum:
                    - "OFF"
                    - INFO
                    - VERBOSE
                    type: string
                  sampling:
                    description: Query sampling configuration
                    properties:
                      maxQueriesPerSecond:
                        description: Maximum queries to sample per second
                        format: int32
                        type: integer
                      rate:
                        description: |-
                          Sampling rate as a decimal between 0 and 1 inclusive — for example
                          "0.5" for 50% sampling, "1.0" for every query, "0" to disable.
                        pattern: ^(0(\.\d+)?|1(\.0+)?)$
                        type: string
                    type: object
                  slowQueryThreshold:
                    default: 5s
                    description: Slow query threshold — maps to db.logs.query.threshold
                      in Neo4j config
                    type: string
                type: object
              nodeSelector:
                additionalProperties:
                  type: string
                description: Node selector for pod scheduling
                type: object
              resources:
                description: Resource requirements for Neo4j pod
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              restoreFrom:
                description: RestoreFrom specifies backup to restore from during standalone
                  creation
                properties:
                  backupRef:
                    description: Backup reference to restore from
                    type: string
                  pointInTime:
                    description: Point in time for restore
                    format: date-time
                    type: string
                  storage:
                    description: Direct storage location
                    properties:
                      bucket:
                        type: string
                      cloud:
                        description: Cloud provider configuration
                        properties:
                          credentialsSecretRef:
                            description: |-
                              CredentialsSecretRef is the name of a Kubernetes Secret containing
                              cloud provider credentials as environment variables. Optional when
                              using workload identity / IAM instance profiles.
                              For S3:    keys AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_REGION
                              For GCS:   key  GOOGLE_APPLICATION_CREDENTIALS_JSON (base64 service-account JSON)
                              For Azure: keys AZURE_STORAGE_ACCOUNT, AZURE_STORAGE_KEY
                            type: string
                          endpointURL:
                            description: |-
                              EndpointURL overrides the S3 API endpoint URL. Use this to target
                              S3-compatible stores such as MinIO, Ceph RGW, or Cloudflare R2.
                              Example: "http://minio.minio-ns.svc:9000"
                              Only applies to the "aws" provider; ignored for gcp and azure.
                            type: string
                          forcePathStyle:
                            description: |-
                              ForcePathStyle forces S3 path-style addressing, where the bucket name
                              appears in the URL path (e.g. http://endpoint/bucket/key) rather than
                              the subdomain (e.g. http://bucket.endpoint/key).
                              Required for MinIO and most self-hosted S3-compatible stores.
                              Only effective when EndpointURL is set.
                            type: boolean
                          identity:
                            description: CloudIdentity defines cloud identity configuration
                            properties:
                              autoCreate:
                                description: AutoCreateSpec defines auto-creation
                                  of service accounts
                                properties:
                                  annotations:
                                    additionalProperties:
                                      type: string
                                    type: object
                                  enabled:
                                    default: true
                                    type: boolean
                                type: object
                              provider:
                                enum:
                                - aws
                                - gcp
                                - azure
                                type: string
                              serviceAccount:
                                type: string
                            required:
                            - provider
                            type: object
                          provider:
                            enum:
                            - aws
                            - gcp
                            - azure
                            type: string
                        type: object
                      path:
                        type: string
                      pvc:
                        description: PVC configuration
                        properties:
                          name:
                            description: Name of the PVC to use (for referencing existing
                              PVCs)
                            type: string
                          size:
                            type: string
                          storageClassName:
                            type: string
                        type: object
                      type:
                        enum:
                        - s3
                        - gcs
                        - azure
                        - pvc
                        type: string
                    required:
                    - type
                    type: object
                type: object
              securityContext:
                description: SecurityContext allows overriding pod/container security
                  settings (e.g., for OpenShift SCC compatibility)
                properties:
                  containerSecurityContext:
                    description: Container-level security settings for Neo4j containers
                    properties:
                      allowPrivilegeEscalation:
                        description: |-
                          AllowPrivilegeEscalation controls whether a process can gain more
                          privileges than its parent process. This bool directly controls if
                          the no_new_privs flag will be set on the container process.
                          AllowPrivilegeEscalation is true always when the container is:
                          1) run as Privileged
                          2) has CAP_SYS_ADMIN
                          Note that this field cannot be set when spec.os.name is windows.
                        type: boolean
                      appArmorProfile:
                        description: |-
                          appArmorProfile is the AppArmor options to use by this container. If set, this profile
                          overrides the pod's appArmorProfile.
                          Note that this field cannot be set when spec.os.name is windows.
                        properties:
                          localhostProfile:
                            description: |-
                              localhostProfile indicates a profile loaded on the node that should be used.
                              The profile must be preconfigured on the node to work.
                              Must match the loaded name of the profile.
                              Must be set if and only if type is "Localhost".
                            type: string
                          type:
                            description: |-
                              type indicates which kind of AppArmor profile will be applied.
                              Valid options are:
                                Localhost - a profile pre-loaded on the node.
                                RuntimeDefault - the container runtime's default profile.
                                Unconfined - no AppArmor enforcement.
                            type: string
                        required:
                        - type
                        type: object
                      capabilities:
                        description: |-
                          The capabilities to add/drop when running containers.
                          Defaults to the default set of capabilities granted by the container runtime.
                          Note that this field cannot be set when spec.os.name is windows.
                        properties:
                          add:
                            description: Added capabilities
                            items:
                              description: Capability represent POSIX capabilities
                                type
                              type: string
                            type: array
                            x-kubernetes-list-type: atomic
                          drop:
                            description: Removed capabilities
                            items:
                              description: Capability represent POSIX capabilities
                                type
                              type: string
                            type: array
                            x-kubernetes-list-type: atomic
                        type: object
                      privileged:
                        description: |-
                          Run container in privileged mode.
                          Processes in privileged containers are essentially equivalent to root on the host.
                          Defaults to false.
                          Note that this field cannot be set when spec.os.name is windows.
                        type: boolean
                      procMount:
                        description: |-
                          procMount denotes the type of proc mount to use for the containers.
                          The default value is Default which uses the container runtime defaults for
                          readonly paths and masked paths.
                          This requires the ProcMountType feature flag to be enabled.
                          Note that this field cannot be set when spec.os.name is windows.
                        type: string
                      readOnlyRootFilesystem:
                        description: |-
                          Whether this container has a read-only root filesystem.
                          Default is false.
                          Note that this field cannot be set when spec.os.name is windows.
                        type: boolean
                      runAsGroup:
                        description: |-
                          The GID to run the entrypoint of the container process.
                          Uses runtime default if unset.
                          May also be set in PodSecurityContext.  If set in both SecurityContext and
                          PodSecurityContext, the value specified in SecurityContext takes precedence.
                          Note that this field cannot be set when spec.os.name is windows.
                        format: int64
                        type: integer
                      runAsNonRoot:
                        description: |-
                          Indicates that the container must run as a non-root user.
                          If true, the Kubelet will validate the image at runtime to ensure that it
                          does not run as UID 0 (root) and fail to start the container if it does.
                          If unset or false, no such validation will be performed.
                          May also be set in PodSecurityContext.  If set in both SecurityContext and
                          PodSecurityContext, the value specified in SecurityContext takes precedence.
                        type: boolean
                      runAsUser:
                        description: |-
                          The UID to run the entrypoint of the container process.
                          Defaults to user specified in image metadata if unspecified.
                          May also be set in PodSecurityContext.  If set in both SecurityContext and
                          PodSecurityContext, the value specified in SecurityContext takes precedence.
                          Note that this field cannot be set when spec.os.name is windows.
                        format: int64
                        type: integer
                      seLinuxOptions:
                        description: |-
                          The SELinux context to be applied to the container.
                          If unspecified, the container runtime will allocate a random SELinux context for each
                          container.  May also be set in PodSecurityContext.  If set in both SecurityContext and
                          PodSecurityContext, the value specified in SecurityContext takes precedence.
                          Note that this field cannot be set when spec.os.name is windows.
                        properties:
                          level:
                            description: Level is SELinux level label that applies
                              to the container.
                            type: string
                          role:
                            description: Role is a SELinux role label that applies
                              to the container.
                            type: string
                          type:
                            description: Type is a SELinux type label that applies
                              to the container.
                            type: string
                          user:
                            description: User is a SELinux user label that applies
                              to the container.
                            type: string
                        type: object
                      seccompProfile:
                        description: |-
                          The seccomp options to use by this container. If seccomp options are
                          provided at both the pod & container level, the container options
                          override the pod options.
                          Note that this field cannot be set when spec.os.name is windows.
                        properties:
                          localhostProfile:
                            description: |-
                              localhostProfile indicates a profile defined in a file on the node should be used.
                              The profile must be preconfigured on the node to work.
                              Must be a descending path, relative to the kubelet's configured seccomp profile location.
                              Must be set if type is "Localhost". Must NOT be set for any other type.
                            type: string
                          type:
                            description: |-
                              type indicates which kind of seccomp profile will be applied.
                              Valid options are:

                              Localhost - a profile defined in a file on the node should be used.
                              RuntimeDefault - the container runtime default profile should be used.
                              Unconfined - no profile should be applied.
                            type: string
                        required:
                        - type
                        type: object
                      windowsOptions:
                        description: |-
                          The Windows specific settings applied to all containers.
                          If unspecified, the options from the PodSecurityContext will be used.
                          If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence.
                          Note that this field cannot be set when spec.os.name is linux.
                        properties:
                          gmsaCredentialSpec:
                            description: |-
                              GMSACredentialSpec is where the GMSA admission webhook
                              (https://github.com/kubernetes-sigs/windows-gmsa) inlines the contents of the
                              GMSA credential spec named by the GMSACredentialSpecName field.
                            type: string
                          gmsaCredentialSpecName:
                            description: GMSACredentialSpecName is the name of the
                              GMSA credential spec to use.
                            type: string
                          hostProcess:
                            description: |-
                              HostProcess determines if a container should be run as a 'Host Process' container.
                              All of a Pod's containers must have the same effective HostProcess value
                              (it is not allowed to have a mix of HostProcess containers and non-HostProcess containers).
                              In addition, if HostProcess is true then HostNetwork must also be set to true.
                            type: boolean
                          runAsUserName:
                            description: |-
                              The UserName in Windows to run the entrypoint of the container process.
                              Defaults to the user specified in image metadata if unspecified.
                              May also be set in PodSecurityContext. If set in both SecurityContext and
                              PodSecurityContext, the value specified in SecurityContext takes precedence.
                            type: string
                        type: object
                    type: object
                  podSecurityContext:
                    description: Pod-level security settings to apply to all Neo4j
                      pods
                    properties:
                      appArmorProfile:
                        description: |-
                          appArmorProfile is the AppArmor options to use by the containers in this pod.
                          Note that this field cannot be set when spec.os.name is windows.
                        properties:
                          localhostProfile:
                            description: |-
                              localhostProfile indicates a profile loaded on the node that should be used.
                              The profile must be preconfigured on the node to work.
                              Must match the loaded name of the profile.
                              Must be set if and only if type is "Localhost".
                            type: string
                          type:
                            description: |-
                              type indicates which kind of AppArmor profile will be applied.
                              Valid options are:
                                Localhost - a profile pre-loaded on the node.
                                RuntimeDefault - the container runtime's default profile.
                                Unconfined - no AppArmor enforcement.
                            type: string
                        required:
                        - type
                        type: object
                      fsGroup:
                        description: |-
                          A special supplemental group that applies to all containers in a pod.
                          Some volume types allow the Kubelet to change the ownership of that volume
                          to be owned by the pod:

                          1. The owning GID will be the FSGroup
                          2. The setgid bit is set (new files created in the volume will be owned by FSGroup)
                          3. The permission bits are OR'd with rw-rw----

                          If unset, the Kubelet will not modify the ownership and permissions of any volume.
                          Note that this field cannot be set when spec.os.name is windows.
                        format: int64
                        type: integer
                      fsGroupChangePolicy:
                        description: |-
                          fsGroupChangePolicy defines behavior of changing ownership and permission of the volume
                          before being exposed inside Pod. This field will only apply to
                          volume types which support fsGroup based ownership(and permissions).
                          It will have no effect on ephemeral volume types such as: secret, configmaps
                          and emptydir.
                          Valid values are "OnRootMismatch" and "Always". If not specified, "Always" is used.
                          Note that this field cannot be set when spec.os.name is windows.
                        type: string
                      runAsGroup:
                        description: |-
                          The GID to run the entrypoint of the container process.
                          Uses runtime default if unset.
                          May also be set in SecurityContext.  If set in both SecurityContext and
                          PodSecurityContext, the value specified in SecurityContext takes precedence
                          for that container.
                          Note that this field cannot be set when spec.os.name is windows.
                        format: int64
                        type: integer
                      runAsNonRoot:
                        description: |-
                          Indicates that the container must run as a non-root user.
                          If true, the Kubelet will validate the image at runtime to ensure that it
                          does not run as UID 0 (root) and fail to start the container if it does.
                          If unset or false, no such validation will be performed.
                          May also be set in SecurityContext.  If set in both SecurityContext and
                          PodSecurityContext, the value specified in SecurityContext takes precedence.
                        type: boolean
                      runAsUser:
                        description: |-
                          The UID to run the entrypoint of the container process.
                          Defaults to user specified in image metadata if unspecified.
                          May also be set in SecurityContext.  If set in both SecurityContext and
                          PodSecurityContext, the value specified in SecurityContext takes precedence
                          for that container.
                          Note that this field cannot be set when spec.os.name is windows.
                        format: int64
                        type: integer
                      seLinuxChangePolicy:
                        description: |-
                          seLinuxChangePolicy defines how the container's SELinux label is applied to all volumes used by the Pod.
                          It has no effect on nodes that do not support SELinux or to volumes does not support SELinux.
                          Valid values are "MountOption" and "Recursive".

                          "Recursive" means relabeling of all files on all Pod volumes by the container runtime.
                          This may be slow for large volumes, but allows mixing privileged and unprivileged Pods sharing the same volume on the same node.

                          "MountOption" mounts all eligible Pod volumes with `-o context` mount option.
                          This requires all Pods that share the same volume to use the same SELinux label.
                          It is not possible to share the same volume among privileged and unprivileged Pods.
                          Eligible volumes are in-tree FibreChannel and iSCSI volumes, and all CSI volumes
                          whose CSI driver announces SELinux support by setting spec.seLinuxMount: true in their
                          CSIDriver instance. Other volumes are always re-labelled recursively.
                          "MountOption" value is allowed only when SELinuxMount feature gate is enabled.

                          If not specified and SELinuxMount feature gate is enabled, "MountOption" is used.
                          If not specified and SELinuxMount feature gate is disabled, "MountOption" is used for ReadWriteOncePod volumes
                          and "Recursive" for all other volumes.

                          This field affects only Pods that have SELinux label set, either in PodSecurityContext or in SecurityContext of all containers.

                          All Pods that use the same volume should use the same seLinuxChangePolicy, otherwise some pods can get stuck in ContainerCreating state.
                          Note that this field cannot be set when spec.os.name is windows.
                        type: string
                      seLinuxOptions:
                        description: |-
                          The SELinux context to be applied to all containers.
                          If unspecified, the container runtime will allocate a random SELinux context for each
                          container.  May also be set in SecurityContext.  If set in
                          both SecurityContext and PodSecurityContext, the value specified in SecurityContext
                          takes precedence for that container.
                          Note that this field cannot be set when spec.os.name is windows.
                        properties:
                          level:
                            description: Level is SELinux level label that applies
                              to the container.
                            type: string
                          role:
                            description: Role is a SELinux role label that applies
                              to the container.
                            type: string
                          type:
                            description: Type is a SELinux type label that applies
                              to the container.
                            type: string
                          user:
                            description: User is a SELinux user label that applies
                              to the container.
                            type: string
                        type: object
                      seccompProfile:
                        description: |-
                          The seccomp options to use by the containers in this pod.
                          Note that this field cannot be set when spec.os.name is windows.
                        properties:
                          localhostProfile:
                            description: |-
                              localhostProfile indicates a profile defined in a file on the node should be used.
                              The profile must be preconfigured on the node to work.
                              Must be a descending path, relative to the kubelet's configured seccomp profile location.
                              Must be set if type is "Localhost". Must NOT be set for any other type.
                            type: string
                          type:
                            description: |-
                              type indicates which kind of seccomp profile will be applied.
                              Valid options are:

                              Localhost - a profile defined in a file on the node should be used.
                              RuntimeDefault - the container runtime default profile should be used.
                              Unconfined - no profile should be applied.
                            type: string
                        required:
                        - type
                        type: object
                      supplementalGroups:
                        description: |-
                          A list of groups applied to the first process run in each container, in
                          addition to the container's primary GID and fsGroup (if specified).  If
                          the SupplementalGroupsPolicy feature is enabled, the
                          supplementalGroupsPolicy field determines whether these are in addition
                          to or instead of any group memberships defined in the container image.
                          If unspecified, no additional groups are added, though group memberships
                          defined in the container image may still be used, depending on the
                          supplementalGroupsPolicy field.
                          Note that this field cannot be set when spec.os.name is windows.
                        items:
                          format: int64
                          type: integer
                        type: array
                        x-kubernetes-list-type: atomic
                      supplementalGroupsPolicy:
                        description: |-
                          Defines how supplemental groups of the first container processes are calculated.
                          Valid values are "Merge" and "Strict". If not specified, "Merge" is used.
                          (Alpha) Using the field requires the SupplementalGroupsPolicy feature gate to be enabled
                          and the container runtime must implement support for this feature.
                          Note that this field cannot be set when spec.os.name is windows.
                        type: string
                      sysctls:
                        description: |-
                          Sysctls hold a list of namespaced sysctls used for the pod. Pods with unsupported
                          sysctls (by the container runtime) might fail to launch.
                          Note that this field cannot be set when spec.os.name is windows.
                        items:
                          description: Sysctl defines a kernel parameter to be set
                          properties:
                            name:
                              description: Name of a property to set
                              type: string
                            value:
                              description: Value of a property to set
                              type: string
                          required:
                          - name
                          - value
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      windowsOptions:
                        description: |-
                          The Windows specific settings applied to all containers.
                          If unspecified, the options within a container's SecurityContext will be used.
                          If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence.
                          Note that this field cannot be set when spec.os.name is linux.
                        properties:
                          gmsaCredentialSpec:
                            description: |-
                              GMSACredentialSpec is where the GMSA admission webhook
                              (https://github.com/kubernetes-sigs/windows-gmsa) inlines the contents of the
                              GMSA credential spec named by the GMSACredentialSpecName field.
                            type: string
                          gmsaCredentialSpecName:
                            description: GMSACredentialSpecName is the name of the
                              GMSA credential spec to use.
                            type: string
                          hostProcess:
                            description: |-
                              HostProcess determines if a container should be run as a 'Host Process' container.
                              All of a Pod's containers must have the same effective HostProcess value
                              (it is not allowed to have a mix of HostProcess containers and non-HostProcess containers).
                              In addition, if HostProcess is true then HostNetwork must also be set to true.
                            type: boolean
                          runAsUserName:
                            description: |-
                              The UserName in Windows to run the entrypoint of the container process.
                              Defaults to the user specified in image metadata if unspecified.
                              May also be set in PodSecurityContext. If set in both SecurityContext and
                              PodSecurityContext, the value specified in SecurityContext takes precedence.
                            type: string
                        type: object
                    type: object
                type: object
              service:
                description: ServiceSpec defines service configuration
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations to add to the service
                    type: object
                  externalTrafficPolicy:
                    description: 'External traffic policy: Cluster or Local'
                    enum:
                    - Cluster
                    - Local
                    type: string
                  ingress:
                    description: Ingress configuration
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        type: object
                      className:
                        type: string
                      enabled:
                        type: boolean
                      host:
                        type: string
                      tlsSecretName:
                        type: string
                    type: object
                  loadBalancerIP:
                    description: LoadBalancer specific configuration
                    type: string
                  loadBalancerSourceRanges:
                    items:
                      type: string
                    type: array
                  route:
                    description: Route configuration (OpenShift only)
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations to add to the Route
                        type: object
                      enabled:
                        description: Enable Route exposure (OpenShift only)
                        type: boolean
                      host:
                        description: Hostname for the Route (optional; generated by
                          OpenShift if empty)
                        type: string
                      path:
                        description: Path for the Route (defaults to \"/\")
                        type: string
                      targetPort:
                        description: Target port on the service (defaults to HTTP
                          port 7474)
                        format: int32
                        type: integer
                      tls:
                        description: TLS configuration for the Route
                        properties:
                          insecureEdgeTerminationPolicy:
                            enum:
                            - None
                            - Allow
                            - Redirect
                            type: string
                          secretName:
                            description: Secret containing the certificate (for reencrypt/passthrough)
                            type: string
                          termination:
                            enum:
                            - edge
                            - reencrypt
                            - passthrough
                            type: string
                        type: object
                    type: object
                  type:
                    default: ClusterIP
                    description: 'Service type: ClusterIP, NodePort, LoadBalancer'
                    enum:
                    - ClusterIP
                    - NodePort
                    - LoadBalancer
                    type: string
                type: object
              storage:
                description: StorageSpec defines storage configuration
                properties:
                  backupStorage:
                    description: Additional storage for backups
                    properties:
                      className:
                        type: string
                      size:
                        type: string
                    type: object
                  className:
                    type: string
                  retentionPolicy:
                    default: Delete
                    description: PVC retention policy when cluster is deleted
                    enum:
                    - Delete
                    - Retain
                    type: string
                  size:
                    type: string
                required:
                - className
                - size
                type: object
              tls:
                description: TLSSpec defines TLS configuration
                properties:
                  certificateSecret:
                    description: Manual certificate configuration
                    type: string
                  duration:
                    description: Certificate duration and renewal settings
                    type: string
                  externalSecrets:
                    description: External Secrets configuration for TLS certificates
                    properties:
                      data:
                        description: Data mapping from external secret store
                        items:
                          description: ExternalSecretData defines data mapping from
                            external secret store
                          properties:
                            remoteRef:
                              description: Remote reference to the secret in external
                                store
                              properties:
                                key:
                                  type: string
                                property:
                                  description: Property within the secret (for JSON/YAML
                                    secrets)
                                  type: string
                                version:
                                  description: Version of the secret
                                  type: string
                              required:
                              - key
                              type: object
                            secretKey:
                              type: string
                          required:
                          - secretKey
                          type: object
                        type: array
                      enabled:
                        default: false
                        description: Enable External Secrets Operator integration
                        type: boolean
                      refreshInterval:
                        default: 1h
                        description: Refresh interval for external secrets
                        type: string
                      secretStoreRef:
                        description: SecretStore reference for External Secrets
                        properties:
                          kind:
                            default: SecretStore
                            enum:
                            - SecretStore
                            - ClusterSecretStore
                            type: string
                          name:
                            type: string
                        required:
                        - name
                        type: object
                    type: object
                  issuerRef:
                    description: |-
                      IssuerRef references a cert-manager-compatible issuer.
                      Supports standard cert-manager issuers (Issuer, ClusterIssuer) as well as
                      third-party external issuers (e.g. AWSPCAClusterIssuer, VaultIssuer) by
                      specifying the appropriate kind and group.
                    properties:
                      group:
                        description: |-
                          Group of the issuer's API. Defaults to cert-manager.io for standard
                          cert-manager issuers. Set to the external issuer's API group for
                          third-party issuers (e.g. awspca.cert-manager.io).
                        type: string
                      kind:
                        default: ClusterIssuer
                        description: |-
                          Kind of the issuer resource. Defaults to ClusterIssuer for standard
                          cert-manager usage. Set to the custom resource kind for external issuers
                          (e.g. AWSPCAClusterIssuer).
                        type: string
                      name:
                        type: string
                    required:
                    - name
                    type: object
                  mode:
                    default: cert-manager
                    enum:
                    - cert-manager
                    - disabled
                    type: string
                  renewBefore:
                    description: Certificate renewal before expiry
                    type: string
                  subject:
                    description: Additional certificate subject fields
                    properties:
                      countries:
                        items:
                          type: string
                        type: array
                      localities:
                        items:
                          type: string
                        type: array
                      organizationalUnits:
                        items:
                          type: string
                        type: array
                      organizations:
                        items:
                          type: string
                        type: array
                      provinces:
                        items:
                          type: string
                        type: array
                    type: object
                  trustedCASecret:
                    description: |-
                      TrustedCASecret references a Secret containing a trusted CA certificate (key: "ca.crt")
                      for verifying Neo4j TLS connections. When omitted with cert-manager mode, TLS verification
                      is skipped (suitable for self-signed development certificates).
                    type: string
                  usages:
                    description: Certificate usage settings
                    items:
                      type: string
                    type: array
                type: object
              tolerations:
                description: Tolerations for pod scheduling
                items:
                  description: |-
                    The pod this Toleration is attached to tolerates any taint that matches
                    the triple <key,value,effect> using the matching operator <operator>.
                  properties:
                    effect:
                      description: |-
                        Effect indicates the taint effect to match. Empty means match all taint effects.
                        When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                      type: string
                    key:
                      description: |-
                        Key is the taint key that the toleration applies to. Empty means match all taint keys.
                        If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                      type: string
                    operator:
                      description: |-
                        Operator represents a key's relationship to the value.
                        Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                        Exists is equivalent to wildcard for value, so that a pod can
                        tolerate all taints of a particular category.
                        Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                      type: string
                    tolerationSeconds:
                      description: |-
                        TolerationSeconds represents the period of time the toleration (which must be
                        of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                        it is not set, which means tolerate the taint forever (do not evict). Zero and
                        negative values will be treated as 0 (evict immediately) by the system.
                      format: int64
                      type: integer
                    value:
                      description: |-
                        Value is the taint value the toleration matches to.
                        If the operator is Exists, the value should be empty, otherwise just a regular string.
                      type: string
                  type: object
                type: array
              ui:
                description: UISpec defines Web UI configuration
                properties:
                  enabled:
                    default: false
                    type: boolean
                  ingress:
                    description: IngressSpec defines ingress configuration
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        type: object
                      className:
                        type: string
                      enabled:
                        type: boolean
                      host:
                        type: string
                      tlsSecretName:
                        type: string
                    type: object
                  resources:
                    description: Resource limits for UI pods
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                type: object
              upgradeStrategy:
                description: UpgradeStrategy specifies how to handle rolling upgrades
                properties:
                  autoPauseOnFailure:
                    default: true
                    description: AutoPauseOnFailure pauses upgrade on failure for
                      manual intervention
                    type: boolean
                  healthCheckTimeout:
                    default: 5m
                    description: HealthCheckTimeout specifies timeout for health checks
                    type: string
                  maxUnavailableDuringUpgrade:
                    default: 1
                    description: MaxUnavailableDuringUpgrade specifies max unavailable
                      replicas during upgrade
                    format: int32
                    type: integer
                  postUpgradeHealthCheck:
                    default: true
                    description: PostUpgradeHealthCheck enables cluster health validation
                      after upgrade
                    type: boolean
                  preUpgradeHealthCheck:
                    default: true
                    description: PreUpgradeHealthCheck enables cluster health validation
                      before upgrade
                    type: boolean
                  stabilizationTimeout:
                    default: 3m
                    description: StabilizationTimeout specifies how long to wait for
                      cluster stabilization
                    type: string
                  strategy:
                    default: RollingUpgrade
                    description: Strategy specifies the upgrade strategy
                    enum:
                    - RollingUpgrade
                    - Recreate
                    type: string
                  upgradeTimeout:
                    default: 30m
                    description: UpgradeTimeout specifies timeout for the entire upgrade
                      process
                    type: string
                type: object
            required:
            - image
            - storage
            type: object
          status:
            description: Neo4jEnterpriseStandaloneStatus defines the observed state
              of Neo4jEnterpriseStandalone
            properties:
              auraFleetManagement:
                description: AuraFleetManagementStatus reports the current state of
                  the Aura Fleet Management integration.
                properties:
                  lastRegistrationTime:
                    description: LastRegistrationTime records when the token was last
                      successfully registered.
                    format: date-time
                    type: string
                  message:
                    description: |-
                      Message provides additional information about the registration state,
                      including error details when registration fails.
                    type: string
                  registered:
                    description: |-
                      Registered is true once the deployment has successfully called
                      fleetManagement.registerToken and been acknowledged by Aura.
                    type: boolean
                required:
                - registered
                type: object
              conditions:
                description: Conditions represent the current state of the standalone
                  deployment
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              databaseStatus:
                description: DatabaseStatus provides information about the Neo4j database
                properties:
                  connectionCount:
                    description: ConnectionCount shows the number of active connections
                    format: int32
                    type: integer
                  databaseMode:
                    description: DatabaseMode shows the current database mode (should
                      be "SINGLE")
                    type: string
                  databaseName:
                    description: DatabaseName shows the active database name
                    type: string
                  healthStatus:
                    description: HealthStatus shows the current health status of the
                      database
                    type: string
                  lastBackupTime:
                    description: LastBackupTime shows when the last backup was completed
                    format: date-time
                    type: string
                  lastHealthCheck:
                    description: LastHealthCheck shows when the last health check
                      was performed
                    format: date-time
                    type: string
                  storageSize:
                    description: StorageSize shows the current storage usage
                    type: string
                type: object
              diagnostics:
                description: |-
                  Diagnostics holds the most recently collected live diagnostics from the standalone instance.
                  Populated when spec.monitoring.enabled=true and the standalone is Ready.
                properties:
                  collectionError:
                    description: |-
                      CollectionError holds the last error message if diagnostics collection failed.
                      Empty when collection succeeds.
                    type: string
                  databases:
                    description: Databases lists the most recently observed state
                      of each database.
                    items:
                      description: DatabaseDiagnosticInfo holds the observed state
                        of a single Neo4j database.
                      properties:
                        default:
                          description: Default indicates whether this is the default
                            database.
                          type: boolean
                        name:
                          description: Name is the database name.
                          type: string
                        requestedStatus:
                          description: RequestedStatus is the desired operational
                            status.
                          type: string
                        role:
                          description: Role is the database role on the most recently
                            contacted server (e.g. "primary", "secondary").
                          type: string
                        status:
                          description: Status is the current operational status (e.g.
                            "online", "offline", "quarantined").
                          type: string
                      required:
                      - name
                      - requestedStatus
                      - role
                      - status
                      type: object
                    type: array
                  lastCollected:
                    description: LastCollected is the timestamp of the most recent
                      successful diagnostics collection.
                    format: date-time
                    type: string
                  roleCount:
                    description: RoleCount is the total number of roles observed via
                      SHOW ROLES.
                    type: integer
                  roles:
                    description: |-
                      Roles lists the roles currently present in the standalone's `system`
                      database (from SHOW ROLES YIELD role, immutable).
                    items:
                      description: RoleDiagnosticInfo summarises one row of `SHOW
                        ROLES YIELD role, immutable`.
                      properties:
                        immutable:
                          description: |-
                            Immutable is true when the role was created with `CREATE IMMUTABLE ROLE`
                            or is one of the built-in immutable roles.
                          type: boolean
                        role:
                          description: Role is the role name.
                          type: string
                      required:
                      - role
                      type: object
                    type: array
                  userCount:
                    description: UserCount is the total number of users observed via
                      SHOW USERS.
                    type: integer
                  users:
                    description: |-
                      Users lists the users currently present in the standalone's `system`
                      database (from SHOW USERS). Useful for observing the effect of
                      Neo4jUser / Neo4jRoleBinding reconciliation.
                    items:
                      description: UserDiagnosticInfo summarises one row of `SHOW
                        USERS`.
                      properties:
                        homeDatabase:
                          description: HomeDatabase is the user's configured home
                            database, if any.
                          type: string
                        roles:
                          description: |-
                            Roles is the set of roles directly granted to the user (excluding the
                            implicit PUBLIC role).
                          items:
                            type: string
                          type: array
                        suspended:
                          description: Suspended is true when STATUS = SUSPENDED.
                          type: boolean
                        user:
                          description: User is the username.
                          type: string
                      required:
                      - user
                      type: object
                    type: array
                type: object
              endpoints:
                description: Endpoints provides connection information
                properties:
                  bolt:
                    description: Bolt protocol endpoint
                    type: string
                  connectionExamples:
                    description: Connection examples for external access
                    properties:
                      boltURI:
                        description: Driver connection examples
                        type: string
                      browserURL:
                        description: Browser URL examples
                        type: string
                      javaExample:
                        description: Java driver example
                        type: string
                      neo4jURI:
                        type: string
                      portForward:
                        description: Port forwarding command example
                        type: string
                      pythonExample:
                        description: Python driver example
                        type: string
                    type: object
                  http:
                    description: HTTP endpoint
                    type: string
                  https:
                    description: HTTPS endpoint
                    type: string
                  internal:
                    description: Internal service endpoints
                    properties:
                      client:
                        type: string
                      headless:
                        type: string
                    type: object
                type: object
              lastStartTime:
                description: LastStartTime shows when the standalone deployment was
                  last started
                format: date-time
                type: string
              message:
                description: Message provides additional information about the current
                  state
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation most recently
                  observed by the controller
                format: int64
                type: integer
              phase:
                description: Phase represents the current phase of the standalone
                  deployment
                type: string
              podStatus:
                description: PodStatus provides information about the Neo4j pod
                properties:
                  conditions:
                    description: Conditions represent the current conditions of the
                      pod
                    items:
                      description: PodCondition contains details for the current condition
                        of this pod.
                      properties:
                        lastProbeTime:
                          description: Last time we probed the condition.
                          format: date-time
                          type: string
                        lastTransitionTime:
                          description: Last time the condition transitioned from one
                            status to another.
                          format: date-time
                          type: string
                        message:
                          description: Human-readable message indicating details about
                            last transition.
                          type: string
                        observedGeneration:
                          description: |-
                            If set, this represents the .metadata.generation that the pod condition was set based upon.
                            The PodObservedGenerationTracking feature gate must be enabled to use this field.
                          format: int64
                          type: integer
                        reason:
                          description: Unique, one-word, CamelCase reason for the
                            condition's last transition.
                          type: string
                        status:
                          description: |-
                            Status is the status of the condition.
                            Can be True, False, Unknown.
                            More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#pod-conditions
                          type: string
                        type:
                          description: |-
                            Type is the type of the condition.
                            More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#pod-conditions
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  nodeName:
                    description: NodeName is the name of the Kubernetes node hosting
                      the pod
                    type: string
                  phase:
                    description: Phase is the current phase of the pod
                    type: string
                  podIP:
                    description: PodIP is the IP address of the Neo4j pod
                    type: string
                  podName:
                    description: PodName is the name of the Neo4j pod
                    type: string
                  restartCount:
                    description: RestartCount is the number of times the pod has been
                      restarted
                    format: int32
                    type: integer
                  startTime:
                    description: StartTime is when the pod was started
                    format: date-time
                    type: string
                type: object
              ready:
                description: Ready indicates if the standalone deployment is ready
                  for connections
                type: boolean
              version:
                description: Version shows the current Neo4j version
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
