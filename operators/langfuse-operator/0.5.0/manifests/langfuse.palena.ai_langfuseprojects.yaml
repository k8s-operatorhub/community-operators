apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  creationTimestamp: null
  name: langfuseprojects.langfuse.palena.ai
spec:
  group: langfuse.palena.ai
  names:
    kind: LangfuseProject
    listKind: LangfuseProjectList
    plural: langfuseprojects
    singular: langfuseproject
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .status.projectId
      name: Project ID
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LangfuseProject is the Schema for the langfuseprojects API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LangfuseProjectSpec defines the desired state of LangfuseProject.
            properties:
              apiKeys:
                description: APIKeys defines API keys to create and manage.
                items:
                  description: APIKeySpec defines an API key to create in Langfuse
                    and store in a K8s Secret.
                  properties:
                    name:
                      description: Name is the logical name of the API key.
                      type: string
                    secretName:
                      description: SecretName is the K8s Secret name to store the
                        key pair.
                      type: string
                  required:
                  - name
                  - secretName
                  type: object
                type: array
              instanceRef:
                description: InstanceRef references the parent LangfuseInstance.
                properties:
                  name:
                    description: Name of the referenced resource.
                    type: string
                  namespace:
                    description: Namespace of the referenced resource. Defaults to
                      the referencing resource's namespace.
                    type: string
                required:
                - name
                type: object
              organizationRef:
                description: OrganizationRef references the parent LangfuseOrganization
                  CR.
                properties:
                  name:
                    description: Name of the referenced resource.
                    type: string
                  namespace:
                    description: Namespace of the referenced resource. Defaults to
                      the referencing resource's namespace.
                    type: string
                required:
                - name
                type: object
              projectName:
                description: ProjectName is the project name in Langfuse.
                type: string
            required:
            - instanceRef
            - organizationRef
            - projectName
            type: object
          status:
            description: LangfuseProjectStatus defines the observed state of LangfuseProject.
            properties:
              apiKeys:
                description: APIKeys reports the status of each managed API key.
                items:
                  description: APIKeyStatus reports the status of a single managed
                    API key.
                  properties:
                    created:
                      description: Created indicates if the API key has been created.
                      type: boolean
                    lastRotated:
                      description: LastRotated is the last time the API key was rotated.
                      format: date-time
                      type: string
                    name:
                      description: Name is the logical name of the API key.
                      type: string
                    secretName:
                      description: SecretName is the K8s Secret storing the key pair.
                      type: string
                  required:
                  - created
                  - name
                  - secretName
                  type: object
                type: array
              conditions:
                description: Conditions represent the latest observations of the project's
                  state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              organizationId:
                description: OrganizationID is the Langfuse internal organization
                  ID.
                type: string
              projectId:
                description: ProjectID is the Langfuse internal project ID.
                type: string
              ready:
                description: Ready indicates whether the project is fully synced.
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
