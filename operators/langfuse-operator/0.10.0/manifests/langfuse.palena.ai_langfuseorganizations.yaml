apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  creationTimestamp: null
  name: langfuseorganizations.langfuse.palena.ai
spec:
  group: langfuse.palena.ai
  names:
    kind: LangfuseOrganization
    listKind: LangfuseOrganizationList
    plural: langfuseorganizations
    singular: langfuseorganization
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .status.organizationId
      name: Org ID
      type: string
    - jsonPath: .status.memberCount
      name: Members
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LangfuseOrganization is the Schema for the langfuseorganizations
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LangfuseOrganizationSpec defines the desired state of LangfuseOrganization.
            properties:
              displayName:
                description: DisplayName is the organization name in Langfuse.
                type: string
              instanceRef:
                description: InstanceRef references the parent LangfuseInstance.
                properties:
                  name:
                    description: Name of the referenced resource.
                    type: string
                  namespace:
                    description: Namespace of the referenced resource. Defaults to
                      the referencing resource's namespace.
                    type: string
                required:
                - name
                type: object
              members:
                description: Members configures organization membership.
                properties:
                  managedExclusively:
                    description: ManagedExclusively when true removes users not in
                      this list.
                    type: boolean
                  users:
                    description: Users is the list of organization members.
                    items:
                      description: OrganizationMemberSpec defines a single organization
                        member.
                      properties:
                        email:
                          description: Email is the user's email address.
                          type: string
                        role:
                          description: Role is the user's role in the organization.
                          enum:
                          - owner
                          - admin
                          - member
                          - viewer
                          type: string
                      required:
                      - email
                      - role
                      type: object
                    type: array
                type: object
            required:
            - displayName
            - instanceRef
            type: object
          status:
            description: LangfuseOrganizationStatus defines the observed state of
              LangfuseOrganization.
            properties:
              conditions:
                description: Conditions represent the latest observations of the organization's
                  state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              memberCount:
                description: MemberCount is the total number of members.
                type: integer
              organizationId:
                description: OrganizationID is the Langfuse internal organization
                  ID.
                type: string
              projectCount:
                description: ProjectCount is the number of LangfuseProject CRs referencing
                  this org.
                type: integer
              ready:
                description: Ready indicates whether the organization is fully synced.
                type: boolean
              syncedMembers:
                description: SyncedMembers is the number of confirmed synced members.
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
