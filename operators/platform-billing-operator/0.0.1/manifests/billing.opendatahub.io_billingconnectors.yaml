apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  creationTimestamp: null
  name: billingconnectors.billing.opendatahub.io
spec:
  group: billing.opendatahub.io
  names:
    kind: BillingConnector
    listKind: BillingConnectorList
    plural: billingconnectors
    singular: billingconnector
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .status.connected
      name: Connected
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BillingConnector is the Schema for the billingconnectors API.
          It configures integration with a payment service provider (Stripe, generic
          webhook, etc.) for the psp-connector binary.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BillingConnectorSpec defines the desired state of a BillingConnector.
            properties:
              credentialsSecretRef:
                description: |-
                  CredentialsSecretRef references a Secret containing PSP credentials.
                  For Stripe: keys "secretKey", "webhookSecret".
                  For webhook: key "hmacSecret".
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              sync:
                description: Sync configures polling intervals for usage and invoice
                  sync.
                properties:
                  invoiceInterval:
                    default: 1h
                    description: InvoiceInterval is the interval for syncing invoice
                      state with the PSP.
                    type: string
                  usageRecordsInterval:
                    default: 5m
                    description: UsageRecordsInterval is the interval for pushing
                      usage records to the PSP.
                    type: string
                type: object
              taxMode:
                default: none
                description: TaxMode controls whether tax computation is delegated
                  to the PSP.
                enum:
                - psp
                - none
                type: string
              type:
                description: Type is the payment service provider type.
                enum:
                - stripe
                - webhook
                type: string
              webhookIngress:
                description: |-
                  WebhookIngress configures the publicly reachable endpoint for PSP
                  webhook delivery.
                properties:
                  host:
                    description: Host is the publicly reachable hostname for webhook
                      delivery.
                    type: string
                  tlsSecretRef:
                    description: |-
                      TLSSecretRef references a Secret containing TLS certificate and key
                      for the webhook endpoint.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                required:
                - host
                type: object
            required:
            - credentialsSecretRef
            - type
            type: object
          status:
            description: BillingConnectorStatus defines the observed state of a BillingConnector.
            properties:
              conditions:
                description: |-
                  Conditions represent the latest available observations of the
                  BillingConnector's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              connected:
                description: |-
                  Connected indicates whether the connector has successfully authenticated
                  with the PSP.
                type: boolean
              lastSync:
                description: LastSync records the result of the most recent sync operation.
                properties:
                  at:
                    description: At is the timestamp of the last sync.
                    format: date-time
                    type: string
                  failed:
                    description: Failed is the number of records that failed to push.
                    format: int64
                    type: integer
                  pushedRecords:
                    description: PushedRecords is the number of records pushed in
                      the last sync.
                    format: int64
                    type: integer
                type: object
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              webhookStats:
                description: WebhookStats records webhook delivery statistics.
                properties:
                  received24h:
                    description: Received24h is the number of webhooks received in
                      the last 24 hours.
                    format: int64
                    type: integer
                  signatureFailures:
                    description: |-
                      SignatureFailures is the number of webhook signature verification failures
                      in the last 24 hours.
                    format: int64
                    type: integer
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
