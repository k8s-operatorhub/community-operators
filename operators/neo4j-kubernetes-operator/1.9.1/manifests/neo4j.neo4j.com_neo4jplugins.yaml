apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  creationTimestamp: null
  name: neo4jplugins.neo4j.neo4j.com
spec:
  group: neo4j.neo4j.com
  names:
    kind: Neo4jPlugin
    listKind: Neo4jPluginList
    plural: neo4jplugins
    singular: neo4jplugin
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.name
      name: Plugin
      type: string
    - jsonPath: .spec.version
      name: Version
      type: string
    - jsonPath: .spec.clusterRef
      name: Cluster
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: |-
          Neo4jPlugin declaratively installs and manages a Neo4j plugin
          (APOC, Graph Data Science, Bloom, GenAI, n10s, GraphQL, or a custom
          plugin) on a referenced cluster or standalone. The controller
          merges the plugin name into the StatefulSet's NEO4J_PLUGINS env
          var (without overwriting plugins added by other controllers),
          applies plugin-specific configuration, and triggers a controlled
          rolling restart. Multiple Neo4jPlugin resources targeting the same
          deployment coexist and stack additively.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Neo4jPluginSpec defines the desired state of Neo4jPlugin
            properties:
              clusterRef:
                description: Target cluster reference
                type: string
              config:
                additionalProperties:
                  type: string
                description: Plugin configuration
                type: object
              dependencies:
                description: Dependencies
                items:
                  description: PluginDependency defines a plugin dependency
                  properties:
                    name:
                      description: Dependency name
                      type: string
                    optional:
                      description: Whether dependency is optional
                      type: boolean
                    versionConstraint:
                      description: Version constraint
                      type: string
                  required:
                  - name
                  type: object
                type: array
              enabled:
                default: true
                description: Enable the plugin
                type: boolean
              name:
                description: Plugin name
                type: string
              resources:
                description: Resource requirements for the plugin
                properties:
                  cpuLimit:
                    description: CPU limit for the plugin
                    type: string
                  memoryLimit:
                    description: Memory limit for the plugin
                    type: string
                  threadPoolSize:
                    description: Thread pool size
                    format: int32
                    type: integer
                type: object
              security:
                description: Security configuration
                properties:
                  allowedProcedures:
                    description: Allowed procedures
                    items:
                      type: string
                    type: array
                  deniedProcedures:
                    description: Denied procedures
                    items:
                      type: string
                    type: array
                  sandbox:
                    description: Sandbox mode
                    type: boolean
                  securityPolicy:
                    description: Security policy
                    type: string
                type: object
              source:
                description: Plugin source configuration
                properties:
                  authSecret:
                    description: Secret containing authentication for private repositories
                    type: string
                  checksum:
                    description: Checksum for verification
                    type: string
                  registry:
                    description: Custom registry configuration
                    properties:
                      authSecret:
                        description: Authentication secret
                        type: string
                      tls:
                        description: TLS configuration
                        properties:
                          caSecret:
                            description: CA certificate secret
                            type: string
                          insecureSkipVerify:
                            description: Skip TLS verification
                            type: boolean
                        type: object
                      url:
                        description: Registry URL
                        type: string
                    required:
                    - url
                    type: object
                  type:
                    default: official
                    description: Source type
                    enum:
                    - official
                    - community
                    - custom
                    - url
                    type: string
                  url:
                    description: URL for custom plugins
                    type: string
                type: object
              version:
                description: Plugin version
                type: string
            required:
            - clusterRef
            - name
            - version
            type: object
          status:
            description: Neo4jPluginStatus defines the observed state of Neo4jPlugin
            properties:
              conditions:
                description: Conditions represent the current state of the plugin
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              health:
                description: Plugin health status
                properties:
                  errors:
                    description: Error messages
                    items:
                      type: string
                    type: array
                  lastHealthCheck:
                    description: Last health check
                    format: date-time
                    type: string
                  performance:
                    description: Performance metrics
                    properties:
                      avgExecutionTime:
                        description: Average execution time
                        type: string
                      cpuUsage:
                        description: CPU usage
                        type: string
                      executionCount:
                        description: Execution count
                        format: int64
                        type: integer
                      memoryUsage:
                        description: Memory usage
                        type: string
                    type: object
                  status:
                    description: Plugin status
                    type: string
                type: object
              installationTime:
                description: Installation time
                format: date-time
                type: string
              installedVersion:
                description: Installed version
                type: string
              message:
                description: Message provides additional information
                type: string
              observedGeneration:
                description: Observed generation
                format: int64
                type: integer
              phase:
                description: Phase represents the current phase
                type: string
              usage:
                description: Usage statistics
                properties:
                  lastUsed:
                    description: Last used time
                    format: date-time
                    type: string
                  proceduresCalled:
                    additionalProperties:
                      format: int64
                      type: integer
                    description: Procedures called
                    type: object
                  usageFrequency:
                    description: Usage frequency
                    type: string
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
