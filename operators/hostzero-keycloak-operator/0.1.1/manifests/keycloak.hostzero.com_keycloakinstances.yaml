---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: keycloakinstances.keycloak.hostzero.com
spec:
  group: keycloak.hostzero.com
  names:
    categories:
    - keycloak
    - all
    kind: KeycloakInstance
    listKind: KeycloakInstanceList
    plural: keycloakinstances
    shortNames:
    - kci
    singular: keycloakinstance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Whether the instance is ready
      jsonPath: .status.ready
      name: Ready
      type: boolean
    - description: The base URL of the Keycloak instance
      jsonPath: .spec.baseUrl
      name: URL
      type: string
    - description: Keycloak server version
      jsonPath: .status.version
      name: Version
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: KeycloakInstance makes a Keycloak server known to the operator
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KeycloakInstanceSpec defines the desired state of KeycloakInstance
            properties:
              baseUrl:
                description: BaseUrl is the URL of the Keycloak server (e.g., http://keycloak:8080)
                type: string
              client:
                description: Client contains optional service account client configuration
                properties:
                  id:
                    description: ID is the client ID for service account authentication
                    type: string
                  secret:
                    description: Secret is the client secret (optional for public
                      clients)
                    type: string
                required:
                - id
                type: object
              credentials:
                description: Credentials contains the reference to the admin credentials
                  secret
                properties:
                  secretRef:
                    description: SecretRef contains the reference to the secret with
                      credentials
                    properties:
                      name:
                        description: Name is the name of the secret
                        type: string
                      namespace:
                        description: Namespace is the namespace of the secret (defaults
                          to resource namespace)
                        type: string
                      passwordKey:
                        default: password
                        description: PasswordKey is the key in the secret for the
                          password (defaults to "password")
                        type: string
                      usernameKey:
                        default: username
                        description: UsernameKey is the key in the secret for the
                          username (defaults to "username")
                        type: string
                    required:
                    - name
                    type: object
                required:
                - secretRef
                type: object
              realm:
                description: Realm is the admin realm (defaults to "master")
                type: string
              token:
                description: Token contains optional token caching configuration
                properties:
                  expiresKey:
                    description: ExpiresKey is the key in the secret for the token
                      expiration
                    type: string
                  secretName:
                    description: SecretName is the name of the secret to cache the
                      token
                    type: string
                  tokenKey:
                    description: TokenKey is the key in the secret for the token
                    type: string
                type: object
            required:
            - baseUrl
            - credentials
            type: object
          status:
            description: KeycloakInstanceStatus defines the observed state of KeycloakInstance
            properties:
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              message:
                description: Message contains additional information about the status
                type: string
              ready:
                description: Ready indicates if the Keycloak instance is accessible
                type: boolean
              resourcePath:
                description: ResourcePath is the API path for this resource
                type: string
              status:
                description: Status is a human-readable status message
                type: string
              version:
                description: Version is the Keycloak server version
                type: string
            required:
            - ready
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
