apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  creationTimestamp: null
  name: microvms.lambdamicrovms.services.k8s.aws
spec:
  group: lambdamicrovms.services.k8s.aws
  names:
    kind: Microvm
    listKind: MicrovmList
    plural: microvms
    singular: microvm
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.microvmID
      name: MICROVM-ID
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Microvm is the Schema for the Microvms API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MicrovmSpec defines the desired state of Microvm.
            properties:
              egressNetworkConnectors:
                description: The list of egress network connectors to configure for
                  the MicroVM.
                items:
                  type: string
                type: array
              executionRoleARN:
                description: |-
                  The ARN of the IAM role to be assumed by the MicroVM during execution.

                  Regex Pattern: `^arn:aws[a-z\-]*:iam::[0-9]{12}:role/?[a-zA-Z_0-9+=,.@\-_/]+$`
                type: string
              executionRoleRef:
                description: "AWSResourceReferenceWrapper provides a wrapper around
                  *AWSResourceReference\ntype to provide more user friendly syntax
                  for references using 'from' field\nEx:\nAPIIDRef:\n\n\tfrom:\n\t
                  \ name: my-api"
                properties:
                  from:
                    description: |-
                      AWSResourceReference provides all the values necessary to reference another
                      k8s resource for finding the identifier(Id/ARN/Name)
                    properties:
                      name:
                        type: string
                      namespace:
                        type: string
                    type: object
                type: object
              idlePolicy:
                description: Configuration to control auto-suspend and auto-resume
                  behavior.
                properties:
                  autoResumeEnabled:
                    type: boolean
                  maxIdleDurationSeconds:
                    format: int64
                    type: integer
                  suspendedDurationSeconds:
                    format: int64
                    type: integer
                type: object
              imageIdentifier:
                description: The identifier (ARN or ID) of the MicroVM image to run.
                type: string
              imageIdentifierRef:
                description: "AWSResourceReferenceWrapper provides a wrapper around
                  *AWSResourceReference\ntype to provide more user friendly syntax
                  for references using 'from' field\nEx:\nAPIIDRef:\n\n\tfrom:\n\t
                  \ name: my-api"
                properties:
                  from:
                    description: |-
                      AWSResourceReference provides all the values necessary to reference another
                      k8s resource for finding the identifier(Id/ARN/Name)
                    properties:
                      name:
                        type: string
                      namespace:
                        type: string
                    type: object
                type: object
              imageVersion:
                description: |-
                  The version of the MicroVM image to run.

                  Regex Pattern: `^[^\s]+$`
                type: string
              ingressNetworkConnectors:
                description: The list of ingress network connectors to configure for
                  the MicroVM.
                items:
                  type: string
                type: array
              logging:
                description: |-
                  The logging configuration for this MicroVM instance. Specify {"cloudWatch":
                  {"logGroup": "..."}} to stream application logs to a custom CloudWatch log
                  group, or {"disabled": {}} to turn off logging.
                properties:
                  cloudWatch:
                    description: Configuration for Amazon CloudWatch Logs logging.
                    properties:
                      logGroup:
                        type: string
                      logStream:
                        type: string
                    type: object
                  disabled:
                    additionalProperties:
                      type: string
                    description: Specifies that logging is disabled for the MicroVM.
                    type: object
                type: object
              maximumDurationInSeconds:
                description: |-
                  The maximum duration in seconds that the MicroVM can exist before being terminated
                  by the platform. Valid range: 1–28,800 (8 hours).
                format: int64
                type: integer
              runHookPayload:
                description: |-
                  Per-MicroVM initialization data delivered as the request body of the /run
                  lifecycle hook. Use to pass tenant-specific configuration such as session
                  IDs or secret references. Maximum: 16,384 bytes.
                properties:
                  key:
                    description: Key is the key within the secret
                    type: string
                  name:
                    description: name is unique within a namespace to reference a
                      secret resource.
                    type: string
                  namespace:
                    description: namespace defines the space within which the secret
                      name must be unique.
                    type: string
                required:
                - key
                type: object
                x-kubernetes-map-type: atomic
            type: object
          status:
            description: MicrovmStatus defines the observed state of Microvm
            properties:
              ackResourceMetadata:
                description: |-
                  All CRs managed by ACK have a common `Status.ACKResourceMetadata` member
                  that is used to contain resource sync state, account ownership,
                  constructed ARN for the resource
                properties:
                  arn:
                    description: |-
                      ARN is the Amazon Resource Name for the resource. This is a
                      globally-unique identifier and is set only by the ACK service controller
                      once the controller has orchestrated the creation of the resource OR
                      when it has verified that an "adopted" resource (a resource where the
                      ARN annotation was set by the Kubernetes user on the CR) exists and
                      matches the supplied CR's Spec field values.
                      https://github.com/aws/aws-controllers-k8s/issues/270
                    type: string
                  ownerAccountID:
                    description: |-
                      OwnerAccountID is the AWS Account ID of the account that owns the
                      backend AWS service API resource.
                    type: string
                  partition:
                    description: Partition is the AWS partition in which the resource
                      exists or will exist
                    type: string
                  region:
                    description: Region is the AWS region in which the resource exists
                      or will exist.
                    type: string
                required:
                - ownerAccountID
                - region
                type: object
              conditions:
                description: |-
                  All CRs managed by ACK have a common `Status.Conditions` member that
                  contains a collection of `ackv1alpha1.Condition` objects that describe
                  the various terminal states of the CR and its backend AWS service API
                  resource
                items:
                  description: |-
                    Condition is the common struct used by all CRDs managed by ACK service
                    controllers to indicate terminal states  of the CR and its backend AWS
                    service API resource
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      format: date-time
                      type: string
                    message:
                      description: A human readable message indicating details about
                        the transition.
                      type: string
                    reason:
                      description: The reason for the condition's last transition.
                      type: string
                    status:
                      description: Status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the Condition
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              endpoint:
                description: |-
                  The HTTPS endpoint URL for communicating with the MicroVM. Include a valid
                  authentication token in the X-aws-proxy-auth header when sending requests.
                type: string
              imageARN:
                description: The ARN of the MicroVM image used to run this MicroVM.
                type: string
              microvmID:
                description: The unique identifier of the MicroVM.
                type: string
              startedAt:
                description: The timestamp when the MicroVM first started.
                format: date-time
                type: string
              state:
                description: The current lifecycle state of the MicroVM.
                type: string
              stateReason:
                description: |-
                  The reason for why the MicroVM is in the current state.

                  Regex Pattern: `^[^\s]+$`
                type: string
              terminatedAt:
                description: The timestamp when the MicroVM terminated.
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
