apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  creationTimestamp: null
  name: browserprofiles.bedrockagentcorecontrol.services.k8s.aws
spec:
  group: bedrockagentcorecontrol.services.k8s.aws
  names:
    kind: BrowserProfile
    listKind: BrowserProfileList
    plural: browserprofiles
    singular: browserprofile
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BrowserProfile is the Schema for the BrowserProfiles API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BrowserProfileSpec defines the desired state of BrowserProfile.
            properties:
              description:
                description: |-
                  A description of the browser profile. Use this field to describe the purpose
                  or contents of the profile.
                type: string
                x-kubernetes-validations:
                - message: Value is immutable once set
                  rule: self == oldSelf
              name:
                description: |-
                  The name of the browser profile. The name must be unique within your account
                  and can contain alphanumeric characters and underscores.

                  Regex Pattern: `^[a-zA-Z][a-zA-Z0-9_]{0,47}$`
                type: string
                x-kubernetes-validations:
                - message: Value is immutable once set
                  rule: self == oldSelf
              tags:
                additionalProperties:
                  type: string
                description: |-
                  A map of tag keys and values to assign to the browser profile. Tags enable
                  you to categorize your resources in different ways, for example, by purpose,
                  owner, or environment.
                type: object
            required:
            - name
            type: object
          status:
            description: BrowserProfileStatus defines the observed state of BrowserProfile
            properties:
              ackResourceMetadata:
                description: |-
                  All CRs managed by ACK have a common `Status.ACKResourceMetadata` member
                  that is used to contain resource sync state, account ownership,
                  constructed ARN for the resource
                properties:
                  arn:
                    description: |-
                      ARN is the Amazon Resource Name for the resource. This is a
                      globally-unique identifier and is set only by the ACK service controller
                      once the controller has orchestrated the creation of the resource OR
                      when it has verified that an "adopted" resource (a resource where the
                      ARN annotation was set by the Kubernetes user on the CR) exists and
                      matches the supplied CR's Spec field values.
                      https://github.com/aws/aws-controllers-k8s/issues/270
                    type: string
                  ownerAccountID:
                    description: |-
                      OwnerAccountID is the AWS Account ID of the account that owns the
                      backend AWS service API resource.
                    type: string
                  partition:
                    description: Partition is the AWS partition in which the resource
                      exists or will exist
                    type: string
                  region:
                    description: Region is the AWS region in which the resource exists
                      or will exist.
                    type: string
                required:
                - ownerAccountID
                - region
                type: object
              conditions:
                description: |-
                  All CRs managed by ACK have a common `Status.Conditions` member that
                  contains a collection of `ackv1alpha1.Condition` objects that describe
                  the various terminal states of the CR and its backend AWS service API
                  resource
                items:
                  description: |-
                    Condition is the common struct used by all CRDs managed by ACK service
                    controllers to indicate terminal states  of the CR and its backend AWS
                    service API resource
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      format: date-time
                      type: string
                    message:
                      description: A human readable message indicating details about
                        the transition.
                      type: string
                    reason:
                      description: The reason for the condition's last transition.
                      type: string
                    status:
                      description: Status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the Condition
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: The timestamp when the browser profile was created.
                format: date-time
                type: string
              id:
                description: |-
                  The unique identifier of the created browser profile.

                  Regex Pattern: `^[a-zA-Z][a-zA-Z0-9_]{0,47}-[a-zA-Z0-9]{10}$`
                type: string
              lastSavedAt:
                description: The timestamp when browser session data was last saved
                  to this profile.
                format: date-time
                type: string
              lastSavedBrowserID:
                description: |-
                  The identifier of the browser from which data was last saved to this profile.

                  Regex Pattern: `^(aws\.browser\.v1|[a-zA-Z][a-zA-Z0-9_]{0,47}-[a-zA-Z0-9]{10})$`
                type: string
              lastSavedBrowserSessionID:
                description: |-
                  The identifier of the browser session from which data was last saved to this
                  profile.

                  Regex Pattern: `^[0-9a-zA-Z]{1,40}$`
                type: string
              lastUpdatedAt:
                description: The timestamp when the browser profile was last updated.
                format: date-time
                type: string
              status:
                description: The current status of the browser profile.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
