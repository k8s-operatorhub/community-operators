apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  creationTimestamp: null
  name: litellmcredentials.litellm.palena.ai
spec:
  group: litellm.palena.ai
  names:
    kind: LiteLLMCredential
    listKind: LiteLLMCredentialList
    plural: litellmcredentials
    shortNames:
    - lc
    singular: litellmcredential
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.credentialName
      name: Credential
      type: string
    - jsonPath: .spec.instanceRef.name
      name: Instance
      type: string
    - jsonPath: .status.configured
      name: Configured
      type: boolean
    - jsonPath: .status.referencedByModels
      name: Models
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LiteLLMCredential is the Schema for the litellmcredentials API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              LiteLLMCredentialSpec defines the desired state of LiteLLMCredential.

              A LiteLLMCredential represents a reusable provider credential written to
              the proxy's `credential_list` config section. Multiple LiteLLMModel CRs
              can reference the same credential by name instead of each embedding its
              own API key, which reduces Secret sprawl and simplifies key rotation.
            properties:
              apiBase:
                description: API base URL for the provider (e.g., https://api.openai.com/v1).
                type: string
              apiKeySecretRef:
                description: Reference to a Secret containing the provider API key.
                properties:
                  key:
                    description: Key within the Secret.
                    type: string
                  name:
                    description: Name of the Secret.
                    type: string
                required:
                - key
                - name
                type: object
              apiVersion:
                description: API version (e.g., "2024-02-01" for Azure OpenAI).
                type: string
              credentialName:
                description: |-
                  Unique name for this credential, referenced by models via
                  litellm_params.litellm_credential_name.
                minLength: 1
                type: string
              instanceRef:
                description: Reference to the LiteLLMInstance this credential belongs
                  to.
                properties:
                  name:
                    description: Name of the LiteLLMInstance CR.
                    type: string
                required:
                - name
                type: object
              params:
                additionalProperties:
                  type: string
                description: |-
                  Additional provider-specific parameters written verbatim into
                  credential_info alongside api_key / api_base / api_version.
                type: object
            required:
            - apiKeySecretRef
            - credentialName
            - instanceRef
            type: object
          status:
            description: LiteLLMCredentialStatus defines the observed state of LiteLLMCredential.
            properties:
              conditions:
                description: Standard conditions.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              configured:
                description: Whether the credential is configured in the referenced
                  instance's proxy config.
                type: boolean
              lastSyncTime:
                description: Last time the credential config was validated and reconciled.
                format: date-time
                type: string
              referencedByModels:
                description: Number of LiteLLMModel CRs referencing this credential
                  via credentialRef.
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
