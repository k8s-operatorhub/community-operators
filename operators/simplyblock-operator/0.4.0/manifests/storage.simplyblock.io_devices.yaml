apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  creationTimestamp: null
  name: devices.storage.simplyblock.io
spec:
  group: storage.simplyblock.io
  names:
    kind: Device
    listKind: DeviceList
    plural: devices
    singular: device
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Device is the Schema for the devices API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Device
            properties:
              action:
                description: Action triggers an imperative device operation.
                enum:
                - remove
                - restart
                type: string
              clusterName:
                description: ClusterName is the target storage cluster name.
                type: string
              deviceID:
                description: DeviceID is the backend device identifier used for actions.
                type: string
              nodeUUID:
                description: NodeUUID scopes operations to a single storage node when
                  set.
                type: string
            required:
            - clusterName
            type: object
          status:
            description: status defines the observed state of Device
            properties:
              actionStatus:
                description: ActionStatus tracks the lifecycle of the latest device
                  action.
                properties:
                  action:
                    description: Action is the requested action name.
                    type: string
                  message:
                    description: Message is a human-readable action result or error.
                    type: string
                  nodeUUID:
                    description: NodeUUID is the target node UUID for the action.
                    type: string
                  observedGeneration:
                    description: ObservedGeneration is the resource generation observed
                      by this status.
                    format: int64
                    type: integer
                  state:
                    type: string
                  triggered:
                    description: Triggered indicates whether the underlying backend
                      action has been fired.
                    type: boolean
                  updatedAt:
                    description: UpdatedAt is the timestamp of the last status transition.
                    format: date-time
                    type: string
                type: object
              nodes:
                description: Nodes contains observed devices grouped by storage node.
                items:
                  properties:
                    devices:
                      description: Devices is the observed device inventory for the
                        node.
                      items:
                        properties:
                          health:
                            description: Health is the backend health indicator for
                              the device.
                            type: string
                          model:
                            description: Model is the reported device model.
                            type: string
                          size:
                            description: Size is the formatted device capacity value.
                            type: string
                          stats:
                            description: Stats is the time-series/statistics collection
                              for the device.
                            items:
                              properties:
                                iops:
                                  description: |-
                                    IOPS contains read/write IOPS values.
                                    FIXME: Unused for now
                                  properties:
                                    read:
                                      description: |-
                                        Read is the read IOPS metric.
                                        FIXME: Unused for now
                                      format: int64
                                      type: integer
                                    write:
                                      description: |-
                                        Write is the write IOPS metric.
                                        FIXME: Unused for now
                                      format: int64
                                      type: integer
                                  type: object
                                throughput:
                                  description: |-
                                    Throughput contains read/write throughput values.
                                    FIXME: Unused for now
                                  properties:
                                    read:
                                      description: |-
                                        Read is the read throughput metric.
                                        FIXME: Unused for now
                                      format: int64
                                      type: integer
                                    write:
                                      description: |-
                                        Write is the write throughput metric.
                                        FIXME: Unused for now
                                      format: int64
                                      type: integer
                                  type: object
                                utilizedCapacity:
                                  description: |-
                                    UtilizedCapacity is the used-capacity metric for the device.
                                    FIXME: Unused for now
                                  format: int64
                                  type: integer
                              type: object
                            type: array
                          status:
                            description: Status is the backend lifecycle status of
                              the device.
                            type: string
                          utilization:
                            description: |-
                              Utilization is the backend utilization metric.
                              FIXME: Unused for now
                            format: int64
                            type: integer
                          uuid:
                            description: UUID is the backend device UUID.
                            type: string
                        type: object
                      type: array
                    nodeUUID:
                      description: NodeUUID is the backend node UUID owning the listed
                        devices.
                      type: string
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
        x-kubernetes-validations:
        - message: nodeUUID and deviceID are required when action is specified
          rule: '!(has(self.spec.action) && self.spec.action != "" && ((!has(self.spec.nodeUUID)
            || self.spec.nodeUUID == "") || (!has(self.spec.deviceID) || self.spec.deviceID
            == "")))'
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
