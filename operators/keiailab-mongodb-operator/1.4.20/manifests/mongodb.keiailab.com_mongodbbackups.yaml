apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  creationTimestamp: null
  name: mongodbbackups.mongodb.keiailab.com
spec:
  group: mongodb.keiailab.com
  names:
    kind: MongoDBBackup
    listKind: MongoDBBackupList
    plural: mongodbbackups
    shortNames:
    - mdbbackup
    singular: mongodbbackup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterRef.name
      name: Cluster
      type: string
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.size
      name: Size
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MongoDBBackup is the Schema for the mongodbbackups API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MongoDBBackupSpec defines the desired state of MongoDBBackup
            properties:
              clusterRef:
                description: ClusterRef references the MongoDB or MongoDBSharded cluster
                properties:
                  kind:
                    description: Kind is the cluster kind (MongoDB or MongoDBSharded)
                    enum:
                    - MongoDB
                    - MongoDBSharded
                    type: string
                  name:
                    description: Name is the cluster name
                    type: string
                required:
                - kind
                - name
                type: object
              compression:
                default: true
                description: Compression enables backup compression
                type: boolean
              compressionType:
                default: zstd
                description: CompressionType defines compression algorithm
                enum:
                - gzip
                - zstd
                - snappy
                type: string
              storage:
                description: Storage defines backup storage location
                properties:
                  pvc:
                    description: PVC defines PVC-based storage
                    properties:
                      size:
                        anyOf:
                        - type: integer
                        - type: string
                        description: Size is the PVC size
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      storageClassName:
                        description: StorageClassName is the storage class for backup
                          PVC
                        type: string
                    required:
                    - size
                    type: object
                  s3:
                    description: S3 defines S3-compatible storage (including Ceph
                      ObjectStore)
                    properties:
                      bucket:
                        description: Bucket is the S3 bucket name
                        type: string
                      credentialsRef:
                        description: CredentialsRef references the S3 credentials
                          secret
                        properties:
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                      endpoint:
                        description: Endpoint is the S3 endpoint URL
                        type: string
                      insecureSkipTLS:
                        default: false
                        description: InsecureSkipTLS skips TLS verification
                        type: boolean
                      prefix:
                        description: Prefix is the key prefix for backups
                        type: string
                      region:
                        description: Region is the S3 region
                        type: string
                    required:
                    - bucket
                    - credentialsRef
                    type: object
                  type:
                    description: Type is the storage type
                    enum:
                    - s3
                    - pvc
                    type: string
                required:
                - type
                type: object
              type:
                default: full
                description: Type is the backup type
                enum:
                - full
                - incremental
                type: string
            required:
            - clusterRef
            - storage
            type: object
          status:
            description: MongoDBBackupStatus defines the observed state of MongoDBBackup
            properties:
              completionTime:
                description: CompletionTime is when the backup completed
                format: date-time
                type: string
              conditions:
                description: Conditions represents the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              error:
                description: Error contains error message if failed
                type: string
              location:
                description: Location is the backup location
                type: string
              phase:
                description: Phase represents the current backup phase
                enum:
                - Pending
                - Running
                - Completed
                - Failed
                type: string
              size:
                description: Size is the backup size
                type: string
              startTime:
                description: StartTime is when the backup started
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
