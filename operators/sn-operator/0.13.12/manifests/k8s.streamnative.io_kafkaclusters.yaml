apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  creationTimestamp: null
  name: kafkaclusters.k8s.streamnative.io
spec:
  group: k8s.streamnative.io
  names:
    kind: KafkaCluster
    listKind: KafkaClusterList
    plural: kafkaclusters
    shortNames:
    - kc
    - kafkac
    singular: kafkacluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.brokerCount
      name: Brokers
      type: integer
    - jsonPath: .status.readyBrokerCount
      name: Ready
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          KafkaCluster is the Schema for the kafkaclusters API.
          It decorates existing Strimzi Kafka clusters with Istio ingress gateway networking.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KafkaClusterSpec defines the desired state of KafkaCluster
            properties:
              advertisedListeners:
                description: |-
                  AdvertisedListeners defines the access points for the cluster.
                  Each entry creates the necessary ingress infrastructure (e.g. Istio Gateway)
                  and configures the broker to advertise that address.
                items:
                  description: AdvertisedListener defines how brokers are advertised
                    to clients via Istio
                  properties:
                    authentication:
                      description: Authentication defines the authentication mechanism
                        for this listener.
                      properties:
                        apiKey:
                          description: ApiKey configures the API Key authentication.
                          properties:
                            oidcIssuers:
                              description: OIDCIssuers OpenID Connect configuration
                                for API keys
                              items:
                                properties:
                                  adminScope:
                                    default: admin
                                    description: AdminScope Scope to operate as a
                                      Pulsar superuser, by default admin.
                                    type: string
                                  audience:
                                    description: Audience The expected audience for
                                      the OIDC token. This field is required.
                                    type: string
                                  authzAdminRole:
                                    description: AuthzAdminRole Authz Role to operate
                                      as a Pulsar superuser.
                                    type: string
                                  authzRoleClaim:
                                    description: AuthzRoleClaim JWT claim to use for
                                      Authz roles.
                                    type: string
                                  issuerUrl:
                                    description: IssuerUrl of the provider which allows
                                      Pulsar to discover public signing keys. Required.
                                    type: string
                                  publicKeyFilePath:
                                    description: |-
                                      PublicKeyFilePath Optional path to a local JWKS file.
                                      If set, the public key will be read from this file instead of fetching from the issuer's JWKS endpoint.
                                      This is useful for offline deployments or testing.
                                    type: string
                                  publicKeySecretRef:
                                    description: |-
                                      PublicKeySecretRef Optional secret reference to a local JWKS file.
                                      If set, the operator will mount the secret to the broker pod and set the PublicKeyFilePath to the mount path.
                                    properties:
                                      key:
                                        description: The key of the secret to select
                                          from.  Must be a valid secret key.
                                        type: string
                                      name:
                                        description: Name of the referent.
                                        type: string
                                    required:
                                    - name
                                    type: object
                                  requiredScope:
                                    description: RequiredScope to require in a JWT
                                      token used for authentication, optional.
                                    type: string
                                  scopeClaim:
                                    default: scope
                                    description: ScopeClaim JWT claim to use as the
                                      scope claim, by default scope.
                                    type: string
                                  subjectClaim:
                                    default: sub
                                    description: SubjectClaim JWT claim to use as
                                      the user name, by default sub.
                                    type: string
                                required:
                                - audience
                                - issuerUrl
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - issuerUrl
                              x-kubernetes-list-type: map
                            revocationListLoadIntervalInSecs:
                              description: RevocationListLoadIntervalInSecs Interval
                                to load the revocation list from api keys service
                              format: int32
                              type: integer
                            revocationListSecretRef:
                              description: |-
                                RevocationListSecretRef Optional secret reference to a local revocation list file.
                                If set, the operator will mount the secret to the broker pod and set the revocationListFile path
                              properties:
                                key:
                                  description: The key of the secret to select from.  Must
                                    be a valid secret key.
                                  type: string
                                name:
                                  description: Name of the referent.
                                  type: string
                              required:
                              - name
                              type: object
                            revocationListUrl:
                              description: RevocationListUrl URL to fetch the revocation
                                list from api keys service
                              type: string
                            skipRevocationListValidationOnRevocationListInitFailure:
                              description: SkipRevocationListValidationOnRevocationListInitFailure
                                Skip validation of revocation list if the revocation
                                list
                              type: boolean
                          type: object
                        custom:
                          description: |-
                            Custom allows passing raw Strimzi authentication configuration.
                            Use this for advanced scenarios not covered by structured fields.
                            Required when type is "custom".
                          type: object
                          x-kubernetes-preserve-unknown-fields: true
                        type:
                          default: plain
                          description: Type of authentication (PLAIN, SCRAM-SHA-512,
                            TLS, OAUTH, APIKEY, CUSTOM)
                          enum:
                          - plain
                          - scram-sha-512
                          - tls
                          - oauth
                          - apikey
                          - custom
                          type: string
                      type: object
                    containerPort:
                      default: 9092
                      description: 'ContainerPort is the port on the Kafka container
                        (default: 9092).'
                      format: int32
                      maximum: 65535
                      minimum: 1
                      type: integer
                    hostTemplate:
                      description: |-
                        HostTemplate defines the advertised hostname format for this listener.
                        Supports placeholder $(POD_ID) for per-broker addresses.
                        Examples: "broker-$(POD_ID).kafka.example.com", "my-kafka.domain.com"
                      type: string
                    istioGateway:
                      description: |-
                        IstioGateway configures the Istio Gateway and VirtualService for exposing this listener.
                        If this field is defined, the listener will be exposed through an Istio Ingress Gateway.
                      properties:
                        advertisedPort:
                          description: AdvertisedPort is the external port on the
                            Istio Gateway (e.g. 443).
                          format: int32
                          maximum: 65535
                          minimum: 1
                          type: integer
                        clusterAddress:
                          description: |-
                            ClusterAddress is a single, static address for the entire Kafka cluster
                            which clients can use for bootstrap. This is typically used when the
                            Kafka clients are configured with a single address for the cluster,
                            and the Istio VirtualService handles routing to individual brokers.
                            If set, the Istio Gateway will create a host for this address, and the
                            VirtualService will include this as a host that routes traffic.
                          type: string
                        selector:
                          additionalProperties:
                            type: string
                          description: |-
                            Selector defines the labels used to select the Istio Ingress Gateway workload.
                            e.g. {"istio": "ingressgateway"}
                          type: object
                        tls:
                          description: TLS configuration for the Gateway listener.
                          properties:
                            certSecretName:
                              description: |-
                                CertSecretName defines the name of the secret that contains the certificate
                                to use in Istio Ingress Gateway (SIMPLE mode).
                              type: string
                            mode:
                              description: Mode defines the TLS mode for the Gateway
                                (SIMPLE, PASSTHROUGH).
                              enum:
                              - SIMPLE
                              - PASSTHROUGH
                              type: string
                          type: object
                      required:
                      - advertisedPort
                      type: object
                    name:
                      description: |-
                        Name identifies this listener (e.g., "external", "internal").
                        The controller will create a Kafka listener with this name on the configured ContainerPort (default 9092).
                      type: string
                    protocol:
                      default: PLAINTEXT
                      description: Protocol defines the Kafka protocol for this listener.
                      enum:
                      - PLAINTEXT
                      - SSL
                      - SASL_SSL
                      - SASL_PLAINTEXT
                      type: string
                    tls:
                      description: TLS configures the server-side TLS for this listener.
                      properties:
                        certificate:
                          default: tls.crt
                          description: |-
                            Certificate is the key in the Secret containing the certificate data.
                            Defaults to "tls.crt".
                          type: string
                        key:
                          default: tls.key
                          description: |-
                            Key is the key in the Secret containing the private key.
                            Defaults to "tls.key".
                          type: string
                        secretName:
                          description: SecretName is the name of the Secret containing
                            the server certificate.
                          type: string
                      required:
                      - secretName
                      type: object
                  required:
                  - name
                  type: object
                maxItems: 10
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              config:
                description: Config defines the arbitrary Kafka configuration.
                properties:
                  custom:
                    additionalProperties:
                      type: string
                    description: |-
                      Custom allows passing raw Strimzi configuration.
                      Use this for advanced scenarios not covered by structured fields.
                    type: object
                type: object
              externalKafkaResourceRefs:
                description: |-
                  ExternalKafkaResourceRefs is a list of references to external Kafka resources to be managed.
                  It can reference Strimzi Kafka CRs, or other custom NodeGroup CRs.
                  The operator will aggregate networking logic for these resources.
                items:
                  description: |-
                    TypedLocalObjectReference contains enough information to let you locate the
                    typed referenced object inside the same namespace.
                  properties:
                    apiGroup:
                      description: |-
                        APIGroup is the group for the resource being referenced.
                        If APIGroup is not specified, the specified Kind must be in the core API group.
                        For any other third-party types, APIGroup is required.
                      type: string
                    kind:
                      description: Kind is the type of resource being referenced
                      type: string
                    name:
                      description: Name is the name of resource being referenced
                      type: string
                  required:
                  - kind
                  - name
                  type: object
                minItems: 1
                type: array
            required:
            - externalKafkaResourceRefs
            type: object
          status:
            description: KafkaClusterStatus defines the observed state of KafkaCluster
            properties:
              brokerCount:
                description: BrokerCount is the number of broker pods discovered
                format: int32
                type: integer
              conditions:
                description: Conditions is an array of current observed conditions.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              endpoints:
                description: Endpoints lists the service endpoints exposed via Istio
                items:
                  type: string
                type: array
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  for this cluster.
                format: int64
                type: integer
              readyBrokerCount:
                description: ReadyBrokerCount is the number of ready broker pods
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
