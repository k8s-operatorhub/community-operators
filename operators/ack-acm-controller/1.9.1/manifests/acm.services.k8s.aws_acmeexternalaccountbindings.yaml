apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  creationTimestamp: null
  name: acmeexternalaccountbindings.acm.services.k8s.aws
spec:
  group: acm.services.k8s.aws
  names:
    kind: AcmeExternalAccountBinding
    listKind: AcmeExternalAccountBindingList
    plural: acmeexternalaccountbindings
    singular: acmeexternalaccountbinding
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AcmeExternalAccountBinding is the Schema for the AcmeExternalAccountBindings
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              AcmeExternalAccountBindingSpec defines the desired state of AcmeExternalAccountBinding.

              Contains detailed information about an ACME external account binding.
            properties:
              acmeEndpointARN:
                description: |-
                  The ARN of the ACME endpoint to associate this external account binding with.

                  The Amazon Resource Name (ARN) of the ACME endpoint.

                  Regex Pattern: `^arn:aws[a-z-]*:acm:[a-z0-9-]+:[0-9]{12}:acme-endpoint/[a-zA-Z0-9-]+$`
                type: string
                x-kubernetes-validations:
                - message: Value is immutable once set
                  rule: self == oldSelf
              acmeEndpointRef:
                description: "AWSResourceReferenceWrapper provides a wrapper around
                  *AWSResourceReference\ntype to provide more user friendly syntax
                  for references using 'from' field\nEx:\nAPIIDRef:\n\n\tfrom:\n\t
                  \ name: my-api"
                properties:
                  from:
                    description: |-
                      AWSResourceReference provides all the values necessary to reference another
                      k8s resource for finding the identifier(Id/ARN/Name)
                    properties:
                      name:
                        type: string
                      namespace:
                        type: string
                    type: object
                type: object
              credentialsOutput:
                description: |-
                  The Kubernetes Secret where EAB credentials are written. Specify the namespace,
                  name, and key. The sensitive macKey is written under the given key; the keyId is
                  written under a fixed keyId key, unless key is itself keyId, in which case the
                  macKey wins and the identifier is available in status.keyID. If namespace is
                  omitted, the resource's namespace is used. The Secret must already exist; the
                  controller populates it.
                properties:
                  key:
                    description: Key is the key within the secret
                    type: string
                  name:
                    description: name is unique within a namespace to reference a
                      secret resource.
                    type: string
                  namespace:
                    description: namespace defines the space within which the secret
                      name must be unique.
                    type: string
                required:
                - key
                type: object
                x-kubernetes-map-type: atomic
                x-kubernetes-validations:
                - message: Value is immutable once set
                  rule: self == oldSelf
              expiration:
                description: The expiration configuration for the external account
                  binding.
                properties:
                  type:
                    type: string
                  value:
                    format: int64
                    type: integer
                type: object
                x-kubernetes-validations:
                - message: Value is immutable once set
                  rule: self == oldSelf
              roleARN:
                description: |-
                  The ARN of the IAM role associated with the external account binding.
                  The role's trust policy must grant acm-acme.amazonaws.com access to
                  sts:AssumeRole, sts:SetSourceIdentity, and sts:TagSession.

                  The Amazon Resource Name (ARN) of the IAM role to associate with the external
                  account binding.

                  Regex Pattern: `^arn:aws[a-z-]*:iam::[0-9]{12}:role/.+$`
                type: string
                x-kubernetes-validations:
                - message: Value is immutable once set
                  rule: self == oldSelf
              roleRef:
                description: "AWSResourceReferenceWrapper provides a wrapper around
                  *AWSResourceReference\ntype to provide more user friendly syntax
                  for references using 'from' field\nEx:\nAPIIDRef:\n\n\tfrom:\n\t
                  \ name: my-api"
                properties:
                  from:
                    description: |-
                      AWSResourceReference provides all the values necessary to reference another
                      k8s resource for finding the identifier(Id/ARN/Name)
                    properties:
                      name:
                        type: string
                      namespace:
                        type: string
                    type: object
                type: object
              tags:
                description: One or more tags to associate with the external account
                  binding.
                items:
                  description: A key-value pair that identifies or specifies metadata
                    about an ACM resource.
                  properties:
                    key:
                      type: string
                    value:
                      type: string
                  type: object
                type: array
            type: object
          status:
            description: AcmeExternalAccountBindingStatus defines the observed state
              of AcmeExternalAccountBinding
            properties:
              ackResourceMetadata:
                description: |-
                  All CRs managed by ACK have a common `Status.ACKResourceMetadata` member
                  that is used to contain resource sync state, account ownership,
                  constructed ARN for the resource
                properties:
                  arn:
                    description: |-
                      ARN is the Amazon Resource Name for the resource. This is a
                      globally-unique identifier and is set only by the ACK service controller
                      once the controller has orchestrated the creation of the resource OR
                      when it has verified that an "adopted" resource (a resource where the
                      ARN annotation was set by the Kubernetes user on the CR) exists and
                      matches the supplied CR's Spec field values.
                      https://github.com/aws/aws-controllers-k8s/issues/270
                    type: string
                  ownerAccountID:
                    description: |-
                      OwnerAccountID is the AWS Account ID of the account that owns the
                      backend AWS service API resource.
                    type: string
                  partition:
                    description: Partition is the AWS partition in which the resource
                      exists or will exist
                    type: string
                  region:
                    description: Region is the AWS region in which the resource exists
                      or will exist.
                    type: string
                required:
                - ownerAccountID
                - region
                type: object
              conditions:
                description: |-
                  All CRs managed by ACK have a common `Status.Conditions` member that
                  contains a collection of `ackv1alpha1.Condition` objects that describe
                  the various terminal states of the CR and its backend AWS service API
                  resource
                items:
                  description: |-
                    Condition is the common struct used by all CRDs managed by ACK service
                    controllers to indicate terminal states  of the CR and its backend AWS
                    service API resource
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      format: date-time
                      type: string
                    message:
                      description: A human readable message indicating details about
                        the transition.
                      type: string
                    reason:
                      description: The reason for the condition's last transition.
                      type: string
                    status:
                      description: Status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the Condition
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: The time at which the external account binding was created.
                format: date-time
                type: string
              expiresAt:
                description: The time at which the external account binding expires.
                format: date-time
                type: string
              keyID:
                description: |-
                  The external account binding key identifier. This value is not sensitive and
                  pairs with the macKey stored in the credentials Secret. It is surfaced in status
                  so ACME clients can reference it directly when registering an account.
                type: string
              lastUsedAt:
                description: The time at which the external account binding was last
                  used.
                format: date-time
                type: string
              revokedAt:
                description: The time at which the external account binding was revoked.
                format: date-time
                type: string
              updatedAt:
                description: The time at which the external account binding was last
                  updated.
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
