---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: operationrulesets.ifo.kubevirt.io
spec:
  group: ifo.kubevirt.io
  names:
    kind: OperationRuleSet
    listKind: OperationRuleSetList
    plural: operationrulesets
    shortNames:
    - ors
    singular: operationruleset
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OperationRuleSet is the Schema for the OperationRuleSets API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of OperationRuleSet
            properties:
              component:
                description: Component is the name of the component this rule belongs
                  to, e.g. KubeVirt or Forklift.
                type: string
              labelExpressions:
                description: LabelExpressions are CEL expressions evaluated against
                  the resource to dynamically assign labels to IFOs.
                items:
                  description: LabelExpression defines a CEL expression that produces
                    a label value
                  properties:
                    expression:
                      description: Expression is a CEL expression that returns a string
                        value
                      type: string
                    key:
                      description: Key is the label key to set on the InFlightOperation
                      type: string
                  required:
                  - expression
                  - key
                  type: object
                type: array
              labels:
                additionalProperties:
                  type: string
                description: Labels to attach to all IFOs created by this ruleset.
                type: object
              namespaces:
                description: |-
                  Namespaces determines which namespaces to watch
                  If empty, watches all namespaces
                items:
                  type: string
                type: array
              rules:
                description: Rules contains the CEL expressions to evaluate
                items:
                  description: Rule represents a single CEL evaluation rule
                  properties:
                    expression:
                      description: Expression is the CEL expression to evaluate against
                        the resource
                      type: string
                    operation:
                      description: Operation is the name of the operation (e.g., "Migrating",
                        "RestartRequired")
                      type: string
                  required:
                  - expression
                  - operation
                  type: object
                minItems: 1
                type: array
              target:
                description: Target specifies which Kubernetes resource type to watch
                properties:
                  group:
                    description: |-
                      Group is the API group of the resource
                      Empty string for core resources
                    type: string
                  resource:
                    description: Resource is the plural name of the resource
                    type: string
                  version:
                    description: Version is the API version of the resource
                    type: string
                required:
                - group
                - resource
                - version
                type: object
            required:
            - rules
            - target
            type: object
          status:
            description: status defines the observed state of OperationRuleSet
            properties:
              conditions:
                description: List of conditions.
                items:
                  description: Condition
                  properties:
                    category:
                      description: The condition category.
                      type: string
                    durable:
                      description: The condition is durable - never un-staged.
                      type: boolean
                    items:
                      description: A list of items referenced in the `Message`.
                      items:
                        type: string
                      type: array
                    lastTransitionTime:
                      description: When the last status transition occurred.
                      format: date-time
                      type: string
                    message:
                      description: The human readable description of the condition.
                      type: string
                    reason:
                      description: The reason for the condition or transition.
                      type: string
                    status:
                      description: The condition status [true,false].
                      type: string
                    type:
                      description: The condition type.
                      type: string
                  required:
                  - category
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                type: array
              lastEvaluationTime:
                description: LastEvaluationTime is the timestamp of the last rule
                  evaluation
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation most recently
                  observed by the controller
                format: int64
                type: integer
              watchActive:
                description: WatchActive indicates whether the dynamic watch is currently
                  active
                type: boolean
            required:
            - watchActive
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
