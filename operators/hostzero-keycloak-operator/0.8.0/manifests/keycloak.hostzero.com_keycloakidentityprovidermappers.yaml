apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  creationTimestamp: null
  name: keycloakidentityprovidermappers.keycloak.hostzero.com
spec:
  group: keycloak.hostzero.com
  names:
    categories:
    - keycloak
    - all
    kind: KeycloakIdentityProviderMapper
    listKind: KeycloakIdentityProviderMapperList
    plural: keycloakidentityprovidermappers
    shortNames:
    - kcidpm
    singular: keycloakidentityprovidermapper
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Whether the identity provider mapper is ready
      jsonPath: .status.ready
      name: Ready
      type: boolean
    - description: Mapper name
      jsonPath: .status.mapperName
      name: Mapper
      type: string
    - description: Parent identity provider alias
      jsonPath: .status.identityProviderAlias
      name: IdP
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: KeycloakIdentityProviderMapper defines a mapper attached to a
          KeycloakIdentityProvider
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KeycloakIdentityProviderMapperSpec defines the desired state
              of KeycloakIdentityProviderMapper
            properties:
              definition:
                description: |-
                  Definition contains the Keycloak IdentityProviderMapperRepresentation.
                  The identityProviderAlias field is auto-injected from the parent
                  KeycloakIdentityProvider at reconcile time and does not need to be set
                  here.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              identityProviderRef:
                description: |-
                  IdentityProviderRef is a reference to a KeycloakIdentityProvider that owns
                  this mapper. The realm and Keycloak instance are derived from the parent
                  identity provider.
                properties:
                  name:
                    description: Name of the resource
                    type: string
                  namespace:
                    description: Namespace of the resource (optional, defaults to
                      the same namespace)
                    type: string
                required:
                - name
                type: object
            required:
            - definition
            - identityProviderRef
            type: object
          status:
            description: KeycloakIdentityProviderMapperStatus defines the observed
              state of KeycloakIdentityProviderMapper
            properties:
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identityProviderAlias:
                description: IdentityProviderAlias is the alias of the parent identity
                  provider
                type: string
              instance:
                description: Instance contains the resolved instance reference
                properties:
                  clusterInstanceRef:
                    description: ClusterInstanceRef is the name of the cluster instance
                    type: string
                  instanceRef:
                    description: InstanceRef is the name of the namespaced instance
                    type: string
                type: object
              mapperID:
                description: MapperID is the Keycloak internal mapper ID
                type: string
              mapperName:
                description: MapperName is the mapper name in Keycloak
                type: string
              message:
                description: Message contains additional information
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the spec that
                  was last processed
                format: int64
                type: integer
              ready:
                description: Ready indicates if the identity provider mapper is ready
                type: boolean
              realm:
                description: Realm contains the resolved realm reference
                properties:
                  clusterRealmRef:
                    description: ClusterRealmRef is the name of the cluster realm
                    type: string
                  realmRef:
                    description: RealmRef is the name of the namespaced realm
                    type: string
                type: object
              resourcePath:
                description: ResourcePath is the Keycloak API path for this identity
                  provider mapper
                type: string
              status:
                description: Status is a human-readable status message
                type: string
            required:
            - ready
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
