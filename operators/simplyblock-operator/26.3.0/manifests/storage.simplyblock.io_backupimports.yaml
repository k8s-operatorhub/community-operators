apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  creationTimestamp: null
  name: backupimports.storage.simplyblock.io
spec:
  group: storage.simplyblock.io
  names:
    kind: BackupImport
    listKind: BackupImportList
    plural: backupimports
    shortNames:
    - bi
    singular: backupimport
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.sourceClusterName
      name: Source
      type: string
    - jsonPath: .spec.targetClusterName
      name: Target
      type: string
    - jsonPath: .status.storageBackupRef
      name: BackupRef
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BackupImport imports a completed backup from a source cluster into a target cluster,
          creating a StorageBackup CR that can be referenced by a BackupRestore.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of BackupImport
            properties:
              sourceBackupID:
                description: SourceBackupID is the UUID of the backup on the source
                  cluster to import.
                pattern: ^[a-zA-Z0-9_-]{1,128}$
                type: string
              sourceClusterName:
                description: SourceClusterName is the StorageCluster CR name of the
                  cluster that owns the backup.
                type: string
                x-kubernetes-validations:
                - message: field is immutable
                  rule: self == oldSelf
              targetClusterName:
                description: TargetClusterName is the StorageCluster CR name of the
                  cluster to import into.
                type: string
                x-kubernetes-validations:
                - message: field is immutable
                  rule: self == oldSelf
            required:
            - sourceBackupID
            - sourceClusterName
            - targetClusterName
            type: object
          status:
            description: status defines the observed state of BackupImport
            properties:
              completedAt:
                description: CompletedAt is when the import completed.
                format: date-time
                type: string
              importedBackupID:
                description: ImportedBackupID is the backup UUID after successful
                  import into the target cluster.
                type: string
              message:
                description: Message contains the latest reconciliation detail or
                  error.
                type: string
              phase:
                description: Phase is the high-level lifecycle shown in kubectl output.
                type: string
              sourceClusterUUID:
                description: SourceClusterUUID is the resolved UUID of the source
                  cluster.
                type: string
              storageBackupRef:
                description: |-
                  StorageBackupRef is the name of the StorageBackup CR created in the target namespace
                  after a successful import. This CR can be referenced directly in a BackupRestore.
                type: string
              targetClusterUUID:
                description: TargetClusterUUID is the resolved UUID of the target
                  cluster.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
