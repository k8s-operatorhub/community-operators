apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  creationTimestamp: null
  name: lvols.storage.simplyblock.io
spec:
  group: storage.simplyblock.io
  names:
    kind: Lvol
    listKind: LvolList
    plural: lvols
    singular: lvol
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.lvols.length()
      name: LVOLs
      type: integer
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Lvol is the Schema for the lvols API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Lvol
            properties:
              clusterName:
                description: ClusterName is the target storage cluster name.
                type: string
              poolName:
                description: PoolName is the target storage pool name.
                type: string
            required:
            - clusterName
            - poolName
            type: object
          status:
            description: status defines the observed state of Lvol
            properties:
              configured:
                description: Configured indicates whether initial Lvol reconciliation
                  has completed.
                type: boolean
              lvols:
                description: Lvols contains observed logical volume entries.
                items:
                  properties:
                    blobID:
                      description: BlobID is the backend blob identifier.
                      format: int64
                      type: integer
                    clonedFromSnapshot:
                      description: ClonedFromSnapshot is the source snapshot name/ID
                        for clones.
                      type: string
                    created:
                      description: |-
                        Created is the backend creation timestamp.
                        FIXME: Unused for now
                      format: date-time
                      type: string
                    encrypted:
                      description: IsEncrypted indicates whether encryption is enabled
                        for the volume.
                      type: boolean
                    erasureCodingScheme:
                      description: ErasureCodingScheme is the erasure coding layout,
                        for example "2x1".
                      type: string
                    fabricType:
                      description: FabricType is the storage fabric/protocol in use.
                      type: string
                    ha:
                      description: HA indicates whether high availability is enabled.
                      type: boolean
                    health:
                      description: Health indicates current health-check state.
                      type: boolean
                    hostname:
                      description: Hostname is the node hostname associated with the
                        volume.
                      type: string
                    lvolName:
                      description: LvolName is the logical volume name.
                      type: string
                    maxNamespacesPerSubsystem:
                      description: MaxNamespacesPerSubsystem is the max number of
                        namespaces per subsystem.
                      format: int64
                      type: integer
                    namespaceID:
                      description: NamespaceID is the NVMe namespace identifier.
                      format: int64
                      type: integer
                    nodeUUID:
                      description: NodeUUID is the set of node UUIDs associated with
                        the volume.
                      items:
                        type: string
                      type: array
                    nqn:
                      description: NQN is the NVMe Qualified Name for the volume.
                      type: string
                    poolName:
                      description: PoolName is the storage pool name.
                      type: string
                    poolUUID:
                      description: PoolUUID is the backend storage pool UUID.
                      type: string
                    pvcName:
                      description: PvcName is the bound Kubernetes PVC name when applicable.
                      type: string
                    qos:
                      description: QoS contains quality-of-service limits/metrics.
                      properties:
                        class:
                          description: Class is the QosSpec class identifier.
                          format: int64
                          type: integer
                        iops:
                          description: IOPS is the IOPS limit/metric.
                          format: int64
                          type: integer
                        throughput:
                          description: Throughput contains throughput limits/metrics.
                          properties:
                            read:
                              description: Read is the read throughput limit/metric.
                              format: int64
                              type: integer
                            readWrite:
                              description: ReadWrite is the combined read/write throughput
                                limit/metric.
                              format: int64
                              type: integer
                            write:
                              description: Write is the write throughput limit/metric.
                              format: int64
                              type: integer
                          type: object
                      type: object
                    size:
                      description: Size is the formatted volume size.
                      type: string
                    sourceSnapshotName:
                      description: SourceSnapshotName is the source snapshot name
                        used for this volume.
                      type: string
                    status:
                      description: Status is the backend lifecycle status.
                      type: string
                    subsysPort:
                      description: SubsysPort is the NVMe subsystem/listener port.
                      format: int64
                      type: integer
                    updated:
                      description: |-
                        Updated is the backend last-update timestamp.
                        FIXME: Unused for now
                      format: date-time
                      type: string
                    uuid:
                      description: UUID is the backend logical volume UUID.
                      type: string
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
