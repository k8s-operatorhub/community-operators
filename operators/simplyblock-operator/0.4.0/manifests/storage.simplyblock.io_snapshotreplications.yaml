apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  creationTimestamp: null
  name: snapshotreplications.storage.simplyblock.io
spec:
  group: storage.simplyblock.io
  names:
    kind: SnapshotReplication
    listKind: SnapshotReplicationList
    plural: snapshotreplications
    singular: snapshotreplication
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.configured
      name: Configured
      type: boolean
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SnapshotReplication is the Schema for the snapshotreplications
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of SnapshotReplication
            properties:
              action:
                enum:
                - failback
                type: string
              excludeVolumeIDs:
                description: 'Optional: volumes to exclude from failback.'
                items:
                  type: string
                type: array
              includeVolumeIDs:
                description: |-
                  Optional: only these volumes are included in failback.
                  If empty, all volumes are candidates unless excluded below.
                items:
                  type: string
                type: array
              interval:
                description: 'snapshot replication interval in seconds (default: 300sec)'
                format: int32
                type: integer
              sourceCluster:
                description: Source cluster for the snapshots
                type: string
              sourcePool:
                description: required for failback to a fresh source cluster
                type: string
              targetCluster:
                description: Target cluster for replication
                type: string
              targetPool:
                description: Target cluster pool for replication
                type: string
              timeout:
                description: snapshot replication timeout
                format: int32
                type: integer
              volumeIDs:
                description: 'Optional: list of volumes to replicate. Empty means
                  all volumes'
                items:
                  type: string
                type: array
            required:
            - sourceCluster
            - targetCluster
            - targetPool
            type: object
          status:
            description: status defines the observed state of SnapshotReplication
            properties:
              conditions:
                description: Conditions provides human-readable status conditions
                  for kubectl get output.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              configured:
                type: boolean
              observedFailbackGeneration:
                description: The metadata.generation value for which failback was
                  last processed.
                format: int64
                type: integer
              volumes:
                description: Per-volume replication status
                items:
                  description: VolumeReplicationStatus tracks the replication state
                    of an individual volume
                  properties:
                    errors:
                      description: 'Optional: list of errors encountered for this
                        volume'
                      items:
                        description: ReplicationError stores timestamped error messages
                        properties:
                          message:
                            type: string
                          timestamp:
                            format: date-time
                            type: string
                        required:
                        - message
                        - timestamp
                        type: object
                      type: array
                    lastReplicationTime:
                      description: Timestamp of the last successful replication for
                        this volume
                      format: date-time
                      type: string
                    lastSnapshotID:
                      description: Last snapshot ID replicated for this volume
                      type: string
                    phase:
                      description: Phase is the current replication phase for this
                        volume.
                      enum:
                      - Pending
                      - Running
                      - TriggeringTargetReplication
                      - WaitingForTargetReplication
                      - ReplicatingToSource
                      - WaitingForTargetDeletion
                      - Completed
                      - Failed
                      - Paused
                      type: string
                    replicatedCount:
                      description: Number of snapshots successfully replicated
                      format: int32
                      type: integer
                    volumeID:
                      description: Volume ID
                      type: string
                  required:
                  - volumeID
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
