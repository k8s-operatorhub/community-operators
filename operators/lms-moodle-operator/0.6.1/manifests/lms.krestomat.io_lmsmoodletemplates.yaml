apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  creationTimestamp: null
  name: lmsmoodletemplates.lms.krestomat.io
spec:
  group: lms.krestomat.io
  names:
    categories:
    - lms
    kind: LMSMoodleTemplate
    listKind: LMSMoodleTemplateList
    plural: lmsmoodletemplates
    shortNames:
    - lmt
    singular: lmsmoodletemplate
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - description: Age of the resource
      jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    - description: LMSMoodleTemplate status such as Unknown/Used/NotUsed/Terminating
        etc
      jsonPath: .status.state
      name: STATUS
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LMSMoodleTemplate is the Schema for the lmsmoodletemplates API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LMSMoodleTemplateSpec defines the desired state of LMSMoodleTemplate
            properties:
              keydbSpec:
                description: KeydbSpec defines Keydb spec to deploy optionally
                properties:
                  keydbAffinity:
                    description: KeydbAffinity defines any affinity rules for Keydb
                      pods.
                    type: string
                  keydbExtraConfig:
                    description: KeydbExtraConfig contains extra keydb config
                    type: string
                  keydbImage:
                    description: KeydbImage defines image for keydb container
                    maxLength: 255
                    type: string
                  keydbMode:
                    description: KeydbMode describes mode keydb runs
                    enum:
                    - standalone
                    - multimaster
                    - custom
                    type: string
                  keydbNetpolEgressIpblock:
                    description: KeydbNetpolEgressIpblock defines egress ip block
                      for keydb default network policy
                    type: string
                  keydbNetpolIngressIpblock:
                    description: GaneshaNetpolIngressIpblock defines ingress ip block
                      for keydb default network policy
                    type: string
                  keydbNetpolOmit:
                    description: 'KeydbNetpolOmit whether to omit default keydb network
                      policy. Default: true'
                    type: boolean
                  keydbNodeSelector:
                    description: KeydbNodeSelector defines any node labels selectors
                      for Keydb pods.
                    type: string
                  keydbPvcDataAutoexpansion:
                    description: KeydbPvcDataAutoexpansion enables autoexpansion
                    type: boolean
                  keydbPvcDataAutoexpansionCapGib:
                    description: KeydbPvcDataAutoexpansionCapGib defines limit for
                      autoexpansion increments
                    format: int32
                    type: integer
                  keydbPvcDataAutoexpansionIncrementGib:
                    description: KeydbPvcDataAutoexpansionIncrementGib defines Gib
                      to increment
                    format: int32
                    type: integer
                  keydbPvcDataSize:
                    description: KeydbPvcDataSize defines keydb storage size
                    maxLength: 20
                    minLength: 2
                    type: string
                  keydbPvcDataStorageAccessMode:
                    description: KeydbPvcDataStorageAccessMode defines keydb storage
                      access modes
                    enum:
                    - ReadWriteOnce
                    - ReadOnlyMany
                    - ReadWriteMany
                    type: string
                  keydbPvcDataStorageClassName:
                    description: KeydbPvcDataStorageClassName defines keydb storage
                      class
                    maxLength: 63
                    minLength: 2
                    type: string
                  keydbResourceLimits:
                    description: 'KeydbResourceLimits whether keydb resource limits
                      are added. Default: false'
                    type: boolean
                  keydbResourceLimitsCpu:
                    description: KeydbResourceLimitsCpu set keydb resource limits
                      cpu
                    maxLength: 20
                    type: string
                  keydbResourceLimitsMemory:
                    description: KeydbResourceLimitsMemory set keydb resource limits
                      memory
                    maxLength: 20
                    type: string
                  keydbResourceRequests:
                    description: 'KeydbResourceRequests whether keydb resource requests
                      are added. Default: true'
                    type: boolean
                  keydbResourceRequestsCpu:
                    description: KeydbResourceRequestsCpu set keydb resource requests
                      cpu
                    maxLength: 20
                    type: string
                  keydbResourceRequestsMemory:
                    description: KeydbResourceRequestsMemory set keydb resource requests
                      memory
                    maxLength: 20
                    type: string
                  keydbSize:
                    description: KeydbSize defines keydb number of replicas
                    format: int32
                    type: integer
                  keydbTolerations:
                    description: KeydbTolerations defines any tolerations for Keydb
                      pods.
                    items:
                      description: |-
                        The pod this Toleration is attached to tolerates any taint that matches
                        the triple <key,value,effect> using the matching operator <operator>.
                      properties:
                        effect:
                          description: |-
                            Effect indicates the taint effect to match. Empty means match all taint effects.
                            When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: |-
                            Key is the taint key that the toleration applies to. Empty means match all taint keys.
                            If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                          type: string
                        operator:
                          description: |-
                            Operator represents a key's relationship to the value.
                            Valid operators are Exists and Equal. Defaults to Equal.
                            Exists is equivalent to wildcard for value, so that a pod can
                            tolerate all taints of a particular category.
                          type: string
                        tolerationSeconds:
                          description: |-
                            TolerationSeconds represents the period of time the toleration (which must be
                            of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                            it is not set, which means tolerate the taint forever (do not evict). Zero and
                            negative values will be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: |-
                            Value is the taint value the toleration matches to.
                            If the operator is Exists, the value should be empty, otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                  keydbVpaSpec:
                    description: KeydbVpaSpec set keydb horizontal pod autoscaler
                      spec
                    type: string
                type: object
              moodleSpec:
                description: MoodleSpec defines Moodle spec
                properties:
                  moodleConfigAdditionalBlock:
                    description: MoodleConfigAdditionalBlock defines moodle extra
                      block in config.php
                    type: string
                  moodleConfigAdditionalCfg:
                    description: MoodleConfigAdditionalCfg defines moodle extra config
                      properties in config.php
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  moodleConfigSessionRedisCompressor:
                    description: MoodleConfigSessionRedisCompressor defines redis
                      session compresor
                    enum:
                    - none
                    - gzip
                    - zstd
                    type: string
                  moodleConfigSessionRedisPrefix:
                    description: 'MoodleConfigSessionRedisPrefix defines prefix for
                      redis session. Default: '''''
                    maxLength: 100
                    type: string
                  moodleConfigSessionRedisSerializerUseIgbinary:
                    description: 'MoodleConfigSessionRedisSerializerUseIgbinary whether
                      igbinary is used for redis session. Default: false'
                    type: boolean
                  moodleCronjobAffinity:
                    description: MoodleCronjobAffinity defines any affinity rules
                      for Moodle cronjob pods.
                    type: string
                  moodleCronjobNodeSelector:
                    description: MoodleCronjobNodeSelector defines any node labels
                      selectors for Moodle cronjob pods.
                    type: string
                  moodleCronjobResourceLimits:
                    description: 'MoodleCronjobResourceLimits whether moodle cronjob
                      resource limits are added. Default: false'
                    type: boolean
                  moodleCronjobResourceLimitsCpu:
                    description: MoodleCronjobResourceLimitsCpu set moodle cronjob
                      resource limits cpu
                    maxLength: 20
                    type: string
                  moodleCronjobResourceLimitsMemory:
                    description: MoodleCronjobResourceLimitsMemory set moodle cronjob
                      resource limits memory
                    maxLength: 20
                    type: string
                  moodleCronjobResourceRequests:
                    description: 'MoodleCronjobResourceRequests whether moodle cronjob
                      resource requests are added. Default: true'
                    type: boolean
                  moodleCronjobResourceRequestsCpu:
                    description: MoodleCronjobResourceRequestsCpu set moodle cronjob
                      resource requests cpu
                    maxLength: 20
                    type: string
                  moodleCronjobResourceRequestsMemory:
                    description: MoodleCronjobResourceRequestsMemory set moodle cronjob
                      resource requests memory
                    maxLength: 20
                    type: string
                  moodleCronjobTolerations:
                    description: MoodleCronjobTolerations defines any tolerations
                      for Moodle cronjob pods.
                    items:
                      description: |-
                        The pod this Toleration is attached to tolerates any taint that matches
                        the triple <key,value,effect> using the matching operator <operator>.
                      properties:
                        effect:
                          description: |-
                            Effect indicates the taint effect to match. Empty means match all taint effects.
                            When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: |-
                            Key is the taint key that the toleration applies to. Empty means match all taint keys.
                            If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                          type: string
                        operator:
                          description: |-
                            Operator represents a key's relationship to the value.
                            Valid operators are Exists and Equal. Defaults to Equal.
                            Exists is equivalent to wildcard for value, so that a pod can
                            tolerate all taints of a particular category.
                          type: string
                        tolerationSeconds:
                          description: |-
                            TolerationSeconds represents the period of time the toleration (which must be
                            of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                            it is not set, which means tolerate the taint forever (do not evict). Zero and
                            negative values will be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: |-
                            Value is the taint value the toleration matches to.
                            If the operator is Exists, the value should be empty, otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                  moodleCronjobVpaSpec:
                    description: MoodleCronjobVpaSpec set moodle cronjob vertical
                      pod autoscaler spec
                    type: string
                  moodleHealthcheckSubpath:
                    description: MoodleHealthcheckSubpath defines Moodle subpath for
                      nginx check
                    maxLength: 100
                    minLength: 2
                    type: string
                  moodleHost:
                    description: MoodleHost defines Moodle host for url
                    maxLength: 100
                    minLength: 2
                    type: string
                  moodleImage:
                    description: MoodleImage defines image for moodle container
                    maxLength: 255
                    type: string
                  moodleKeydbMetaName:
                    description: MoodleKeydbMetaName defines Keydb CR name to use
                      as redis cache.
                    maxLength: 63
                    type: string
                  moodleNetpolEgressIpblock:
                    description: MoodleNetpolEgressIpblock defines egress ip block
                      for moodle default network policy
                    type: string
                  moodleNetpolIngressIpblock:
                    description: MoodleNetpolIngressIpblock defines ingress ip block
                      for moodle default network policy
                    type: string
                  moodleNetpolOmit:
                    description: 'MoodleNetpolOmit whether to omit default moodle
                      network policy. Default: true'
                    type: boolean
                  moodleNewAdminpassHash:
                    description: MoodleNewAdminPassHash is the bcrypt compatible admin
                      password to set in new instance. Required
                    maxLength: 60
                    minLength: 60
                    pattern: ^\$2[ayb]\$.{56}$
                    type: string
                  moodleNewInstance:
                    description: MoodleNewInstance whether new instance job runs
                    type: boolean
                  moodleNewInstanceAdminmail:
                    description: MoodleNewInstanceAdminMail is the admin email to
                      set in new instance. Required
                    maxLength: 100
                    minLength: 3
                    type: string
                  moodleNewInstanceAdminuser:
                    maxLength: 100
                    minLength: 1
                    type: string
                  moodleNewInstanceAgreeLicense:
                    description: MoodleNewInstanceAgreeLicense whether agree to Moodle
                      license. Required
                    type: boolean
                  moodleNewInstanceFullname:
                    maxLength: 100
                    type: string
                  moodleNewInstanceJobAffinity:
                    description: MoodleNewInstanceJobAffinity defines any affinity
                      rules for Moodle cronjob pods.
                    type: string
                  moodleNewInstanceJobNodeSelector:
                    description: MoodleNewInstanceJobNodeSelector defines any node
                      labels selectors for Moodle cronjob pods.
                    type: string
                  moodleNewInstanceJobResourceLimits:
                    description: 'MoodleNewInstanceJobResourceLimits whether moodle
                      new instance job resource limits are added. Default: false'
                    type: boolean
                  moodleNewInstanceJobResourceLimitsCpu:
                    description: MoodleNewInstanceJobResourceLimitsCpu set moodle
                      new instance job resource limits cpu
                    maxLength: 20
                    type: string
                  moodleNewInstanceJobResourceLimitsMemory:
                    description: MoodleNewInstanceJobResourceLimitsMemory set moodle
                      new instance job resource limits memory
                    maxLength: 20
                    type: string
                  moodleNewInstanceJobResourceRequests:
                    description: 'MoodleNewInstanceJobResourceRequests whether moodle
                      new instance job resource requests are added. Default: true'
                    type: boolean
                  moodleNewInstanceJobResourceRequestsCpu:
                    description: MoodleNewInstanceJobResourceRequestsCpu set moodle
                      new instance job resource requests cpu
                    maxLength: 20
                    type: string
                  moodleNewInstanceJobResourceRequestsMemory:
                    description: MoodleNewInstanceJobResourceRequestsMemory set moodle
                      new instance job resource requests memory
                    maxLength: 20
                    type: string
                  moodleNewInstanceJobTolerations:
                    description: MoodleNewInstanceJobTolerations defines any tolerations
                      for Moodle cronjob pods.
                    items:
                      description: |-
                        The pod this Toleration is attached to tolerates any taint that matches
                        the triple <key,value,effect> using the matching operator <operator>.
                      properties:
                        effect:
                          description: |-
                            Effect indicates the taint effect to match. Empty means match all taint effects.
                            When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: |-
                            Key is the taint key that the toleration applies to. Empty means match all taint keys.
                            If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                          type: string
                        operator:
                          description: |-
                            Operator represents a key's relationship to the value.
                            Valid operators are Exists and Equal. Defaults to Equal.
                            Exists is equivalent to wildcard for value, so that a pod can
                            tolerate all taints of a particular category.
                          type: string
                        tolerationSeconds:
                          description: |-
                            TolerationSeconds represents the period of time the toleration (which must be
                            of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                            it is not set, which means tolerate the taint forever (do not evict). Zero and
                            negative values will be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: |-
                            Value is the taint value the toleration matches to.
                            If the operator is Exists, the value should be empty, otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                  moodleNewInstanceLang:
                    description: MoodleNewInstanceLang set moodle language code
                    maxLength: 15
                    minLength: 2
                    pattern: ^[a-z_]+$
                    type: string
                  moodleNewInstanceShortname:
                    maxLength: 100
                    type: string
                  moodleNewInstanceSummary:
                    maxLength: 300
                    type: string
                  moodleNfsMetaName:
                    description: MoodleNfsMetaName defines (NFS) Ganesha server CR
                      name to use as shared storage for moodledata.
                    maxLength: 63
                    type: string
                  moodlePort:
                    description: MoodlePort defines Moodle port for url
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                  moodlePostgresMetaName:
                    description: MoodlePostgresMetaName defines Postgres CR name to
                      use as database.
                    maxLength: 63
                    type: string
                  moodleProtocol:
                    description: MoodleProtocol whether to use http or https
                    enum:
                    - http
                    - https
                    type: string
                  moodlePvcDataSize:
                    description: MoodlePvcDataSize defines moodledata storage size
                    maxLength: 100
                    minLength: 2
                    type: string
                  moodlePvcDataStorageAccessMode:
                    description: MoodlePvcDataStorageAccessMode defines moodledata
                      storage access modes
                    enum:
                    - ReadWriteOnce
                    - ReadOnlyMany
                    - ReadWriteMany
                    type: string
                  moodlePvcDataStorageClassName:
                    description: MoodlePvcDataStorageClassName defines moodledata
                      storage class
                    maxLength: 63
                    minLength: 2
                    type: string
                  moodleRedisHost:
                    description: 'MoodleRedisHost defines redis host. Default: ''127.0.0.1'''
                    maxLength: 100
                    type: string
                  moodleRedisMucStore:
                    description: 'MoodleRedisMucStore whether redis is configured
                      as MUC store. Default: false'
                    type: boolean
                  moodleRedisMucStoreCompressor:
                    description: 'MoodleRedisMucStoreCompressor defines compressor
                      for redis MUC store. Default: 0'
                    maximum: 1
                    minimum: 0
                    type: integer
                  moodleRedisMucStorePrefix:
                    description: 'MoodleRedisMucStorePrefix defines prefix for redis
                      MUC store. Default: '''''
                    maxLength: 100
                    type: string
                  moodleRedisMucStoreSerializer:
                    description: 'MoodleRedisMucStoreSerializer defines serializer
                      for redis MUC store. Default: 1'
                    maximum: 2
                    minimum: 1
                    type: integer
                  moodleRedisSecret:
                    description: 'MoodleRedisSecret defines redis auth secret name.
                      Default: '''''
                    maxLength: 255
                    type: string
                  moodleRedisSecretAuthKey:
                    description: 'MoodleRedisSecretAuthKey defines key inside auth
                      secret name. Default: ''keydb_password'''
                    maxLength: 100
                    type: string
                  moodleRedisSessionStore:
                    description: 'MoodleRedisSessionStore whether redis is configured
                      as session store. Default: false'
                    type: boolean
                  moodleSubpath:
                    description: MoodleSubpath defines Moodle subpath for url
                    maxLength: 100
                    minLength: 2
                    type: string
                  moodleUpdateJobAffinity:
                    description: MoodleUpdateJobAffinity defines any affinity rules
                      for Moodle cronjob pods.
                    type: string
                  moodleUpdateJobNodeSelector:
                    description: MoodleUpdateJobNodeSelector defines any node labels
                      selectors for Moodle cronjob pods.
                    type: string
                  moodleUpdateJobResourceLimits:
                    description: 'MoodleUpdateJobResourceLimits whether moodle update
                      job resource limits are added. Default: false'
                    type: boolean
                  moodleUpdateJobResourceLimitsCpu:
                    description: MoodleUpdateJobResourceLimitsCpu set moodle update
                      job resource limits cpu
                    maxLength: 20
                    type: string
                  moodleUpdateJobResourceLimitsMemory:
                    description: MoodleUpdateJobResourceLimitsMemory set moodle cronjob
                      resource limits memory
                    maxLength: 20
                    type: string
                  moodleUpdateJobResourceRequests:
                    description: 'MoodleUpdateJobResourceRequests whether moodle update
                      job resource requests are added. Default: true'
                    type: boolean
                  moodleUpdateJobResourceRequestsCpu:
                    description: MoodleUpdateJobResourceRequestsCpu set moodle update
                      job resource requests cpu
                    maxLength: 20
                    type: string
                  moodleUpdateJobResourceRequestsMemory:
                    description: MoodleUpdateJobResourceRequestsMemory set moodle
                      update job resource requests memory
                    maxLength: 20
                    type: string
                  moodleUpdateJobTolerations:
                    description: MoodleUpdateJobTolerations defines any tolerations
                      for Moodle cronjob pods.
                    items:
                      description: |-
                        The pod this Toleration is attached to tolerates any taint that matches
                        the triple <key,value,effect> using the matching operator <operator>.
                      properties:
                        effect:
                          description: |-
                            Effect indicates the taint effect to match. Empty means match all taint effects.
                            When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: |-
                            Key is the taint key that the toleration applies to. Empty means match all taint keys.
                            If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                          type: string
                        operator:
                          description: |-
                            Operator represents a key's relationship to the value.
                            Valid operators are Exists and Equal. Defaults to Equal.
                            Exists is equivalent to wildcard for value, so that a pod can
                            tolerate all taints of a particular category.
                          type: string
                        tolerationSeconds:
                          description: |-
                            TolerationSeconds represents the period of time the toleration (which must be
                            of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                            it is not set, which means tolerate the taint forever (do not evict). Zero and
                            negative values will be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: |-
                            Value is the taint value the toleration matches to.
                            If the operator is Exists, the value should be empty, otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                  moodleUpdateMajor:
                    description: 'MoodleUpdateMajor whether major updates are automatically
                      applied. Default: false'
                    type: boolean
                  moodleUpdateMinor:
                    description: 'MoodleUpdateMinor whether minor updates are automatically
                      applied. Default: true'
                    type: boolean
                  nginxAffinity:
                    description: NginxAffinity defines any affinity rules for Nginx
                      pods.
                    type: string
                  nginxExtraConfig:
                    description: NginxExtraConfig contains extra Nginx config
                    type: string
                  nginxHpaSpec:
                    description: NginxHpaSpec set nginx horizontal pod autoscaler
                      spec
                    type: string
                  nginxImage:
                    description: NginxImage defines image for nginx container
                    maxLength: 255
                    type: string
                  nginxIngressAnnotations:
                    description: NginxIngressAnnotations defines nginx annotations
                    type: string
                  nginxNetpolEgressIpblock:
                    description: NginxNetpolEgressIpblock defines egress ip block
                      for nginx default network policy
                    type: string
                  nginxNetpolIngressIpblock:
                    description: NginxNetpolIngressIpblock defines ingress ip block
                      for nginx default network policy
                    type: string
                  nginxNetpolOmit:
                    description: 'NginxNetpolOmit whether to omit default network
                      policy for nginx. Default: true'
                    type: boolean
                  nginxNodeSelector:
                    description: NginxNodeSelector defines any node labels selectors
                      for Nginx pods.
                    type: string
                  nginxResourceLimits:
                    description: 'NginxResourceLimits whether nginx resource limits
                      are added. Default: false'
                    type: boolean
                  nginxResourceLimitsCpu:
                    description: NginxResourceLimitsCpu set nginx resource limits
                      cpu
                    maxLength: 20
                    type: string
                  nginxResourceLimitsMemory:
                    description: NginxResourceLimitsMemory set nginx resource limits
                      memory
                    maxLength: 20
                    type: string
                  nginxResourceRequests:
                    description: 'NginxResourceRequests whether nginx resource requests
                      are added. Default: true'
                    type: boolean
                  nginxResourceRequestsCpu:
                    description: NginxResourceRequestsCpu set nginx resource requests
                      cpu
                    type: string
                  nginxResourceRequestsMemory:
                    description: NginxResourceRequestsMemory set nginx resource requests
                      memory
                    type: string
                  nginxSize:
                    description: NginxSize defines nginx number of replicas between
                      0 and 255
                    format: int32
                    maximum: 255
                    minimum: 0
                    type: integer
                  nginxTolerations:
                    description: NginxTolerations defines any tolerations for Nginx
                      pods.
                    items:
                      description: |-
                        The pod this Toleration is attached to tolerates any taint that matches
                        the triple <key,value,effect> using the matching operator <operator>.
                      properties:
                        effect:
                          description: |-
                            Effect indicates the taint effect to match. Empty means match all taint effects.
                            When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: |-
                            Key is the taint key that the toleration applies to. Empty means match all taint keys.
                            If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                          type: string
                        operator:
                          description: |-
                            Operator represents a key's relationship to the value.
                            Valid operators are Exists and Equal. Defaults to Equal.
                            Exists is equivalent to wildcard for value, so that a pod can
                            tolerate all taints of a particular category.
                          type: string
                        tolerationSeconds:
                          description: |-
                            TolerationSeconds represents the period of time the toleration (which must be
                            of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                            it is not set, which means tolerate the taint forever (do not evict). Zero and
                            negative values will be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: |-
                            Value is the taint value the toleration matches to.
                            If the operator is Exists, the value should be empty, otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                  nginxVpaSpec:
                    description: NginxVpaSpec set nginx vertical pod autoscaler spec
                    type: string
                  phpFpmAffinity:
                    description: PhpFpmAffinity defines any affinity rules for PhpFpm
                      pods.
                    type: string
                  phpFpmExtraConfig:
                    description: PhpFpmExtraConfig contains extra php-fpm config
                    type: string
                  phpFpmHpaSpec:
                    description: PhpFpmHpaSpec set php-fpm horizontal pod autoscaler
                      spec
                    type: string
                  phpFpmImage:
                    description: PhpFpmImage defines image for php-fpm container
                    maxLength: 255
                    type: string
                  phpFpmIngressAnnotations:
                    description: PhpFpmIngressAnnotations defines php-fpm annotations
                    type: string
                  phpFpmNetpolEgressIpblock:
                    description: PhpFpmNetpolEgressIpblock defines egress ip block
                      for php-fpm default network policy
                    type: string
                  phpFpmNetpolIngressIpblock:
                    description: PhpFpmNetpolIngressIpblock defines ingress ip block
                      for php-fpm default network policy
                    type: string
                  phpFpmNetpolOmit:
                    description: 'PhpFpmNetpolOmit whether to omit default network
                      policy for php-fpm. Default: true'
                    type: boolean
                  phpFpmNodeSelector:
                    description: PhpFpmNodeSelector defines any node labels selectors
                      for PhpFpm pods.
                    type: string
                  phpFpmPhpExtraIni:
                    description: PhpFpmPhpExtraIni contains extra php ini config
                    type: string
                  phpFpmResourceLimits:
                    description: 'PhpFpmResourceLimits whether php-fpm resource limits
                      are added. Default: false'
                    type: boolean
                  phpFpmResourceLimitsCpu:
                    description: PhpFpmResourceLimitsCpu set php-fpm resource limits
                      cpu
                    maxLength: 20
                    type: string
                  phpFpmResourceLimitsMemory:
                    description: PhpFpmResourceLimitsMemory set php-fpm resource limits
                      memory
                    maxLength: 20
                    type: string
                  phpFpmResourceRequests:
                    description: 'PhpFpmResourceRequests whether php-fpm resource
                      requests are added. Default: true'
                    type: boolean
                  phpFpmResourceRequestsCpu:
                    description: PhpFpmResourceRequestsCpu set php-fpm resource requests
                      cpu
                    maxLength: 20
                    type: string
                  phpFpmResourceRequestsMemory:
                    description: PhpFpmResourceRequestsMemory set php-fpm resource
                      requests memory
                    maxLength: 20
                    type: string
                  phpFpmSize:
                    description: PhpFpmSize defines php-fpm number of replicas between
                      0 and 255
                    format: int32
                    maximum: 255
                    minimum: 0
                    type: integer
                  phpFpmTolerations:
                    description: PhpFpmTolerations defines any tolerations for php-fpm
                      pods.
                    items:
                      description: |-
                        The pod this Toleration is attached to tolerates any taint that matches
                        the triple <key,value,effect> using the matching operator <operator>.
                      properties:
                        effect:
                          description: |-
                            Effect indicates the taint effect to match. Empty means match all taint effects.
                            When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: |-
                            Key is the taint key that the toleration applies to. Empty means match all taint keys.
                            If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                          type: string
                        operator:
                          description: |-
                            Operator represents a key's relationship to the value.
                            Valid operators are Exists and Equal. Defaults to Equal.
                            Exists is equivalent to wildcard for value, so that a pod can
                            tolerate all taints of a particular category.
                          type: string
                        tolerationSeconds:
                          description: |-
                            TolerationSeconds represents the period of time the toleration (which must be
                            of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                            it is not set, which means tolerate the taint forever (do not evict). Zero and
                            negative values will be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: |-
                            Value is the taint value the toleration matches to.
                            If the operator is Exists, the value should be empty, otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                  phpFpmVpaSpec:
                    description: PhpFpmVpaSpec set php-fpm vertical pod autoscaler
                      spec
                    type: string
                  routineStatusCrNotify:
                    description: RoutineStatusCrNotify specification using ansible
                      URI module
                    properties:
                      headers:
                        description: Headers used when notifying status to an endpoint
                        type: object
                      jwtSecretEnvName:
                        description: JwtSecretEnvName environment variable name that
                          holds secret to generate jwt tokens
                        type: string
                      method:
                        description: Method The HTTP method of the request or response.
                        enum:
                        - GET
                        - POST
                        - PUT
                        - PATCH
                        - DELETE
                        type: string
                      statusCode:
                        description: StatusCode A list of valid, numeric, HTTP status
                          codes that signifies success of the request.
                        items:
                          type: integer
                        type: array
                      url:
                        description: HTTP or HTTPS URL in the form (http|https)://host.domain[:port]/path
                        type: string
                      uuid:
                        description: UUID used when notifying status to an endpoint
                        maxLength: 36
                        minLength: 36
                        type: string
                    required:
                    - url
                    type: object
                  routineStatusCrNotifyTermination:
                    description: RoutineStatusCrNotifyTermination specification using
                      ansible URI module
                    properties:
                      headers:
                        description: Headers used when notifying status to an endpoint
                        type: object
                      jwtSecretEnvName:
                        description: JwtSecretEnvName environment variable name that
                          holds secret to generate jwt tokens
                        type: string
                      method:
                        description: Method The HTTP method of the request or response.
                        enum:
                        - GET
                        - POST
                        - PUT
                        - PATCH
                        - DELETE
                        type: string
                      statusCode:
                        description: StatusCode A list of valid, numeric, HTTP status
                          codes that signifies success of the request.
                        items:
                          type: integer
                        type: array
                      url:
                        description: HTTP or HTTPS URL in the form (http|https)://host.domain[:port]/path
                        type: string
                      uuid:
                        description: UUID used when notifying status to an endpoint
                        maxLength: 36
                        minLength: 36
                        type: string
                    required:
                    - url
                    type: object
                required:
                - moodleNewAdminpassHash
                - moodleNewInstanceAdminmail
                - moodleNewInstanceAgreeLicense
                type: object
              nfsSpec:
                description: NfsSpec defines (NFS) Ganesha server spec to deploy optionally
                properties:
                  ganeshaAffinity:
                    description: GaneshaAffinity defines any affinity rules for Ganesha
                      pods.
                    type: string
                  ganeshaConfLogLevel:
                    description: 'GaneshaConfLogLevel defines nfs log level. Default:
                      EVENT'
                    enum:
                    - "NULL"
                    - FATAL
                    - MAJ
                    - CRIT
                    - WARN
                    - EVENT
                    - INFO
                    - DEBUG
                    - MID_DEBUG
                    - M_DBG
                    - FULL_DEBUG
                    - F_DBG
                    type: string
                  ganeshaExportGroupid:
                    description: GaneshaExportGroupid defines export folder groupid
                    format: int32
                    type: integer
                  ganeshaExportMode:
                    description: GaneshaExportMode defines folder permissions mode
                    pattern: '[0-7]{4}'
                    type: string
                  ganeshaExportUserid:
                    description: GaneshaExportUserid defines export folder userid
                    format: int32
                    type: integer
                  ganeshaExtraBlockConfig:
                    description: GaneshaExtraBlockConfig contains extra block in ganesha
                      server ganesha config
                    type: string
                  ganeshaImage:
                    description: GaneshaImage defines image for ganesha server container
                    maxLength: 255
                    type: string
                  ganeshaNetpolEgressIpblock:
                    description: GaneshaNetpolEgressIpblock defines egress ip block
                      for ganesha default network policy
                    type: string
                  ganeshaNetpolIngressIpblock:
                    description: GaneshaNetpolIngressIpblock defines ingress ip block
                      for ganesha default network policy
                    type: string
                  ganeshaNetpolOmit:
                    description: 'GaneshaNetpolOmit whether to omit default network
                      policy for ganesha. Default: true'
                    type: boolean
                  ganeshaNodeSelector:
                    description: GaneshaNodeSelector defines any node labels selectors
                      for Ganesha pods.
                    type: string
                  ganeshaPvcDataAutoexpansion:
                    description: GaneshaPvcDataAutoexpansion enables autoexpansion
                    type: boolean
                  ganeshaPvcDataAutoexpansionCapGib:
                    description: GaneshaPvcDataAutoexpansionCapGib defines limit for
                      autoexpansion increments
                    format: int32
                    type: integer
                  ganeshaPvcDataAutoexpansionIncrementGib:
                    description: GaneshaPvcDataAutoexpansionIncrementGib defines Gib
                      to increment
                    format: int32
                    type: integer
                  ganeshaPvcDataSize:
                    description: GaneshaPvcDataSize defines ganesha server storage
                      size
                    maxLength: 20
                    minLength: 2
                    type: string
                  ganeshaPvcDataStorageAccessMode:
                    description: GaneshaPvcDataStorageAccessMode defines ganesha server
                      storage access modes
                    enum:
                    - ReadWriteOnce
                    - ReadOnlyMany
                    - ReadWriteMany
                    type: string
                  ganeshaPvcDataStorageClassName:
                    description: GaneshaPvcDataStorageClassName defines ganesha server
                      storage class
                    maxLength: 63
                    minLength: 2
                    type: string
                  ganeshaResourceLimits:
                    description: 'GaneshaResourceLimits whether ganesha resource limits
                      are added. Default: false'
                    type: boolean
                  ganeshaResourceLimitsCpu:
                    description: GaneshaResourceLimitsCpu set ganesha resource limits
                      cpu
                    maxLength: 20
                    type: string
                  ganeshaResourceLimitsMemory:
                    description: GaneshaResourceLimitsMemory set ganesha resource
                      limits memory
                    maxLength: 20
                    type: string
                  ganeshaResourceRequests:
                    description: 'GaneshaResourceRequests whether ganesha resource
                      requests are added. Default: true'
                    type: boolean
                  ganeshaResourceRequestsCpu:
                    description: GaneshaResourceRequestsCpu set ganesha resource requests
                      cpu
                    maxLength: 20
                    type: string
                  ganeshaResourceRequestsMemory:
                    description: GaneshaResourceRequestsMemory set ganesha resource
                      requests memory
                    maxLength: 20
                    type: string
                  ganeshaTolerations:
                    description: GaneshaTolerations defines any tolerations for Ganesha
                      server pods.
                    items:
                      description: |-
                        The pod this Toleration is attached to tolerates any taint that matches
                        the triple <key,value,effect> using the matching operator <operator>.
                      properties:
                        effect:
                          description: |-
                            Effect indicates the taint effect to match. Empty means match all taint effects.
                            When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: |-
                            Key is the taint key that the toleration applies to. Empty means match all taint keys.
                            If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                          type: string
                        operator:
                          description: |-
                            Operator represents a key's relationship to the value.
                            Valid operators are Exists and Equal. Defaults to Equal.
                            Exists is equivalent to wildcard for value, so that a pod can
                            tolerate all taints of a particular category.
                          type: string
                        tolerationSeconds:
                          description: |-
                            TolerationSeconds represents the period of time the toleration (which must be
                            of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                            it is not set, which means tolerate the taint forever (do not evict). Zero and
                            negative values will be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: |-
                            Value is the taint value the toleration matches to.
                            If the operator is Exists, the value should be empty, otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                  ganeshaVpaSpec:
                    description: GaneshaVpaSpec set ganesha horizontal pod autoscaler
                      spec
                    type: string
                type: object
              postgresSpec:
                description: PostgresSpec defines Postgres spec to deploy optionally
                properties:
                  pgbouncerAffinity:
                    description: PgbouncerAffinity defines any affinity rules for
                      Pgbouncer pods.
                    type: string
                  pgbouncerExtraConfig:
                    description: PgbouncerExtraConfig contains extra pgbouncer config
                    type: string
                  pgbouncerNetpolEgressIpblock:
                    description: PgbouncerNetpolEgressIpblock defines egress ip block
                      for pgbouncer default network policy
                    type: string
                  pgbouncerNetpolIngressIpblock:
                    description: PgbouncerNetpolIngressIpblock defines ipblock for
                      pgbouncer default network policy
                    type: string
                  pgbouncerNetpolOmit:
                    description: 'PgbouncerNetpolOmit whether to omit default network
                      policy for pgbouncer. Default: true'
                    type: boolean
                  pgbouncerNodeSelector:
                    description: PgbouncerNodeSelector defines any node labels selectors
                      for Pgbouncer pods.
                    type: string
                  pgbouncerReadonlyAffinity:
                    description: PgbouncerReadonlyAffinity defines any affinity rules
                      for PgbouncerReadonly pods.
                    type: string
                  pgbouncerReadonlyExtraConfig:
                    description: PgbouncerReadonlyExtraConfig contains extra pgbouncer
                      readonly config
                    type: string
                  pgbouncerReadonlyNodeSelector:
                    description: PgbouncerReadonlyNodeSelector defines any node labels
                      selectors for PgbouncerReadonly pods.
                    type: string
                  pgbouncerReadonlyResourceLimits:
                    description: 'PgbouncerReadonlyResourceLimits whether pgbouncer
                      readonly resource limits are added. Default: false'
                    type: boolean
                  pgbouncerReadonlyResourceLimitsCpu:
                    description: PgbouncerReadonlyResourceLimitsCpu set pgbouncer
                      readonly resource limits cpu
                    maxLength: 20
                    type: string
                  pgbouncerReadonlyResourceLimitsMemory:
                    description: PgbouncerReadonlyResourceLimitsMemory set pgbouncer
                      readonly resource limits memory
                    maxLength: 20
                    type: string
                  pgbouncerReadonlyResourceRequests:
                    description: 'PgbouncerReadonlyResourceRequests whether pgbouncer
                      readonly resource requests are added. Default: true'
                    type: boolean
                  pgbouncerReadonlyResourceRequestsCpu:
                    description: PgbouncerReadonlyResourceRequestsCpu set pgbouncer
                      readonly resource requests cpu
                    maxLength: 20
                    type: string
                  pgbouncerReadonlyResourceRequestsMemory:
                    description: PgbouncerReadonlyResourceRequestsMemory set pgbouncer
                      readonly resource requests memory
                    maxLength: 20
                    type: string
                  pgbouncerReadonlyTolerations:
                    description: PgbouncerReadonlyTolerations defines any tolerations
                      for PgbouncerReadonly pods.
                    items:
                      description: |-
                        The pod this Toleration is attached to tolerates any taint that matches
                        the triple <key,value,effect> using the matching operator <operator>.
                      properties:
                        effect:
                          description: |-
                            Effect indicates the taint effect to match. Empty means match all taint effects.
                            When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: |-
                            Key is the taint key that the toleration applies to. Empty means match all taint keys.
                            If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                          type: string
                        operator:
                          description: |-
                            Operator represents a key's relationship to the value.
                            Valid operators are Exists and Equal. Defaults to Equal.
                            Exists is equivalent to wildcard for value, so that a pod can
                            tolerate all taints of a particular category.
                          type: string
                        tolerationSeconds:
                          description: |-
                            TolerationSeconds represents the period of time the toleration (which must be
                            of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                            it is not set, which means tolerate the taint forever (do not evict). Zero and
                            negative values will be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: |-
                            Value is the taint value the toleration matches to.
                            If the operator is Exists, the value should be empty, otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                  pgbouncerReadonlyVpaSpec:
                    description: PgbouncerReadonlyVpaSpec set pgbouncer readonly horizontal
                      pod autoscaler spec
                    type: string
                  pgbouncerResourceLimits:
                    description: 'PgbouncerResourceLimits whether pgbouncer resource
                      limits are added. Default: false'
                    type: boolean
                  pgbouncerResourceLimitsCpu:
                    description: PgbouncerResourceLimitsCpu set pgbouncer resource
                      limits cpu
                    maxLength: 20
                    type: string
                  pgbouncerResourceLimitsMemory:
                    description: PgbouncerResourceLimitsMemory set pgbouncer resource
                      limits memory
                    maxLength: 20
                    type: string
                  pgbouncerResourceRequests:
                    description: 'PgbouncerResourceRequests whether pgbouncer resource
                      requests are added. Default: true'
                    type: boolean
                  pgbouncerResourceRequestsCpu:
                    description: PgbouncerResourceRequestsCpu set pgbouncer resource
                      requests cpu
                    maxLength: 20
                    type: string
                  pgbouncerResourceRequestsMemory:
                    description: PgbouncerResourceRequestsMemory set pgbouncer resource
                      requests memory
                    maxLength: 20
                    type: string
                  pgbouncerTolerations:
                    description: PgbouncerTolerations defines any tolerations for
                      Pgbouncer pods.
                    items:
                      description: |-
                        The pod this Toleration is attached to tolerates any taint that matches
                        the triple <key,value,effect> using the matching operator <operator>.
                      properties:
                        effect:
                          description: |-
                            Effect indicates the taint effect to match. Empty means match all taint effects.
                            When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: |-
                            Key is the taint key that the toleration applies to. Empty means match all taint keys.
                            If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                          type: string
                        operator:
                          description: |-
                            Operator represents a key's relationship to the value.
                            Valid operators are Exists and Equal. Defaults to Equal.
                            Exists is equivalent to wildcard for value, so that a pod can
                            tolerate all taints of a particular category.
                          type: string
                        tolerationSeconds:
                          description: |-
                            TolerationSeconds represents the period of time the toleration (which must be
                            of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                            it is not set, which means tolerate the taint forever (do not evict). Zero and
                            negative values will be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: |-
                            Value is the taint value the toleration matches to.
                            If the operator is Exists, the value should be empty, otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                  pgbouncerVpaSpec:
                    description: PgbouncerVpaSpec set pgbouncer horizontal pod autoscaler
                      spec
                    type: string
                  postgresAffinity:
                    description: PostgresAffinity defines any affinity rules for Postgres
                      pods.
                    type: string
                  postgresExtraConfig:
                    description: PostgresExtraConfig contains extra postgres config
                    type: string
                  postgresImage:
                    description: PostgresImage defines image for postgres container
                    maxLength: 255
                    type: string
                  postgresMode:
                    description: PostgresMode describes mode postgres runs
                    enum:
                    - standalone
                    - readreplicas
                    type: string
                  postgresNetpolEgressIpblock:
                    description: PostgresNetpolEgressIpblock defines egress ip block
                      for postgres default network policy
                    type: string
                  postgresNetpolIngressIpblock:
                    description: PostgresNetpolIngressIpblock defines ingress ip block
                      for postgres default network policy
                    type: string
                  postgresNetpolOmit:
                    description: 'PostgresNetpolOmit whether to omit default network
                      policy for postgres. Default: true'
                    type: boolean
                  postgresNodeSelector:
                    description: PostgresNodeSelector defines any node labels selectors
                      for Postgres pods.
                    type: string
                  postgresPvcDataAutoexpansion:
                    description: PostgresPvcDataAutoexpansion enables autoexpansion
                    type: boolean
                  postgresPvcDataAutoexpansionCapGib:
                    description: PostgresPvcDataAutoexpansionCapGib defines limit
                      for autoexpansion increments
                    format: int32
                    type: integer
                  postgresPvcDataAutoexpansionIncrementGib:
                    description: PostgresPvcDataAutoexpansionIncrementGib defines
                      Gib to increment
                    format: int32
                    type: integer
                  postgresPvcDataSize:
                    description: PostgresPvcDataSize defines postgres storage size
                    maxLength: 20
                    minLength: 2
                    type: string
                  postgresPvcDataStorageAccessMode:
                    description: PostgresPvcDataStorageAccessMode defines postgres
                      storage access modes
                    enum:
                    - ReadWriteOnce
                    - ReadOnlyMany
                    - ReadWriteMany
                    type: string
                  postgresPvcDataStorageClassName:
                    description: PostgresPvcDataStorageClassName defines postgres
                      storage class
                    maxLength: 63
                    minLength: 2
                    type: string
                  postgresReadreplicasAffinity:
                    description: PostgresReadreplicasAffinity defines any affinity
                      rules for PostgresReadreplicas pods.
                    type: string
                  postgresReadreplicasNodeSelector:
                    description: PostgresReadreplicasNodeSelector defines any node
                      labels selectors for PostgresReadreplicas pods.
                    type: string
                  postgresReadreplicasPvcDataAutoexpansion:
                    description: PostgresReadreplicasPvcDataAutoexpansion enables
                      autoexpansion
                    type: boolean
                  postgresReadreplicasPvcDataAutoexpansionCapGib:
                    description: PostgresReadreplicasPvcDataAutoexpansionCapGib defines
                      limit for autoexpansion increments
                    format: int32
                    type: integer
                  postgresReadreplicasPvcDataAutoexpansionIncrementGib:
                    description: PostgresReadreplicasPvcDataAutoexpansionIncrementGib
                      defines Gib to increment
                    format: int32
                    type: integer
                  postgresReadreplicasPvcDataSize:
                    description: PostgresReadreplicasPvcDataSize defines postgres
                      readreplicas storage size
                    maxLength: 20
                    minLength: 2
                    type: string
                  postgresReadreplicasPvcDataStorageAccessMode:
                    description: PostgresReadreplicasPvcDataStorageAccessMode defines
                      postgres readreplicas storage access modes
                    enum:
                    - ReadWriteOnce
                    - ReadOnlyMany
                    - ReadWriteMany
                    type: string
                  postgresReadreplicasPvcDataStorageClassName:
                    description: PostgresReadreplicasPvcDataStorageClassName defines
                      postgres readreplicas storage class
                    maxLength: 63
                    minLength: 2
                    type: string
                  postgresReadreplicasResourceLimits:
                    description: 'PostgresReadreplicasResourceLimits whether postgres
                      readreplicas resource limits are added. Default: false'
                    type: boolean
                  postgresReadreplicasResourceLimitsCpu:
                    description: PostgresReadreplicasResourceLimitsCpu set postgres
                      readreplicas resource limits cpu
                    maxLength: 20
                    type: string
                  postgresReadreplicasResourceLimitsMemory:
                    description: PostgresReadreplicasResourceLimitsMemory set postgres
                      readreplicas resource limits memory
                    maxLength: 20
                    type: string
                  postgresReadreplicasResourceRequests:
                    description: 'PostgresReadreplicasResourceRequests whether postgres
                      readreplicas resource requests are added. Default: true'
                    type: boolean
                  postgresReadreplicasResourceRequestsCpu:
                    description: PostgresReadreplicasResourceRequestsCpu set postgres
                      readreplicas resource requests cpu
                    maxLength: 20
                    type: string
                  postgresReadreplicasResourceRequestsMemory:
                    description: PostgresReadreplicasResourceRequestsMemory set postgres
                      readreplicas resource requests memory
                    maxLength: 20
                    type: string
                  postgresReadreplicasSize:
                    description: PostgresReadreplicasSize defines postgres readreplicas
                      number of replicas
                    format: int32
                    type: integer
                  postgresReadreplicasTolerations:
                    description: PostgresReadreplicasTolerations defines any tolerations
                      for PostgresReadreplicas pods.
                    items:
                      description: |-
                        The pod this Toleration is attached to tolerates any taint that matches
                        the triple <key,value,effect> using the matching operator <operator>.
                      properties:
                        effect:
                          description: |-
                            Effect indicates the taint effect to match. Empty means match all taint effects.
                            When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: |-
                            Key is the taint key that the toleration applies to. Empty means match all taint keys.
                            If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                          type: string
                        operator:
                          description: |-
                            Operator represents a key's relationship to the value.
                            Valid operators are Exists and Equal. Defaults to Equal.
                            Exists is equivalent to wildcard for value, so that a pod can
                            tolerate all taints of a particular category.
                          type: string
                        tolerationSeconds:
                          description: |-
                            TolerationSeconds represents the period of time the toleration (which must be
                            of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                            it is not set, which means tolerate the taint forever (do not evict). Zero and
                            negative values will be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: |-
                            Value is the taint value the toleration matches to.
                            If the operator is Exists, the value should be empty, otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                  postgresReadreplicasVpaSpec:
                    description: PostgresReadreplicasVpaSpec set postgres readreplicas
                      horizontal pod autoscaler spec
                    type: string
                  postgresResourceLimits:
                    description: 'PostgresResourceLimits whether postgres resource
                      limits are added. Default: false'
                    type: boolean
                  postgresResourceLimitsCpu:
                    description: PostgresResourceLimitsCpu set postgres resource limits
                      cpu
                    maxLength: 20
                    type: string
                  postgresResourceLimitsMemory:
                    description: PostgresResourceLimitsMemory set postgres resource
                      limits memory
                    maxLength: 20
                    type: string
                  postgresResourceRequests:
                    description: 'PostgresResourceRequests whether postgres resource
                      requests are added. Default: true'
                    type: boolean
                  postgresResourceRequestsCpu:
                    description: PostgresResourceRequestsCpu set postgres resource
                      requests cpu
                    maxLength: 20
                    type: string
                  postgresResourceRequestsMemory:
                    description: PostgresResourceRequestsMemory set postgres resource
                      requests memory
                    maxLength: 20
                    type: string
                  postgresSize:
                    description: PostgresSize defines postgres number of replicas
                    format: int32
                    type: integer
                  postgresTolerations:
                    description: PostgresTolerations defines any tolerations for Postgres
                      pods.
                    items:
                      description: |-
                        The pod this Toleration is attached to tolerates any taint that matches
                        the triple <key,value,effect> using the matching operator <operator>.
                      properties:
                        effect:
                          description: |-
                            Effect indicates the taint effect to match. Empty means match all taint effects.
                            When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: |-
                            Key is the taint key that the toleration applies to. Empty means match all taint keys.
                            If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                          type: string
                        operator:
                          description: |-
                            Operator represents a key's relationship to the value.
                            Valid operators are Exists and Equal. Defaults to Equal.
                            Exists is equivalent to wildcard for value, so that a pod can
                            tolerate all taints of a particular category.
                          type: string
                        tolerationSeconds:
                          description: |-
                            TolerationSeconds represents the period of time the toleration (which must be
                            of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                            it is not set, which means tolerate the taint forever (do not evict). Zero and
                            negative values will be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: |-
                            Value is the taint value the toleration matches to.
                            If the operator is Exists, the value should be empty, otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                  postgresUpgrade:
                    description: PostgresUpgrade defines whether postgres upgrade
                      is enabled
                    type: boolean
                  postgresVpaSpec:
                    description: PostgresVpaSpec set postgres horizontal pod autoscaler
                      spec
                    type: string
                type: object
            required:
            - moodleSpec
            type: object
          status:
            description: LMSMoodleTemplateStatus defines the observed state of LMSMoodleTemplate
            properties:
              state:
                default: Unknown
                description: state describes the LMSMoodleTemplate state
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
