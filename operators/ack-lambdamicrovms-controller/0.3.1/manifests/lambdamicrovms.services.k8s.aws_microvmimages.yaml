apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  creationTimestamp: null
  name: microvmimages.lambdamicrovms.services.k8s.aws
spec:
  group: lambdamicrovms.services.k8s.aws
  names:
    kind: MicrovmImage
    listKind: MicrovmImageList
    plural: microvmimages
    singular: microvmimage
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MicrovmImage is the Schema for the MicrovmImages API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MicrovmImageSpec defines the desired state of MicrovmImage.
            properties:
              additionalOsCapabilities:
                description: Additional OS capabilities granted to the MicroVM runtime
                  environment.
                items:
                  type: string
                type: array
              baseImageARN:
                description: |-
                  The ARN of the Lambda-managed base MicroVM image to build upon. Use ListManagedMicrovmImages
                  to discover available base images.

                  Regex Pattern: `^[^\s]+$`
                type: string
              baseImageVersion:
                description: |-
                  The specific version of the base MicroVM image to use.

                  Regex Pattern: `^[^\s]+$`
                type: string
              buildRoleARN:
                description: |-
                  The ARN of the IAM role assumed during the image build process. This role
                  must have permissions to access the code artifact and any required resources.

                  Regex Pattern: `^arn:aws[a-z\-]*:iam::[0-9]{12}:role/?[a-zA-Z_0-9+=,.@\-_/]+$`
                type: string
              buildRoleRef:
                description: "AWSResourceReferenceWrapper provides a wrapper around
                  *AWSResourceReference\ntype to provide more user friendly syntax
                  for references using 'from' field\nEx:\nAPIIDRef:\n\n\tfrom:\n\t
                  \ name: my-api"
                properties:
                  from:
                    description: |-
                      AWSResourceReference provides all the values necessary to reference another
                      k8s resource for finding the identifier(Id/ARN/Name)
                    properties:
                      name:
                        type: string
                      namespace:
                        type: string
                    type: object
                type: object
              codeArtifact:
                description: |-
                  The code artifact containing the application code and metadata for the MicroVM
                  image.
                properties:
                  uri:
                    description: A string which is not empty or blank (only whitespace).
                    type: string
                type: object
              cpuConfigurations:
                description: The list of supported CPU configurations for the MicroVM.
                items:
                  description: Configuration for the CPU architecture of a MicroVM.
                  properties:
                    architecture:
                      type: string
                  type: object
                type: array
              description:
                description: A description of the MicroVM image.
                type: string
              egressNetworkConnectors:
                description: The list of egress network connectors available to the
                  MicroVM at runtime.
                items:
                  type: string
                type: array
              environmentVariables:
                additionalProperties:
                  type: string
                description: Environment variables set in the MicroVM runtime environment.
                type: object
              hooks:
                description: Lifecycle hook configuration for MicroVMs and MicroVM
                  images.
                properties:
                  microvmHooks:
                    description: |-
                      Configuration for lifecycle hooks invoked during MicroVM events such as run,
                      resume, suspend, and terminate.
                    properties:
                      resume:
                        description: Whether invocation hooks are enabled or disabled
                          on a MicroVm.
                        type: string
                      resumeTimeoutInSeconds:
                        format: int64
                        type: integer
                      run:
                        description: Whether invocation hooks are enabled or disabled
                          on a MicroVm.
                        type: string
                      runTimeoutInSeconds:
                        format: int64
                        type: integer
                      suspend:
                        description: Whether invocation hooks are enabled or disabled
                          on a MicroVm.
                        type: string
                      suspendTimeoutInSeconds:
                        format: int64
                        type: integer
                      terminate:
                        description: Whether invocation hooks are enabled or disabled
                          on a MicroVm.
                        type: string
                      terminateTimeoutInSeconds:
                        format: int64
                        type: integer
                    type: object
                  microvmImageHooks:
                    description: |-
                      Configuration for hooks invoked during MicroVM image build events such as
                      ready and validate.
                    properties:
                      ready:
                        description: Whether invocation hooks are enabled or disabled
                          on a MicroVm.
                        type: string
                      readyTimeoutInSeconds:
                        format: int64
                        type: integer
                      validate:
                        description: Whether invocation hooks are enabled or disabled
                          on a MicroVm.
                        type: string
                      validateTimeoutInSeconds:
                        format: int64
                        type: integer
                    type: object
                  port:
                    format: int64
                    type: integer
                type: object
              logging:
                description: |-
                  The logging configuration for build-time and runtime logs. Specify {"cloudWatch":
                  {"logGroup": "..."}} to stream logs to a custom CloudWatch log group, or
                  {"disabled": {}} to turn off logging.
                properties:
                  cloudWatch:
                    description: Configuration for Amazon CloudWatch Logs logging.
                    properties:
                      logGroup:
                        type: string
                      logStream:
                        type: string
                    type: object
                  disabled:
                    additionalProperties:
                      type: string
                    description: Specifies that logging is disabled for the MicroVM.
                    type: object
                type: object
              name:
                description: |-
                  The name of the MicroVM image. Must be unique within the AWS account.

                  Regex Pattern: `^[a-zA-Z0-9-_]+$`
                type: string
                x-kubernetes-validations:
                - message: Value is immutable once set
                  rule: self == oldSelf
              resources:
                description: The resource requirements for the MicroVM.
                items:
                  description: Resource requirements for a MicroVM.
                  properties:
                    minimumMemoryInMiB:
                      format: int64
                      type: integer
                  type: object
                type: array
              tags:
                additionalProperties:
                  type: string
                description: |-
                  A set of key-value pairs that you can attach to the resource. Use tags to
                  categorize resources for cost allocation, access control (ABAC), and organization.
                type: object
            required:
            - baseImageARN
            - codeArtifact
            - name
            type: object
          status:
            description: MicrovmImageStatus defines the observed state of MicrovmImage
            properties:
              ackResourceMetadata:
                description: |-
                  All CRs managed by ACK have a common `Status.ACKResourceMetadata` member
                  that is used to contain resource sync state, account ownership,
                  constructed ARN for the resource
                properties:
                  arn:
                    description: |-
                      ARN is the Amazon Resource Name for the resource. This is a
                      globally-unique identifier and is set only by the ACK service controller
                      once the controller has orchestrated the creation of the resource OR
                      when it has verified that an "adopted" resource (a resource where the
                      ARN annotation was set by the Kubernetes user on the CR) exists and
                      matches the supplied CR's Spec field values.
                      https://github.com/aws/aws-controllers-k8s/issues/270
                    type: string
                  ownerAccountID:
                    description: |-
                      OwnerAccountID is the AWS Account ID of the account that owns the
                      backend AWS service API resource.
                    type: string
                  partition:
                    description: Partition is the AWS partition in which the resource
                      exists or will exist
                    type: string
                  region:
                    description: Region is the AWS region in which the resource exists
                      or will exist.
                    type: string
                required:
                - ownerAccountID
                - region
                type: object
              conditions:
                description: |-
                  All CRs managed by ACK have a common `Status.Conditions` member that
                  contains a collection of `ackv1alpha1.Condition` objects that describe
                  the various terminal states of the CR and its backend AWS service API
                  resource
                items:
                  description: |-
                    Condition is the common struct used by all CRDs managed by ACK service
                    controllers to indicate terminal states  of the CR and its backend AWS
                    service API resource
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      format: date-time
                      type: string
                    message:
                      description: A human readable message indicating details about
                        the transition.
                      type: string
                    reason:
                      description: The reason for the condition's last transition.
                      type: string
                    status:
                      description: Status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the Condition
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: The timestamp when the MicroVM image was created.
                format: date-time
                type: string
              imageVersion:
                description: |-
                  The version of the MicroVM image.

                  Regex Pattern: `^[^\s]+$`
                type: string
              latestActiveImageVersion:
                description: |-
                  The latest active version of the MicroVM image.

                  Regex Pattern: `^[^\s]+$`
                type: string
              latestFailedImageVersion:
                description: |-
                  The latest failed version of the MicroVM image, if any.

                  Regex Pattern: `^[^\s]+$`
                type: string
              resolvedBaseImageVersion:
                description: |-
                  The specific version of the base MicroVM image.

                  Regex Pattern: `^[^\s]+$`
                type: string
              state:
                description: The current state of the MicroVM image.
                type: string
              updatedAt:
                description: The timestamp when the MicroVM image was last updated.
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
