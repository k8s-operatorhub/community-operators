---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: nfsprovisioners.cache.jhouse.com
spec:
  group: cache.jhouse.com
  names:
    kind: NFSProvisioner
    listKind: NFSProvisionerList
    plural: nfsprovisioners
    singular: nfsprovisioner
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NFSProvisioner is the Schema for the nfsprovisioners API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NFSProvisionerSpec defines the desired state of NFSProvisioner
            properties:
              hostPathDir:
                type: string
              nfsImageConfiguration:
                description: ImageConfiguration holds configuration of the image to
                  use
                properties:
                  image:
                    default: k8s.gcr.io/sig-storage/nfs-provisioner@sha256:e943bb77c7df05ebdc8c7888b2db289b13bf9f012d6a3a5a74f14d4d5743d439
                    description: Set nfs provisioner operator image
                    type: string
                  imagePullPolicy:
                    default: IfNotPresent
                    description: Image PullPolicy is for nfs provisioner operator
                      image.
                    type: string
                required:
                - image
                - imagePullPolicy
                type: object
              nodeSelector:
                additionalProperties:
                  type: string
                type: object
              pvc:
                type: string
              scForNFS:
                type: string
              scForNFSPvc:
                type: string
              storageSize:
                type: string
            type: object
          status:
            description: NFSProvisionerStatus defines the observed state of NFSProvisioner
            properties:
              conditions:
                description: |-
                  Conditions represent the latest available observations of the NFSProvisioner's state
                  Standard condition types:
                  - Ready: Indicates whether the NFSProvisioner is ready to serve requests
                  - Progressing: Indicates whether the NFSProvisioner is being reconciled
                  - Degraded: Indicates whether the NFSProvisioner is experiencing issues
                  - Available: Indicates whether the Deployment has available replicas
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              error:
                description: |-
                  Error contains error message if the NFSProvisioner is in failed state
                  Deprecated: Use Conditions instead
                type: string
              nodes:
                description: |-
                  Nodes contains the list of nodes where NFS server pods are running
                  Deprecated: This information should be retrieved from the Deployment status
                items:
                  type: string
                type: array
              observedGeneration:
                description: |-
                  ObservedGeneration is the generation last observed by the controller.
                  This is used to determine if the controller has processed the latest changes.
                format: int64
                type: integer
              phase:
                description: |-
                  Phase represents the current phase of the NFSProvisioner lifecycle.
                  Valid values are: Pending, Progressing, Ready, Failed
                enum:
                - Pending
                - Progressing
                - Ready
                - Failed
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
