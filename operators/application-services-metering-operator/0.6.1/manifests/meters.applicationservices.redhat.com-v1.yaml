# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: meters.applicationservices.redhat.com
spec:
  group: applicationservices.redhat.com
  names:
    kind: Meter
    plural: meters
    singular: meter
  scope: Cluster
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              includeInfrastructure:
                description: Should pods for infrastructure components be included for calculating usage.
                type: boolean
              meterCollectionEnabled:
                description: Indicates if the collection of metrics into the monitoring stack should be active.
                type: boolean
              watchNamespaces:
                description: List of namespaces to watch for pods with the required labels. An empty list indicates to watch all namespaces.
                items:
                  description: Namespace to watch
                  type: string
                type: array
            type: object
          status:
            description: Defines the observed state of Meter.
            properties:
              serviceMonitorInstalled:
                description: Current status of the ServiceMonitor
                type: string
              watchedPodCount:
                description: How many pods are monitored, and included in the usage metrics.
                type: string
              currentlyWatching:
                description: Is the operator currently watching pods?
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
