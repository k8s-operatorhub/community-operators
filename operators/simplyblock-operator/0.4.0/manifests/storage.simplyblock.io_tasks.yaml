apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  creationTimestamp: null
  name: tasks.storage.simplyblock.io
spec:
  group: storage.simplyblock.io
  names:
    kind: Task
    listKind: TaskList
    plural: tasks
    singular: task
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Task is the Schema for the tasks API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Task
            properties:
              clusterName:
                description: ClusterName is the target storage cluster name.
                type: string
              subtasks:
                description: |-
                  Subtasks includes related child subtasks when supported by the backend.
                  FIXME: Unused for now
                type: boolean
              taskID:
                description: TaskID filters results to a specific backend task when
                  set.
                type: string
            required:
            - clusterName
            type: object
          status:
            description: status defines the observed state of Task
            properties:
              tasks:
                description: Tasks is the currently reported task list for the query
                  scope.
                items:
                  properties:
                    canceled:
                      description: Canceled indicates whether the task was canceled.
                      type: boolean
                    parentTask:
                      description: |-
                        ParentTask is the parent task UUID when this task is a subtask.
                        FIXME: Unused for now
                      type: string
                    retried:
                      description: Retried is the number of retry attempts made for
                        the task.
                      format: int32
                      type: integer
                    startedAt:
                      description: |-
                        StartedAt is the backend-reported task start timestamp.
                        FIXME: Unused for now
                      format: date-time
                      type: string
                    taskResult:
                      description: TaskResult is the backend result payload/message.
                      type: string
                    taskStatus:
                      description: TaskStatus is the backend lifecycle status for
                        the task.
                      type: string
                    taskType:
                      description: TaskType is the backend task function/type name.
                      type: string
                    uuid:
                      description: UUID is the backend task UUID.
                      type: string
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
