apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  creationTimestamp: null
  name: clusterkeycloakinstances.keycloak.hostzero.com
spec:
  group: keycloak.hostzero.com
  names:
    categories:
    - keycloak
    - all
    kind: ClusterKeycloakInstance
    listKind: ClusterKeycloakInstanceList
    plural: clusterkeycloakinstances
    shortNames:
    - ckci
    singular: clusterkeycloakinstance
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - description: Whether the instance is ready
      jsonPath: .status.ready
      name: Ready
      type: boolean
    - description: The base URL of the Keycloak instance
      jsonPath: .spec.baseUrl
      name: URL
      type: string
    - description: Keycloak server version
      jsonPath: .status.version
      name: Version
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: ClusterKeycloakInstance makes a Keycloak server known to the
          operator at the cluster level
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              ClusterKeycloakInstanceSpec defines the desired state of ClusterKeycloakInstance.
              It mirrors KeycloakInstanceSpec but is cluster-scoped: secret references must
              specify a namespace explicitly.
            properties:
              auth:
                description: |-
                  Auth selects how the operator authenticates to Keycloak.
                  Exactly one of auth.passwordGrant or auth.clientCredentials must be set.
                properties:
                  clientCredentials:
                    description: |-
                      ClusterClientCredentialsSpec configures OAuth2 client_credentials
                      authentication for cluster-scoped instances.
                    properties:
                      clientId:
                        description: ClientID, when set, overrides secretRef.clientIdKey.
                        type: string
                      secretRef:
                        description: |-
                          ClusterClientCredentialsSecretRefSpec references a client-credentials Secret.
                          Namespace is required because the resource is cluster-scoped.
                        properties:
                          clientIdKey:
                            default: client-id
                            type: string
                          clientSecretKey:
                            default: client-secret
                            type: string
                          name:
                            type: string
                          namespace:
                            type: string
                        required:
                        - name
                        - namespace
                        type: object
                    required:
                    - secretRef
                    type: object
                  passwordGrant:
                    description: |-
                      ClusterPasswordGrantSpec configures password-grant authentication
                      for cluster-scoped instances.
                    properties:
                      secretRef:
                        description: |-
                          ClusterPasswordGrantSecretRefSpec references an admin-credentials Secret.
                          Namespace is required because the resource is cluster-scoped.
                        properties:
                          name:
                            type: string
                          namespace:
                            type: string
                          passwordKey:
                            default: password
                            type: string
                          usernameKey:
                            default: username
                            type: string
                        required:
                        - name
                        - namespace
                        type: object
                      username:
                        description: Username, when set, overrides secretRef.usernameKey.
                        type: string
                    required:
                    - secretRef
                    type: object
                type: object
                x-kubernetes-validations:
                - message: exactly one of auth.passwordGrant or auth.clientCredentials
                    must be set
                  rule: has(self.passwordGrant) != has(self.clientCredentials)
              baseUrl:
                description: BaseUrl is the URL of the Keycloak server (e.g., http://keycloak:8080)
                type: string
              realm:
                description: Realm is the admin realm (defaults to "master")
                type: string
              tls:
                description: TLS configures how the operator verifies the Keycloak
                  server certificate.
                properties:
                  caCert:
                    description: |-
                      ClusterCACertSource references a Secret or ConfigMap key containing a
                      PEM-encoded CA bundle. Exactly one of secretRef or configMapRef must be set.
                    properties:
                      configMapRef:
                        description: ClusterCACertConfigMapRefSpec is the cluster-scoped
                          variant; namespace required.
                        properties:
                          key:
                            default: ca.crt
                            type: string
                          name:
                            type: string
                          namespace:
                            type: string
                        required:
                        - name
                        - namespace
                        type: object
                      secretRef:
                        description: ClusterCACertSecretRefSpec is the cluster-scoped
                          variant; namespace required.
                        properties:
                          key:
                            default: ca.crt
                            type: string
                          name:
                            type: string
                          namespace:
                            type: string
                        required:
                        - name
                        - namespace
                        type: object
                    type: object
                    x-kubernetes-validations:
                    - message: exactly one of caCert.secretRef or caCert.configMapRef
                        must be set
                      rule: has(self.secretRef) != has(self.configMapRef)
                  insecureSkipVerify:
                    type: boolean
                type: object
              token:
                description: Token contains optional token caching configuration
                properties:
                  expiresKey:
                    description: ExpiresKey is the key in the secret for the token
                      expiration
                    type: string
                  secretName:
                    description: SecretName is the name of the secret to cache the
                      token
                    type: string
                  tokenKey:
                    description: TokenKey is the key in the secret for the token
                    type: string
                type: object
            required:
            - auth
            - baseUrl
            type: object
          status:
            description: ClusterKeycloakInstanceStatus defines the observed state
              of ClusterKeycloakInstance
            properties:
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              message:
                description: Message contains additional information about the status
                type: string
              ready:
                description: Ready indicates if the Keycloak instance is accessible
                type: boolean
              resourcePath:
                description: ResourcePath is the API path for this resource
                type: string
              status:
                description: Status is a human-readable status message
                type: string
              version:
                description: Version is the Keycloak server version
                type: string
            required:
            - ready
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
