apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
    alm-examples: '[{"apiVersion":"krkn.krkn-chaos.dev/v1alpha1","kind":"KrknOperatorTarget","metadata":{"name":"example-target"},"spec":{"uuid":"example-target","clusterName":"cluster-1","clusterAPIURL":"https://api.example.invalid:6443","secretType":"kubeconfig","secretUUID":"example-kubeconfig"}}]'
  creationTimestamp: null
  name: krknoperatortargets.krkn.krkn-chaos.dev
spec:
  group: krkn.krkn-chaos.dev
  names:
    kind: KrknOperatorTarget
    listKind: KrknOperatorTargetList
    plural: krknoperatortargets
    shortNames:
      - kot
    singular: krknoperatortarget
  scope: Namespaced
  versions:
    - additionalPrinterColumns:
        - jsonPath: .spec.clusterName
          name: Cluster
          type: string
        - jsonPath: .spec.clusterAPIURL
          name: API URL
          type: string
        - jsonPath: .spec.secretType
          name: Type
          type: string
        - jsonPath: .status.ready
          name: Ready
          type: boolean
        - jsonPath: .metadata.creationTimestamp
          name: Age
          type: date
      name: v1alpha1
      schema:
        openAPIV3Schema:
          description: KrknOperatorTarget is the Schema for the krknoperatortargets API.
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              description: KrknOperatorTargetSpec defines the desired state of KrknOperatorTarget.
              properties:
                caBundle:
                  description: |-
                    CABundle is the base64-encoded CA certificate bundle for TLS verification
                    Optional - if not provided and SecretType is not kubeconfig, TLS verification will be skipped
                  type: string
                clusterAPIURL:
                  description: ClusterAPIURL is the Kubernetes API server URL
                  type: string
                clusterName:
                  description: ClusterName is the name of the target cluster
                  type: string
                insecureSkipTLSVerify:
                  default: false
                  description: |-
                    InsecureSkipTLSVerify skips TLS certificate verification
                    Only used when CABundle is not provided
                  type: boolean
                secretType:
                  description: SecretType specifies the authentication method
                  enum:
                    - kubeconfig
                    - token
                    - credentials
                  type: string
                secretUUID:
                  description: SecretUUID is the UUID of the Secret containing the kubeconfig
                  type: string
                uuid:
                  description: UUID is the unique identifier for this target
                  type: string
              required:
                - clusterName
                - secretType
                - secretUUID
                - uuid
              type: object
            status:
              description: KrknOperatorTargetStatus defines the observed state of KrknOperatorTarget.
              properties:
                lastUpdated:
                  description: LastUpdated is the timestamp of the last update
                  format: date-time
                  type: string
                ready:
                  default: true
                  description: Ready indicates whether the target is ready to be used
                  type: boolean
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
