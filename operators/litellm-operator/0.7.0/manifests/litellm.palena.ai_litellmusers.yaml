apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  creationTimestamp: null
  name: litellmusers.litellm.palena.ai
spec:
  group: litellm.palena.ai
  names:
    kind: LiteLLMUser
    listKind: LiteLLMUserList
    plural: litellmusers
    shortNames:
    - lu
    singular: litellmuser
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.userId
      name: UserID
      type: string
    - jsonPath: .spec.userRole
      name: Role
      type: string
    - jsonPath: .status.synced
      name: Synced
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LiteLLMUser is the Schema for the litellmusers API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LiteLLMUserSpec defines the desired state of LiteLLMUser.
            properties:
              budgetDuration:
                description: Budget reset duration (e.g., "30d").
                type: string
              instanceRef:
                description: Reference to the LiteLLMInstance.
                properties:
                  name:
                    description: Name of the LiteLLMInstance CR.
                    type: string
                required:
                - name
                type: object
              maxBudget:
                description: Maximum budget in USD.
                type: number
              metadata:
                additionalProperties:
                  type: string
                description: Custom metadata.
                type: object
              models:
                description: Models this user can access.
                items:
                  type: string
                type: array
              rpmLimit:
                description: RPM limit for this user.
                type: integer
              teams:
                description: Teams this user belongs to.
                items:
                  description: UserTeamMembership defines a user's team membership.
                  properties:
                    maxBudgetInTeam:
                      description: Max budget within this team.
                      type: number
                    role:
                      default: user
                      description: Role within the team.
                      type: string
                    teamId:
                      description: Direct team ID (used when team is not managed by
                        a CRD).
                      type: string
                    teamRef:
                      description: Reference to a LiteLLMTeam CR.
                      properties:
                        name:
                          description: Name of the LiteLLMInstance CR.
                          type: string
                      required:
                      - name
                      type: object
                  type: object
                type: array
              tpmLimit:
                description: TPM limit for this user.
                type: integer
              userEmail:
                description: User email address.
                type: string
              userId:
                description: Unique user identifier (typically email).
                type: string
              userRole:
                default: internal_user
                description: User role.
                enum:
                - proxy_admin
                - proxy_admin_viewer
                - internal_user
                - internal_user_viewer
                type: string
            required:
            - instanceRef
            - userId
            type: object
          status:
            description: LiteLLMUserStatus defines the observed state of LiteLLMUser.
            properties:
              conditions:
                description: Standard conditions.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              currentSpend:
                description: Current spend in USD.
                type: number
              lastSyncTime:
                description: Last successful sync time.
                format: date-time
                type: string
              litellmUserId:
                description: LiteLLM-assigned user ID.
                type: string
              resolvedTeams:
                description: Teams the user is a member of (resolved).
                items:
                  description: ResolvedTeamMembership defines a resolved team membership.
                  properties:
                    role:
                      description: Role within the team.
                      type: string
                    synced:
                      description: Whether the membership is synced.
                      type: boolean
                    teamAlias:
                      description: Team alias.
                      type: string
                    teamId:
                      description: Team ID.
                      type: string
                  required:
                  - role
                  - synced
                  - teamId
                  type: object
                type: array
              synced:
                description: Whether the user is synced to LiteLLM.
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
