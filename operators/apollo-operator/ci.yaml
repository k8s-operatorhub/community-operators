# Use `replaces-mode` or `semver-mode`. Once you switch to `semver-mode`, do not switch back.# Use `replaces-mode` or `semver-mode`. Once you switch to `semver-mode`, there is no easy way back.
updateGraph: replaces-mode
reviewers:
  - jonathanrainer
  - dotdat
  - morriswchris
  - dmallare
