apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  creationTimestamp: null
  name: pools.storage.simplyblock.io
spec:
  group: storage.simplyblock.io
  names:
    kind: Pool
    listKind: PoolList
    plural: pools
    singular: pool
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Pool is the Schema for the pools API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Pool
            properties:
              action:
                description: |-
                  Action triggers an imperative pool operation.
                  FIXME: Unused for now
                type: string
              capacityLimit:
                description: CapacityLimit is the maximum pool capacity.
                type: string
              clusterName:
                description: ClusterName is the target storage cluster name.
                type: string
              name:
                description: Name is the backend pool name.
                type: string
              qos:
                description: QosSpec defines QosSpec limits for the pool.
                properties:
                  iops:
                    description: IOPS is the IOPS limit for the pool.
                    format: int32
                    type: integer
                  throughput:
                    description: Throughput contains throughput limits for the pool.
                    properties:
                      read:
                        description: Read is the read throughput limit for the pool.
                        format: int32
                        type: integer
                      readWrite:
                        description: ReadWrite is the combined read/write throughput
                          limit for the pool.
                        format: int32
                        type: integer
                      write:
                        description: Write is the write throughput limit for the pool.
                        format: int32
                        type: integer
                    type: object
                type: object
              status:
                description: |-
                  Status is an optional desired-status hint for backend workflows.
                  FIXME: Unused for now
                type: string
              storageClassParameters:
                default: {}
                description: StorageClassParameters sets default StorageClass parameter
                  values for volumes in this pool.
                properties:
                  compression:
                    default: "False"
                    description: Compression enables compression for logical volumes.
                    type: string
                  encryption:
                    default: false
                    description: Encryption enables encryption for logical volumes.
                    type: boolean
                  fabric:
                    default: tcp
                    description: Fabric is the transport fabric (e.g. tcp).
                    type: string
                  lvolPriorityClass:
                    default: "0"
                    description: LvolPriorityClass sets the logical volume priority
                      class.
                    type: string
                  maxNamespacePerSubsys:
                    default: "1"
                    description: MaxNamespacePerSubsys limits namespaces per NVMf
                      subsystem.
                    type: string
                  numDataChunks:
                    default: "1"
                    description: NumDataChunks is the number of data chunks (distr_ndcs).
                    type: string
                  numParityChunks:
                    default: "1"
                    description: NumParityChunks is the number of parity chunks (distr_npcs).
                    type: string
                  qosRMbytes:
                    default: "0"
                    description: QosRMbytes sets the read throughput limit in MB/s
                      (0 = unlimited).
                    type: string
                  qosRwIops:
                    default: "0"
                    description: QosRwIops sets the read/write IOPS limit (0 = unlimited).
                    type: string
                  qosRwMbytes:
                    default: "0"
                    description: QosRwMbytes sets the read/write throughput limit
                      in MB/s (0 = unlimited).
                    type: string
                  qosWMbytes:
                    default: "0"
                    description: QosWMbytes sets the write throughput limit in MB/s
                      (0 = unlimited).
                    type: string
                  replicate:
                    default: false
                    description: Replicate enables replication for logical volumes.
                    type: boolean
                  tune2fsReservedBlocks:
                    default: "0"
                    description: Tune2fsReservedBlocks sets the ext4 reserved-blocks
                      percentage.
                    type: string
                type: object
            required:
            - clusterName
            - name
            type: object
          status:
            description: status defines the observed state of Pool
            properties:
              qos:
                description: QoS contains observed/configured QoS values.
                properties:
                  host:
                    description: Host is the backend host handling pool QosSpec enforcement.
                    type: string
                  iops:
                    description: IOPS is the observed/configured IOPS value.
                    format: int32
                    type: integer
                  throughput:
                    description: Throughput contains observed/configured throughput
                      values.
                    properties:
                      read:
                        description: Read is the observed/configured read throughput
                          value.
                        format: int32
                        type: integer
                      readWrite:
                        description: ReadWrite is the observed/configured combined
                          read/write throughput value.
                        format: int32
                        type: integer
                      write:
                        description: Write is the observed/configured write throughput
                          value.
                        format: int32
                        type: integer
                    type: object
                type: object
              status:
                description: Status is the backend lifecycle status.
                type: string
              uuid:
                description: UUID is the backend pool UUID.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
