---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: certificatemonitors.certmonitor.nachoperator.io
spec:
  group: certmonitor.nachoperator.io
  names:
    kind: CertificateMonitor
    listKind: CertificateMonitorList
    plural: certificatemonitors
    singular: certificatemonitor
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.renewalThresholdDays
      name: Threshold
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CertificateMonitor is the Schema for the certificatemonitors
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CertificateMonitorSpec defines the desired state of CertificateMonitor
            properties:
              emailRecipients:
                description: EmailRecipients is a list of email addresses to send
                  notifications.
                items:
                  type: string
                type: array
              namespaceSelector:
                description: NamespaceSelector specifies the namespaces to analyze.
                  If empty, all namespaces are analyzed.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              notificationEndpoint:
                description: NotificationEndpoint is the endpoint to send alerts (e.g.,
                  Opsgenie webhook).
                type: string
              notificationNamespaceSelector:
                description: |-
                  NotificationNamespaceSelector specifies a label selector for namespaces.
                  Notifications for Webhook (Slack) and Opsgenie will be restricted to
                  certificates found within namespaces matching this selector.
                  If this field is nil or the selector is empty, it may affect how these notifications are scoped
                  (e.g., potentially all matching namespaces if the selector is empty like {}, or skipped if nil).
                  The controller logic will determine precise behavior for nil/empty cases.
                  Email notifications remain governed by the global NamespaceSelector.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              opsgenie:
                description: Opsgenie contains Opsgenie notification configuration.
                properties:
                  apiKeySecretRef:
                    description: |-
                      APIKeySecretRef is a reference to the Kubernetes Secret
                      containing the Opsgenie API key.
                      Required if Opsgenie is enabled.
                    properties:
                      key:
                        description: Key is the key within the Secret's Data map that
                          contains the API key.
                        type: string
                      name:
                        description: Name is the name of the Kubernetes Secret.
                        type: string
                      namespace:
                        description: |-
                          Namespace is the namespace of the Kubernetes Secret.
                          Defaults to the CertificateMonitor's namespace if not specified.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  enabled:
                    description: Enabled specifies whether Opsgenie notifications
                      are enabled.
                    type: boolean
                  priority:
                    default: P3
                    description: Priority for the Opsgenie alert (e.g., P1, P2, P3,
                      P4, P5). Defaults to P3.
                    type: string
                  region:
                    description: |-
                      Region specifies the Opsgenie API region (e.g., "EU", "US").
                      If empty, defaults to the US region API endpoint.
                    type: string
                type: object
              renewalThresholdDays:
                default: 30
                description: RenewalThresholdDays is the number of days before expiration
                  to consider a certificate as "near renewal".
                format: int32
                maximum: 365
                minimum: 1
                type: integer
              requeueInterval:
                description: |-
                  RequeueInterval specifies the desired interval between periodic reconciliations.
                  Use Go duration format string (e.g., "12h", "30m", "24h").
                  If empty or invalid, a default value (12h) will be used by the controller.
                type: string
              secretSelector:
                description: SecretSelector specifies the labels to filter Secrets
                  containing mTLS certificates.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
            required:
            - renewalThresholdDays
            type: object
          status:
            description: CertificateMonitorStatus defines the observed state of CertificateMonitor
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of an object's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastChecked:
                description: LastChecked marks the last time the reconciler ran its
                  check.
                format: date-time
                type: string
              lastNotificationTime:
                description: |-
                  LastNotificationTime records when the last aggregated notification was sent.
                  Used for cooldown logic.
                format: date-time
                type: string
              monitoredCertificates:
                description: MonitoredCertificates contains the list of certificates
                  found and their status.
                items:
                  description: MonitoredCertificateInfo contains details about a specific
                    monitored certificate.
                  properties:
                    daysUntilExpiration:
                      description: Approximate remaining days until expiration.
                      type: integer
                    expiresAt:
                      description: Expiration date and time of the certificate.
                      format: date-time
                      type: string
                    isExpired:
                      description: Indicates if the certificate is already expired.
                      type: boolean
                    isNearExpiration:
                      description: Indicates if the certificate is within the renewal
                        threshold (and not already expired).
                      type: boolean
                    issuerCN:
                      description: Common Name (CN) from the certificate's Issuer.
                      type: string
                    namespace:
                      description: Namespace where the Secret containing the certificate
                        is located.
                      type: string
                    secretKey:
                      description: Key within Secret.Data containing the certificate
                        (e.g., tls.crt, ca.crt).
                      type: string
                    secretName:
                      description: Name of the Secret containing the certificate.
                      type: string
                    subjectCN:
                      description: Common Name (CN) from the certificate's Subject.
                      type: string
                  required:
                  - daysUntilExpiration
                  - expiresAt
                  - namespace
                  - secretKey
                  - secretName
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
