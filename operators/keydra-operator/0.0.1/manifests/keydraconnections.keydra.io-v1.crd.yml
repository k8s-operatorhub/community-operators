# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: "apiextensions.k8s.io/v1"
kind: "CustomResourceDefinition"
metadata:
  name: "keydraconnections.keydra.io"
spec:
  group: "keydra.io"
  names:
    kind: "KeydraConnection"
    plural: "keydraconnections"
    shortNames:
    - "kdc"
    singular: "keydraconnection"
  scope: "Namespaced"
  versions:
  - additionalPrinterColumns:
    - jsonPath: ".metadata.creationTimestamp"
      name: "Age"
      priority: 0
      type: "date"
    - jsonPath: ".spec.host"
      name: "Target"
      priority: 0
      type: "string"
    - jsonPath: ".spec.keydraRef"
      name: "Keydra"
      priority: 0
      type: "string"
    - jsonPath: ".status.conditions[?(@.type==\"Registered\")].status"
      name: "Registered"
      priority: 0
      type: "string"
    - jsonPath: ".status.flavor"
      name: "Flavor"
      priority: 0
      type: "string"
    name: "v1alpha1"
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              consoleAllowed:
                description: "Commands the console may run on this target that it\
                  \ refuses elsewhere. Only the ones refused because of what they\
                  \ do to the target can be named; the ones refused because of what\
                  \ they would do to Keydra's own connection are the same everywhere."
                items:
                  type: "string"
                type: "array"
              database:
                description: "Which numbered database, where the store has them."
                minimum: 0.0
                type: "integer"
              engine:
                enum:
                - "AEROSPIKE"
                - "RESP"
                - "TIKV"
                type: "string"
              guarded:
                description: "Whether an operation that could empty this target has\
                  \ to name it first. Off by default and never inferred: a target\
                  \ is not production because its name says so."
                type: "boolean"
              host:
                type: "string"
              keydraRef:
                description: "The Keydra resource in this namespace that should hold\
                  \ this profile. Cross-namespace is deliberately not possible: it\
                  \ would let anybody who can create a resource in their own namespace\
                  \ add a target to somebody else's console."
                type: "string"
              namespace:
                description: "Aerospike's namespace. Not this resource's Kubernetes\
                  \ namespace."
                type: "string"
              notes:
                type: "string"
              passwordSecret:
                description: "Where the password is. A reference and never a value:\
                  \ a password in a custom resource is readable by anybody who can\
                  \ read the resource."
                properties:
                  key:
                    type: "string"
                  name:
                    type: "string"
                  optional:
                    type: "boolean"
                type: "object"
              port:
                maximum: 65535.0
                minimum: 1.0
                type: "integer"
              profileName:
                description: "The profile's name in Keydra. Empty uses this resource's\
                  \ own name, which is the right default — two names for one thing\
                  \ is two names to keep in step."
                type: "string"
              requiresApproval:
                description: "Whether an operation that could empty this target waits\
                  \ for a second person. Beside the naming rather than inside it:\
                  \ one asks which server this is and the other asks whether it should\
                  \ happen at all."
                type: "boolean"
              sentinelMasterName:
                description: "Required when type is SENTINEL."
                type: "string"
              tls:
                type: "boolean"
              tlsCaCert:
                description: "The authority to trust for this target, as PEM. Empty\
                  \ leaves the JVM's own store applying."
                type: "string"
              tlsClientCert:
                description: "The certificate to present when the target asks for\
                  \ one, as PEM."
                type: "string"
              tlsClientKeyPassphraseSecret:
                description: "Where the passphrase that opens that key is, if it has\
                  \ one."
                properties:
                  key:
                    type: "string"
                  name:
                    type: "string"
                  optional:
                    type: "boolean"
                type: "object"
              tlsClientKeySecret:
                description: "Where its private half is. A reference, for the same\
                  \ reason."
                properties:
                  key:
                    type: "string"
                  name:
                    type: "string"
                  optional:
                    type: "boolean"
                type: "object"
              type:
                enum:
                - "CLUSTER"
                - "SENTINEL"
                - "STANDALONE"
                type: "string"
              username:
                type: "string"
            required:
            - "host"
            - "keydraRef"
            - "port"
            type: "object"
            x-kubernetes-validations:
            - message: "type is SENTINEL and sentinelMasterName is empty. A sentinel\
                \ is asked which server it is watching by name; without one there\
                \ is nothing to ask it for."
              rule: "!has(self.type) || self.type != \"SENTINEL\" || (has(self.sentinelMasterName)\
                \ && self.sentinelMasterName != \"\")"
            - message: "engine is AEROSPIKE and namespace is empty. An Aerospike record\
                \ is identified by a namespace, a set and a key; the first of those\
                \ is not optional."
              rule: "!has(self.engine) || self.engine != \"AEROSPIKE\" || (has(self.namespace)\
                \ && self.namespace != \"\")"
          status:
            properties:
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      type: "string"
                    message:
                      type: "string"
                    observedGeneration:
                      type: "integer"
                    reason:
                      type: "string"
                    status:
                      type: "string"
                    type:
                      type: "string"
                  type: "object"
                type: "array"
              flavor:
                type: "string"
              observedGeneration:
                type: "integer"
              profileId:
                type: "integer"
              serverVersion:
                type: "string"
            type: "object"
        type: "object"
    served: true
    storage: true
    subresources:
      status: {}
