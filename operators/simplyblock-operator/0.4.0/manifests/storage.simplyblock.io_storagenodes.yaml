apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  creationTimestamp: null
  name: storagenodes.storage.simplyblock.io
spec:
  group: storage.simplyblock.io
  names:
    kind: StorageNode
    listKind: StorageNodeList
    plural: storagenodes
    singular: storagenode
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: StorageNode is the Schema for the storagenodes API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of StorageNode
            properties:
              action:
                description: Action triggers an imperative node operation.
                enum:
                - shutdown
                - restart
                - suspend
                - resume
                - remove
                type: string
              addPcieToAllowList:
                description: |-
                  AddPcieToAllowList appends devices to the allow-list during restart actions.
                  FIXME: Unused for now
                items:
                  type: string
                type: array
              clusterImage:
                description: ClusterImage is the container image used for storage-node
                  workloads.
                type: string
              clusterName:
                description: ClusterName is the target storage cluster name.
                type: string
              coreIsolation:
                description: CoreIsolation enables CPU core isolation mode.
                type: boolean
              corePercentage:
                description: CorePercentage is the percentage of cores to be used
                  for spdk (0-99).
                format: int32
                type: integer
              dataIfname:
                description: DataIfname lists data-plane network interfaces.
                items:
                  type: string
                type: array
              deviceNames:
                description: DeviceNames explicitly defines a comma separated list
                  of nvme namespace names like nvme0n1,nvme1n1...
                items:
                  type: string
                type: array
              driveSizeRange:
                description: DriveSizeRange filters devices by size range.
                type: string
              enableCpuTopology:
                description: EnableCpuTopology enables topology-aware CPU handling.
                type: boolean
              force:
                description: |-
                  Force enables forced action execution where supported.
                  FIXME: Unused for now
                type: boolean
              forceFormat4K:
                description: ForceFormat4K forces 4K blocksize formatting of the NVMe
                  device where supported.
                type: boolean
              journalManager:
                description: JournalManagerSpec configures journal manager behavior.
                properties:
                  count:
                    description: Count is the number of journal managers to configure.
                    format: int32
                    type: integer
                  percentPerDevice:
                    description: PercentPerDevice is the journal manager capacity
                      percentage per device.
                    format: int32
                    type: integer
                  useSeparateJournalDevice:
                    description: |-
                      UseSeparateJournalDevice enables using separate journal devices.
                      FIXME: Unused for now
                    type: boolean
                type: object
              maxLogicalVolumeCount:
                description: MaxLogicalVolumeCount is the maximum number of logical
                  volumes per node.
                format: int32
                type: integer
              maxSize:
                description: MaxSize is the maximum allocatable size of the storage
                  node.
                type: string
              mgmtIfname:
                description: MgmtIfname is the management interface name used by storage
                  nodes.
                type: string
              nodeAddr:
                description: |-
                  NodeAddr is the explicit node address used by action flows.
                  FIXME: Unused for now
                type: string
              nodeUUID:
                description: NodeUUID is required when action is specified
                type: string
              nodesPerSocket:
                description: NodesPerSocket defines how many storage nodes are created
                  per NUMA socket.
                format: int32
                type: integer
              openShiftCluster:
                description: OpenShiftCluster indicates OpenShift-specific behavior
                  should be enabled.
                type: boolean
              partitions:
                description: Partitions is the number of partitions created per backend
                  storage device.
                format: int32
                type: integer
              pcieAllowList:
                description: PcieAllowList is the list of PCI addresses allowed for
                  use.
                items:
                  type: string
                type: array
              pcieDenyList:
                description: PcieDenyList is the list of PCI addresses excluded from
                  use.
                items:
                  type: string
                type: array
              pcieModel:
                description: PcieModel filters devices by PCI model.
                type: string
              reservedSystemCPU:
                description: ReservedSystemCPU defines CPUs reserved for system workloads.
                type: string
              skipKubeletConfiguration:
                description: SkipKubeletConfiguration skips kubelet configuration
                  changes.
                type: boolean
              socketsToUse:
                description: SocketsToUse restricts deployment to selected NUMA sockets.
                items:
                  type: string
                type: array
              spdkImage:
                description: SpdkImage is the SPDK image reference used by node services.
                type: string
              spdkProxyImage:
                description: SpdkProxyImage is the SPDK proxy image reference used
                  by node services.
                type: string
              tolerations:
                description: Tolerations configures pod tolerations for storage-node
                  pods.
                items:
                  description: |-
                    The pod this Toleration is attached to tolerates any taint that matches
                    the triple <key,value,effect> using the matching operator <operator>.
                  properties:
                    effect:
                      description: |-
                        Effect indicates the taint effect to match. Empty means match all taint effects.
                        When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                      type: string
                    key:
                      description: |-
                        Key is the taint key that the toleration applies to. Empty means match all taint keys.
                        If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                      type: string
                    operator:
                      description: |-
                        Operator represents a key's relationship to the value.
                        Valid operators are Exists and Equal. Defaults to Equal.
                        Exists is equivalent to wildcard for value, so that a pod can
                        tolerate all taints of a particular category.
                      type: string
                    tolerationSeconds:
                      description: |-
                        TolerationSeconds represents the period of time the toleration (which must be
                        of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                        it is not set, which means tolerate the taint forever (do not evict). Zero and
                        negative values will be treated as 0 (evict immediately) by the system.
                      format: int64
                      type: integer
                    value:
                      description: |-
                        Value is the taint value the toleration matches to.
                        If the operator is Exists, the value should be empty, otherwise just a regular string.
                      type: string
                  type: object
                type: array
              ubuntuHost:
                description: UbuntuHost indicates the node host OS is Ubuntu.
                type: boolean
              workerNode:
                description: WorkerNode is a single worker node used by action flows.
                type: string
              workerNodes:
                description: WorkerNodes is the set of Kubernetes worker nodes to
                  manage.
                items:
                  type: string
                type: array
            required:
            - clusterName
            type: object
          status:
            description: status defines the observed state of StorageNode
            properties:
              actionStatus:
                description: ActionStatus tracks the latest action execution status.
                properties:
                  action:
                    description: Action is the requested action name.
                    type: string
                  message:
                    description: Message is a human-readable action result or error.
                    type: string
                  nodeUUID:
                    description: NodeUUID is the target node UUID for the action.
                    type: string
                  observedGeneration:
                    description: ObservedGeneration is the resource generation observed
                      by this status.
                    format: int64
                    type: integer
                  state:
                    type: string
                  triggered:
                    description: Triggered indicates whether the underlying backend
                      action has been fired.
                    type: boolean
                  updatedAt:
                    description: UpdatedAt is the timestamp of the last status transition.
                    format: date-time
                    type: string
                type: object
              drainCoordination:
                description: DrainCoordination tracks the upgrade-drain state per
                  worker node.
                items:
                  description: NodeDrainState tracks the upgrade-drain coordination
                    state for a single worker node.
                  properties:
                    activeNodeUUID:
                      description: |-
                        ActiveNodeUUID is the backend UUID of the storage node currently being shut
                        down or restarted. Used to sequence through multiple NUMA-socket nodes on
                        the same worker one at a time during drain coordination.
                      type: string
                    hostname:
                      description: Hostname is the Kubernetes node name.
                      type: string
                    message:
                      description: Message provides additional status detail or error
                        information.
                      type: string
                    phase:
                      description: Phase is the current drain coordination phase.
                      enum:
                      - detected
                      - shutdown_called
                      - draining
                      - restart_called
                      - complete
                      - failed
                      type: string
                    startedAt:
                      description: StartedAt is when drain coordination began for
                        this node.
                      format: date-time
                      type: string
                  required:
                  - hostname
                  - phase
                  - startedAt
                  type: object
                type: array
              nodes:
                description: Nodes is the observed state of each managed storage node.
                items:
                  properties:
                    cpu:
                      description: CPU is the reported CPU allocation/count for the
                        node.
                      format: int32
                      type: integer
                    devices:
                      description: Devices is the backend summary of devices on this
                        node.
                      type: string
                    health:
                      description: Health indicates whether health checks are currently
                        passing.
                      type: boolean
                    hostname:
                      description: Hostname is the Kubernetes node hostname.
                      type: string
                    lvolPort:
                      description: LvolPort is the logical-volume subsystem port.
                      format: int32
                      type: integer
                    memory:
                      description: Memory is the reported memory value.
                      type: string
                    mgmtIp:
                      description: MgmtIp is the management IP address for the node.
                      type: string
                    nvmfPort:
                      description: NvmfPort is the NVMf service port.
                      format: int32
                      type: integer
                    postedAt:
                      description: |-
                        PostedAt is when the storage-node add request was sent. Used to detect
                        timeout without blocking the reconcile goroutine.
                      format: date-time
                      type: string
                    rpcPort:
                      description: RpcPort is the node RPC service port.
                      format: int32
                      type: integer
                    status:
                      description: Status is the backend lifecycle state for the node.
                      type: string
                    uptime:
                      description: Uptime is the reported node uptime value.
                      type: string
                    uuid:
                      description: UUID is the backend node UUID.
                      type: string
                    volumes:
                      description: Volumes is the current logical volume count.
                      format: int32
                      type: integer
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
        x-kubernetes-validations:
        - message: nodeUUID is required when action is specified
          rule: '!(has(self.spec.action) && self.spec.action != "" && (!has(self.spec.nodeUUID)
            || self.spec.nodeUUID == ""))'
        - message: clusterImage, maxLogicalVolumeCount, and workerNodes are required
            when action is not specified
          rule: (has(self.spec.action) && self.spec.action != "") || (has(self.spec.clusterImage)
            && self.spec.clusterImage != "" && has(self.spec.maxLogicalVolumeCount)
            && has(self.spec.workerNodes) && size(self.spec.workerNodes) > 0)
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
