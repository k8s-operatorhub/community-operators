apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  creationTimestamp: null
  name: sitebackups.vyogo.tech
spec:
  group: vyogo.tech
  names:
    kind: SiteBackup
    listKind: SiteBackupList
    plural: sitebackups
    singular: sitebackup
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: SiteBackup is the Schema for the sitebackups API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SiteBackupSpec defines the desired state of SiteBackup
            properties:
              backupPath:
                description: |-
                  BackupPath specifies the path to save the backup files
                  If empty, uses the default site backup location
                type: string
              backupPathConf:
                description: BackupPathConf specifies the path to save the config
                  file
                type: string
              backupPathDB:
                description: BackupPathDB specifies the path to save the database
                  file
                type: string
              backupPathFiles:
                description: BackupPathFiles specifies the path to save the public
                  file
                type: string
              backupPathPrivateFiles:
                description: BackupPathPrivateFiles specifies the path to save the
                  private file
                type: string
              compress:
                default: false
                description: Compress compresses the backup files
                type: boolean
              exclude:
                description: Exclude specifies the DocTypes to not backup, separated
                  by commas
                items:
                  type: string
                type: array
              ignoreBackupConf:
                default: false
                description: IgnoreBackupConf ignores excludes/includes set in config
                type: boolean
              include:
                description: Include specifies the DocTypes to backup, separated by
                  commas
                items:
                  type: string
                type: array
              schedule:
                description: |-
                  Schedule is a cron expression for scheduled backups (e.g., "0 2 * * *")
                  If empty, performs a one-time backup
                type: string
              site:
                description: Site is the name of the Frappe site to backup
                type: string
              storage:
                description: Storage configures where to store the backup
                properties:
                  pvc:
                    description: PVC configuration (future use)
                    type: string
                  s3:
                    description: S3 configuration
                    properties:
                      accessKeySecret:
                        description: AccessKeySecret references a secret key containing
                          the Access Key ID
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      bucket:
                        description: Bucket name
                        type: string
                      endpoint:
                        description: Endpoint is the S3 service endpoint (e.g., "https://s3.amazonaws.com"
                          or minio URL)
                        type: string
                      region:
                        description: Region (standard S3 region)
                        type: string
                      secretKeySecret:
                        description: SecretKeySecret references a secret key containing
                          the Secret Access Key
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      useSSL:
                        default: true
                        description: UseSSL enables SSL/TLS for the connection
                        type: boolean
                    required:
                    - accessKeySecret
                    - bucket
                    - endpoint
                    - secretKeySecret
                    type: object
                  type:
                    default: pvc
                    description: 'Type of storage: s3 or pvc'
                    enum:
                    - s3
                    - pvc
                    type: string
                type: object
              verbose:
                default: false
                description: Verbose adds verbosity to the backup process
                type: boolean
              withFiles:
                default: false
                description: WithFiles includes private and public files in the backup
                type: boolean
            required:
            - site
            type: object
          status:
            description: SiteBackupStatus defines the observed state of SiteBackup
            properties:
              lastBackup:
                description: LastBackup is the timestamp of the last successful backup
                format: date-time
                type: string
              lastBackupJob:
                description: LastBackupJob is the name of the last backup job or cronjob
                type: string
              message:
                description: Message provides additional information about the backup
                  status
                type: string
              phase:
                description: Phase indicates the current phase of the backup
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
