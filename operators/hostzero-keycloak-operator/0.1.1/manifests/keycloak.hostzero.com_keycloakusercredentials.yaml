---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: keycloakusercredentials.keycloak.hostzero.com
spec:
  group: keycloak.hostzero.com
  names:
    categories:
    - keycloak
    - all
    kind: KeycloakUserCredential
    listKind: KeycloakUserCredentialList
    plural: keycloakusercredentials
    shortNames:
    - kcuc
    singular: keycloakusercredential
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Whether the credentials are synchronized
      jsonPath: .status.ready
      name: Ready
      type: boolean
    - description: Status message
      jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: KeycloakUserCredential manages credentials for a KeycloakUser
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KeycloakUserCredentialSpec defines the desired state of KeycloakUserCredential
            properties:
              userRef:
                description: UserRef is a reference to a KeycloakUser
                properties:
                  name:
                    description: Name of the resource
                    type: string
                  namespace:
                    description: Namespace of the resource (optional, defaults to
                      the same namespace)
                    type: string
                required:
                - name
                type: object
              userSecret:
                description: UserSecret defines the secret containing the credentials
                properties:
                  create:
                    description: Create indicates whether to create the secret if
                      it doesn't exist
                    type: boolean
                  emailKey:
                    description: EmailKey is the key for the email in the secret
                    type: string
                  passwordKey:
                    default: password
                    description: PasswordKey is the key for the password in the secret
                    type: string
                  passwordPolicy:
                    description: PasswordPolicy configures password generation
                    properties:
                      includeNumbers:
                        default: true
                        description: IncludeNumbers includes numbers in the password
                        type: boolean
                      includeSymbols:
                        default: true
                        description: IncludeSymbols includes symbols in the password
                        type: boolean
                      length:
                        default: 24
                        description: Length is the password length (default 24)
                        type: integer
                    type: object
                  secretName:
                    description: SecretName is the name of the Kubernetes secret
                    type: string
                  usernameKey:
                    default: username
                    description: UsernameKey is the key for the username in the secret
                    type: string
                required:
                - secretName
                type: object
            required:
            - userRef
            - userSecret
            type: object
          status:
            description: KeycloakUserCredentialStatus defines the observed state of
              KeycloakUserCredential
            properties:
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              instance:
                description: Instance contains the resolved instance reference
                properties:
                  clusterInstanceRef:
                    description: ClusterInstanceRef is the name of the cluster instance
                    type: string
                  instanceRef:
                    description: InstanceRef is the name of the namespaced instance
                    type: string
                type: object
              message:
                description: Message contains additional information
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the spec that
                  was last processed
                format: int64
                type: integer
              passwordHash:
                description: PasswordHash is a hash of the last synchronized password
                  (for change detection)
                type: string
              ready:
                description: Ready indicates if the credentials are synchronized
                type: boolean
              realm:
                description: Realm contains the resolved realm reference
                properties:
                  clusterRealmRef:
                    description: ClusterRealmRef is the name of the cluster realm
                    type: string
                  realmRef:
                    description: RealmRef is the name of the namespaced realm
                    type: string
                type: object
              resourcePath:
                description: ResourcePath is the Keycloak API path for the user
                type: string
              secretCreated:
                description: SecretCreated indicates if the secret was created by
                  the operator
                type: boolean
              secretResourceVersion:
                description: SecretResourceVersion is the resource version of the
                  secret when last synced
                type: string
              status:
                description: Status is a human-readable status message
                type: string
            required:
            - ready
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
