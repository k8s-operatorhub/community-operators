apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  creationTimestamp: null
  name: litellmorganizations.litellm.palena.ai
spec:
  group: litellm.palena.ai
  names:
    kind: LiteLLMOrganization
    listKind: LiteLLMOrganizationList
    plural: litellmorganizations
    shortNames:
    - lo
    singular: litellmorganization
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.organizationAlias
      name: Alias
      type: string
    - jsonPath: .status.memberCount
      name: Members
      type: integer
    - jsonPath: .status.synced
      name: Synced
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LiteLLMOrganization is the Schema for the litellmorganizations
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LiteLLMOrganizationSpec defines the desired state of LiteLLMOrganization.
            properties:
              budgetDuration:
                description: Budget reset interval (e.g., "1d", "7d", "30d").
                type: string
              instanceRef:
                description: Reference to the LiteLLMInstance this organization belongs
                  to.
                properties:
                  name:
                    description: Name of the LiteLLMInstance CR.
                    type: string
                required:
                - name
                type: object
              maxBudget:
                description: Maximum budget in USD.
                type: number
              members:
                description: Organization members.
                items:
                  description: OrganizationMember defines a member of an organization.
                  properties:
                    email:
                      description: User email address.
                      type: string
                    role:
                      default: internal_user
                      description: 'Role within the organization: org_admin or internal_user.'
                      enum:
                      - org_admin
                      - internal_user
                      type: string
                  required:
                  - email
                  type: object
                type: array
              metadata:
                additionalProperties:
                  type: string
                description: Metadata key-value pairs.
                type: object
              models:
                description: List of models this organization can access.
                items:
                  type: string
                type: array
              organizationAlias:
                description: Human-readable alias for the organization.
                type: string
              rpmLimit:
                description: RPM limit for the organization.
                format: int64
                type: integer
              tpmLimit:
                description: TPM limit for the organization.
                format: int64
                type: integer
            required:
            - instanceRef
            - organizationAlias
            type: object
          status:
            description: LiteLLMOrganizationStatus defines the observed state of LiteLLMOrganization.
            properties:
              conditions:
                description: Standard conditions.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              currentSpend:
                description: Current spend in USD.
                type: number
              lastSyncTime:
                description: Last successful sync time.
                format: date-time
                type: string
              litellmOrganizationId:
                description: Organization ID assigned by LiteLLM.
                type: string
              memberCount:
                description: Number of members.
                type: integer
              synced:
                description: Whether the organization is synced to LiteLLM.
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
