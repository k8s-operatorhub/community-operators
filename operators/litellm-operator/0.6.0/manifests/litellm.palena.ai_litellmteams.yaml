apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  creationTimestamp: null
  name: litellmteams.litellm.palena.ai
spec:
  group: litellm.palena.ai
  names:
    kind: LiteLLMTeam
    listKind: LiteLLMTeamList
    plural: litellmteams
    shortNames:
    - lt
    singular: litellmteam
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.teamAlias
      name: Alias
      type: string
    - jsonPath: .status.totalMemberCount
      name: Members
      type: integer
    - jsonPath: .spec.memberManagement
      name: MemberMgmt
      type: string
    - jsonPath: .status.synced
      name: Synced
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LiteLLMTeam is the Schema for the litellmteams API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LiteLLMTeamSpec defines the desired state of LiteLLMTeam.
            properties:
              budgetDuration:
                description: Budget reset duration (e.g., "30d", "7d").
                type: string
              instanceRef:
                description: Reference to the LiteLLMInstance.
                properties:
                  name:
                    description: Name of the LiteLLMInstance CR.
                    type: string
                required:
                - name
                type: object
              maxBudgetMonthly:
                description: Maximum monthly budget in USD.
                type: number
              memberManagement:
                default: mixed
                description: |-
                  Controls who owns team membership.
                    "crd"   — CRD is authoritative. Only listed members exist.
                    "sso"   — IdP is authoritative. spec.members is ignored.
                    "mixed" — CRD members are additive. SSO members are preserved.
                enum:
                - crd
                - sso
                - mixed
                type: string
              members:
                description: |-
                  Team members. Behavior depends on memberManagement mode.
                  Ignored when memberManagement is "sso".
                items:
                  description: TeamMember defines a team member.
                  properties:
                    email:
                      description: User email address.
                      type: string
                    role:
                      default: user
                      description: Role within the team.
                      enum:
                      - admin
                      - user
                      type: string
                  required:
                  - email
                  type: object
                type: array
              metadata:
                additionalProperties:
                  type: string
                description: Custom metadata for the team.
                type: object
              models:
                description: Models this team can access.
                items:
                  type: string
                type: array
              rpmLimit:
                description: RPM limit for the team.
                type: integer
              teamAlias:
                description: Human-readable team alias.
                type: string
              teamMemberRpmLimit:
                description: RPM limit per team member.
                type: integer
              teamMemberTpmLimit:
                description: TPM limit per team member.
                type: integer
              tpmLimit:
                description: TPM limit for the team.
                type: integer
            required:
            - instanceRef
            - teamAlias
            type: object
          status:
            description: LiteLLMTeamStatus defines the observed state of LiteLLMTeam.
            properties:
              conditions:
                description: Standard conditions.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              crdMembers:
                description: Members managed by this CRD.
                items:
                  description: TeamMemberStatus defines the status of a team member.
                  properties:
                    email:
                      description: Member email.
                      type: string
                    role:
                      description: Member role.
                      type: string
                    source:
                      description: Source of the member ("crd", "azure-entra", "okta",
                        etc.).
                      type: string
                    synced:
                      description: Whether the member is synced.
                      type: boolean
                  required:
                  - email
                  - role
                  type: object
                type: array
              currentSpend:
                description: Current spend in USD.
                type: number
              lastSyncTime:
                description: Last successful sync time.
                format: date-time
                type: string
              litellmTeamId:
                description: LiteLLM-assigned team ID.
                type: string
              ssoMembers:
                description: Members provisioned by SSO/SCIM (not managed by CRD).
                items:
                  description: TeamMemberStatus defines the status of a team member.
                  properties:
                    email:
                      description: Member email.
                      type: string
                    role:
                      description: Member role.
                      type: string
                    source:
                      description: Source of the member ("crd", "azure-entra", "okta",
                        etc.).
                      type: string
                    synced:
                      description: Whether the member is synced.
                      type: boolean
                  required:
                  - email
                  - role
                  type: object
                type: array
              synced:
                description: Whether the team is synced to LiteLLM.
                type: boolean
              totalMemberCount:
                description: Total member count (CRD + SSO).
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
