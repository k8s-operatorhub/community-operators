apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  creationTimestamp: null
  name: replicationpairs.storage.simplyblock.io
spec:
  group: storage.simplyblock.io
  names:
    kind: ReplicationPair
    listKind: ReplicationPairList
    plural: replicationpairs
    shortNames:
    - relpair
    singular: replicationpair
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.sourceCluster
      name: Source
      type: string
    - jsonPath: .spec.targetCluster
      name: Target
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ReplicationPair defines the source and target clusters for a replication relationship.
          It is reusable configuration — multiple ReplicationPolicies may reference the same pair
          to replicate volumes between the same two clusters with different schedules or retention.
          The operator ensures the corresponding backend ReplicationTarget exists and stores its ID
          in status.backendTargetID for use by ReplicationPolicy resources.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ReplicationPairSpec defines the source and target clusters
              for a replication relationship.
            properties:
              sourceCluster:
                description: SourceCluster is the name of the local StorageCluster
                  (the replication source).
                type: string
              targetCluster:
                description: |-
                  TargetCluster is the name or UUID of the remote cluster (the replication target).
                  Immutable after creation.
                type: string
                x-kubernetes-validations:
                - message: targetCluster is immutable
                  rule: self == oldSelf
            required:
            - sourceCluster
            - targetCluster
            type: object
          status:
            description: ReplicationPairStatus holds the observed state of a ReplicationPair.
            properties:
              activeOpsRef:
                description: |-
                  ActiveOpsRef is the name of the ReplicationOps currently holding the
                  target-scope lock on this pair. Only one scope=target ReplicationOps may
                  be active per pair at a time.
                type: string
              backendTargetID:
                description: BackendTargetID is the UUID of the backend ReplicationTarget
                  resource.
                type: string
              conditions:
                description: Conditions holds standard Kubernetes condition types.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              message:
                description: Message provides a human-readable description of the
                  current state.
                type: string
              ready:
                description: Ready is true when the backend ReplicationTarget has
                  been created and is available.
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
