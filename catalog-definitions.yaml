schema: olm.composite.catalogs
catalogs:
- name: v4.10
  destination:
    workingDir: ../../catalogs/v4.10 # relative to operator location
  builders:
    - olm.builder.basic
    - olm.builder.semver
- name: v4.11
  destination:
    workingDir: ../../catalogs/v4.11 # relative to operator location
  builders:
    - olm.builder.basic
    - olm.builder.semver
- name: v4.12
  destination:
    workingDir: ../../catalogs/v4.12 # relative to operator location
  builders:
    - olm.builder.basic
    - olm.builder.semver
- name: v4.13
  destination:
    workingDir: ../../catalogs/v4.13 # relative to operator location
  builders:
    - olm.builder.basic
    - olm.builder.semver
- name: v4.14
  destination:
    workingDir: ../../catalogs/v4.14 # relative to operator location
  builders:
    - olm.builder.basic
    - olm.builder.semver
- name: v4.15
  destination:
    workingDir: ../../catalogs/v4.15 # relative to operator location
  builders:
    - olm.builder.basic
    - olm.builder.semver
- name: v4.16
  destination:
    workingDir: ../../catalogs/v4.16 # relative to operator location
  builders:
    - olm.builder.basic
    - olm.builder.semver
