apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  creationTimestamp: null
  name: krkntargetrequests.krkn.krkn-chaos.dev
spec:
  group: krkn.krkn-chaos.dev
  names:
    kind: KrknTargetRequest
    listKind: KrknTargetRequestList
    plural: krkntargetrequests
    shortNames:
    - ktr
    singular: krkntargetrequest
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .spec.uuid
      name: UUID
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: KrknTargetRequest is the Schema for the krkntargetrequests API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KrknTargetRequestSpec defines the desired state of KrknTargetRequest.
            properties:
              uuid:
                description: |-
                  UUID is a unique identifier for this request.
                  The operator will automatically add a label 'krkn.krkn-chaos.dev/uuid' with this value
                  for easy selection: kubectl get krkntargetrequests -l krkn.krkn-chaos.dev/uuid=<uuid>
                type: string
            required:
            - uuid
            type: object
          status:
            description: KrknTargetRequestStatus defines the observed state of KrknTargetRequest.
            properties:
              completed:
                description: Completed is the timestamp when the CR was marked as
                  completed
                format: date-time
                type: string
              status:
                description: Status represents the current state of the request (pending,
                  completed)
                type: string
              targetData:
                additionalProperties:
                  items:
                    description: ClusterTarget represents the target cluster information
                    properties:
                      cluster-api-url:
                        description: ClusterAPIURL is the API server URL of the managed
                          cluster
                        type: string
                      cluster-name:
                        description: ClusterName is the name of the managed cluster
                        type: string
                    required:
                    - cluster-api-url
                    - cluster-name
                    type: object
                  type: array
                description: |-
                  TargetData contains a map of operator-name to list of cluster targets
                  This allows multiple operators to contribute their targets to the same request
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
