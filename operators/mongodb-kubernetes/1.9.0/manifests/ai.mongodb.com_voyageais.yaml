apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  creationTimestamp: null
  name: voyageais.ai.mongodb.com
spec:
  group: ai.mongodb.com
  names:
    kind: VoyageAI
    listKind: VoyageAIList
    plural: voyageais
    shortNames:
    - vai
    singular: voyageai
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Current state of the VoyageAI deployment.
      jsonPath: .status.phase
      name: Phase
      type: string
    - description: VoyageAI version reconciled by the operator.
      jsonPath: .status.version
      name: Version
      type: string
    - description: VoyageAI model.
      jsonPath: .spec.model
      name: Model
      type: string
    - description: The time since the VoyageAI resource was created.
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              dataParallel:
                description: DataParallel configures data parallel processing settings.
                properties:
                  batching:
                    description: Batching configures batching settings for data parallel
                      processing.
                    properties:
                      maxQueueSize:
                        default: 2000
                        description: MaxQueueSize is the maximum number of items in
                          the batch queue.
                        format: int32
                        type: integer
                      maxWaitTimeMs:
                        default: 10
                        description: MaxWaitTimeMs is the maximum time in milliseconds
                          to wait for a batch to fill.
                        format: int32
                        type: integer
                      strategy:
                        default: simple
                        description: Strategy is the batching strategy to use.
                        enum:
                        - simple
                        - time_window
                        type: string
                    type: object
                  enabled:
                    description: Enabled controls whether data parallel processing
                      is enabled.
                    type: boolean
                  healthMonitoring:
                    description: HealthMonitoring configures health monitoring settings
                      for data parallel workers.
                    properties:
                      activeCheckIntervalSeconds:
                        default: 60
                        description: ActiveCheckIntervalSeconds is the interval in
                          seconds between active health checks.
                        format: int32
                        type: integer
                      activeCheckTimeoutSeconds:
                        default: 5
                        description: ActiveCheckTimeoutSeconds is the timeout in seconds
                          for active health checks.
                        format: int32
                        type: integer
                      checkIntervalSeconds:
                        default: 5
                        description: CheckIntervalSeconds is the interval in seconds
                          between health checks.
                        format: int32
                        type: integer
                      enableActiveChecks:
                        default: false
                        description: EnableActiveChecks controls whether active health
                          checks are enabled.
                        type: boolean
                      maxConsecutiveTimeouts:
                        default: 3
                        description: MaxConsecutiveTimeouts is the maximum number
                          of consecutive timeouts before a worker is considered unhealthy.
                        format: int32
                        type: integer
                      maxRestartAttempts:
                        default: 3
                        description: MaxRestartAttempts is the maximum number of restart
                          attempts for an unhealthy worker.
                        format: int32
                        type: integer
                      restartCooldownSeconds:
                        default: 30
                        description: RestartCooldownSeconds is the cooldown period
                          in seconds between restart attempts.
                        format: int32
                        type: integer
                    type: object
                  loadBalancingStrategy:
                    default: round_robin
                    description: LoadBalancingStrategy is the strategy for distributing
                      work across workers.
                    enum:
                    - round_robin
                    - least_loaded
                    - token_aware
                    type: string
                  numWorkers:
                    description: NumWorkers is the number of data parallel workers.
                      Nil means auto.
                    format: int32
                    type: integer
                  workerExecutionTimeoutSeconds:
                    default: 30
                    description: WorkerExecutionTimeoutSeconds is the timeout in seconds
                      for worker execution.
                    format: int32
                    type: integer
                  workerInitTimeoutSeconds:
                    default: 600
                    description: WorkerInitTimeoutSeconds is the timeout in seconds
                      for worker initialization.
                    format: int32
                    type: integer
                  workerQueueMaxSize:
                    default: 100
                    description: WorkerQueueMaxSize is the maximum size of the worker
                      queue.
                    format: int32
                    type: integer
                type: object
              metrics:
                description: Metrics configures the Prometheus metrics endpoint exposed
                  by the server.
                properties:
                  enabled:
                    default: true
                    description: |-
                      Enabled controls whether the Prometheus metrics endpoint is active.
                      Maps to SERVER__METRICS__ENABLED.
                    type: boolean
                  path:
                    default: /metrics
                    description: |-
                      Path is the HTTP path at which Prometheus metrics are served on the
                      dedicated metrics port.
                      Maps to SERVER__METRICS__PATH.
                    type: string
                  port:
                    default: 9946
                    description: |-
                      Port is the dedicated TCP port for the Prometheus HTTP server
                      (e.g. 9946).
                      Maps to SERVER__METRICS__PORT.
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                type: object
              model:
                description: Model is the VoyageAI model to deploy.
                enum:
                - voyage-4-large
                - voyage-4
                - voyage-4-lite
                - rerank-2.5
                - rerank-2.5-lite
                - voyage-context-3
                - voyage-code-3
                type: string
              nodeAffinity:
                description: NodeAffinity configures node affinity scheduling rules
                  for VoyageAI pods.
                properties:
                  preferredDuringSchedulingIgnoredDuringExecution:
                    description: |-
                      The scheduler will prefer to schedule pods to nodes that satisfy
                      the affinity expressions specified by this field, but it may choose
                      a node that violates one or more of the expressions. The node that is
                      most preferred is the one with the greatest sum of weights, i.e.
                      for each node that meets all of the scheduling requirements (resource
                      request, requiredDuringScheduling affinity expressions, etc.),
                      compute a sum by iterating through the elements of this field and adding
                      "weight" to the sum if the node matches the corresponding matchExpressions; the
                      node(s) with the highest sum are the most preferred.
                    items:
                      description: |-
                        An empty preferred scheduling term matches all objects with implicit weight 0
                        (i.e. it's a no-op). A null preferred scheduling term matches no objects (i.e. is also a no-op).
                      properties:
                        preference:
                          description: A node selector term, associated with the corresponding
                            weight.
                          properties:
                            matchExpressions:
                              description: A list of node selector requirements by
                                node's labels.
                              items:
                                description: |-
                                  A node selector requirement is a selector that contains values, a key, and an operator
                                  that relates the key and values.
                                properties:
                                  key:
                                    description: The label key that the selector applies
                                      to.
                                    type: string
                                  operator:
                                    description: |-
                                      Represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                    type: string
                                  values:
                                    description: |-
                                      An array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. If the operator is Gt or Lt, the values
                                      array must have a single element, which will be interpreted as an integer.
                                      This array is replaced during a strategic merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchFields:
                              description: A list of node selector requirements by
                                node's fields.
                              items:
                                description: |-
                                  A node selector requirement is a selector that contains values, a key, and an operator
                                  that relates the key and values.
                                properties:
                                  key:
                                    description: The label key that the selector applies
                                      to.
                                    type: string
                                  operator:
                                    description: |-
                                      Represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                    type: string
                                  values:
                                    description: |-
                                      An array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. If the operator is Gt or Lt, the values
                                      array must have a single element, which will be interpreted as an integer.
                                      This array is replaced during a strategic merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                          type: object
                          x-kubernetes-map-type: atomic
                        weight:
                          description: Weight associated with matching the corresponding
                            nodeSelectorTerm, in the range 1-100.
                          format: int32
                          type: integer
                      required:
                      - preference
                      - weight
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  requiredDuringSchedulingIgnoredDuringExecution:
                    description: |-
                      If the affinity requirements specified by this field are not met at
                      scheduling time, the pod will not be scheduled onto the node.
                      If the affinity requirements specified by this field cease to be met
                      at some point during pod execution (e.g. due to an update), the system
                      may or may not try to eventually evict the pod from its node.
                    properties:
                      nodeSelectorTerms:
                        description: Required. A list of node selector terms. The
                          terms are ORed.
                        items:
                          description: |-
                            A null or empty node selector term matches no objects. The requirements of
                            them are ANDed.
                            The TopologySelectorTerm type implements a subset of the NodeSelectorTerm.
                          properties:
                            matchExpressions:
                              description: A list of node selector requirements by
                                node's labels.
                              items:
                                description: |-
                                  A node selector requirement is a selector that contains values, a key, and an operator
                                  that relates the key and values.
                                properties:
                                  key:
                                    description: The label key that the selector applies
                                      to.
                                    type: string
                                  operator:
                                    description: |-
                                      Represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                    type: string
                                  values:
                                    description: |-
                                      An array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. If the operator is Gt or Lt, the values
                                      array must have a single element, which will be interpreted as an integer.
                                      This array is replaced during a strategic merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchFields:
                              description: A list of node selector requirements by
                                node's fields.
                              items:
                                description: |-
                                  A node selector requirement is a selector that contains values, a key, and an operator
                                  that relates the key and values.
                                properties:
                                  key:
                                    description: The label key that the selector applies
                                      to.
                                    type: string
                                  operator:
                                    description: |-
                                      Represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                    type: string
                                  values:
                                    description: |-
                                      An array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. If the operator is Gt or Lt, the values
                                      array must have a single element, which will be interpreted as an integer.
                                      This array is replaced during a strategic merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                          type: object
                          x-kubernetes-map-type: atomic
                        type: array
                        x-kubernetes-list-type: atomic
                    required:
                    - nodeSelectorTerms
                    type: object
                    x-kubernetes-map-type: atomic
                type: object
              replicas:
                default: 1
                description: Replicas is the number of VoyageAI pods to deploy.
                format: int32
                minimum: 1
                type: integer
              resourceRequirements:
                description: ResourceRequirements configures resource requests and
                  limits for the VoyageAI container.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This is an alpha field and requires enabling the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              security:
                description: Security configures TLS settings for the VoyageAI server.
                properties:
                  tls:
                    description: TLS configures TLS for the VoyageAI server. When
                      set, TLS is enabled.
                    properties:
                      certificateKeySecretRef:
                        description: |-
                          CertificateKeySecretRef is a reference to a Secret containing a private key and certificate for TLS.
                          The key and cert are expected to be PEM encoded and available at "tls.key" and "tls.crt".
                          This enables server-side TLS only. For mutual TLS (client certificate
                          verification) use a service mesh such as Istio or Linkerd.
                        properties:
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                    required:
                    - certificateKeySecretRef
                    type: object
                type: object
              server:
                default: {}
                description: Server configures the VoyageAI server settings.
                properties:
                  maxRequests:
                    default: 0
                    description: MaxRequests is the maximum number of requests before
                      a worker is restarted.
                    format: int32
                    minimum: 0
                    type: integer
                  maxRequestsJitter:
                    default: 0
                    description: MaxRequestsJitter is the jitter applied to MaxRequests.
                    format: int32
                    minimum: 0
                    type: integer
                  port:
                    default: 8080
                    description: Port is the port the VoyageAI server listens on.
                    format: int32
                    maximum: 65535
                    minimum: 0
                    type: integer
                  timeout:
                    default: 30
                    description: Timeout is the server request timeout in seconds.
                    format: int32
                    minimum: 1
                    type: integer
                  workers:
                    default: 1
                    description: Workers is the number of server workers.
                    format: int32
                    minimum: 1
                    type: integer
                type: object
              version:
                description: |-
                  Version is the version (image tag) of the VoyageAI model image. The full
                  image is composed as <repository>/<model>:<version>, where the repository
                  is configured operator-wide via the MDB_VOYAGEAI_REPO_URL environment
                  variable (defaulting to quay.io/mongodb/voyageai).
                minLength: 1
                type: string
            required:
            - model
            - version
            type: object
          status:
            properties:
              lastTransition:
                type: string
              message:
                type: string
              observedGeneration:
                format: int64
                type: integer
              phase:
                type: string
              pvc:
                items:
                  properties:
                    phase:
                      type: string
                    statefulsetName:
                      type: string
                  required:
                  - phase
                  - statefulsetName
                  type: object
                type: array
              resourcesNotReady:
                items:
                  description: ResourceNotReady describes the dependent resource which
                    is not ready yet
                  properties:
                    errors:
                      items:
                        properties:
                          message:
                            type: string
                          reason:
                            type: string
                        type: object
                      type: array
                    kind:
                      description: ResourceKind specifies a kind of a Kubernetes resource.
                        Used in status of a Custom Resource
                      type: string
                    message:
                      type: string
                    name:
                      type: string
                  required:
                  - kind
                  - name
                  type: object
                type: array
              version:
                type: string
              warnings:
                items:
                  type: string
                type: array
            required:
            - phase
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
