apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  creationTimestamp: null
  name: flows.automation.kubezap.io
spec:
  group: automation.kubezap.io
  names:
    kind: Flow
    listKind: FlowList
    plural: flows
    singular: flow
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Current phase
      jsonPath: .status.conditions[?(@.type==\"Ready\")].status
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Flow defines an ordered workflow of steps with conditional logic
          and data transforms.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FlowSpec defines the desired state of Flow.
            properties:
              description:
                description: Optional human-readable description for this Flow.
                type: string
              failurePolicy:
                default: Fail
                description: Behavior when a step fails.
                enum:
                - Fail
                - Continue
                type: string
              params:
                description: Parameters that can be passed into the Flow.
                items:
                  description: ParamDeclaration defines an input parameter for a Flow.
                  properties:
                    default:
                      description: Default value used when parameter is omitted.
                      type: string
                    description:
                      description: Optional description for the parameter.
                      type: string
                    name:
                      description: Name of the parameter.
                      type: string
                    required:
                      default: false
                      description: Whether this parameter is required.
                      type: boolean
                  required:
                  - name
                  type: object
                type: array
              steps:
                description: Ordered list of steps in the Flow.
                items:
                  description: FlowStep represents a single step in a Flow.
                  properties:
                    action:
                      description: Action to execute.
                      properties:
                        http:
                          description: HTTP action details.
                          properties:
                            body:
                              description: Inline body template.
                              type: string
                            headers:
                              additionalProperties:
                                type: string
                              description: Request headers.
                              type: object
                            integrationRef:
                              description: |-
                                Reference to an http-type Integration providing base URL, auth, and default headers.
                                When set, the Integration's auth and defaultHeaders are merged into the request.
                                For type=secretUrl Integrations, the step URL is replaced by the Integration's URL secret.
                              properties:
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                              type: object
                              x-kubernetes-map-type: atomic
                            method:
                              default: POST
                              description: HTTP method for the call.
                              enum:
                              - GET
                              - POST
                              - PUT
                              - PATCH
                              - DELETE
                              type: string
                            resultMappings:
                              additionalProperties:
                                type: string
                              description: |-
                                Map names for result extraction. Keys are result names; values are JSONPath
                                expressions of the form "$.field". Only single-level paths (e.g., "$.tier")
                                are supported — multi-level paths (e.g., "$.order.id") silently return empty string.
                              type: object
                            timeoutSeconds:
                              default: 30
                              description: Request timeout in seconds.
                              format: int32
                              type: integer
                            url:
                              description: URL to call.
                              type: string
                          required:
                          - url
                          type: object
                        publish:
                          description: Publish action details.
                          properties:
                            body:
                              description: Optional body to publish.
                              type: string
                            headers:
                              additionalProperties:
                                type: string
                              description: Optional publish headers.
                              type: object
                            integrationRef:
                              description: Reference to an Integration resource.
                              properties:
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                              type: object
                              x-kubernetes-map-type: atomic
                            topic:
                              type: string
                          required:
                          - integrationRef
                          - topic
                          type: object
                        transform:
                          description: Transform action details.
                          properties:
                            mappings:
                              additionalProperties:
                                type: string
                              description: Mappings used to generate transformed output.
                              minProperties: 1
                              type: object
                          required:
                          - mappings
                          type: object
                        type:
                          description: Type of step action.
                          enum:
                          - http
                          - transform
                          - publish
                          - wait
                          type: string
                        wait:
                          description: Wait action details.
                          properties:
                            duration:
                              description: |-
                                Duration is the amount of time to wait, as a Go duration string (e.g. "10m", "30s", "1h", "1h30m").
                                Compound durations such as "1h30m" are accepted, matching what time.ParseDuration accepts.
                              pattern: ^([0-9]+(ns|us|µs|ms|s|m|h))+$
                              type: string
                          required:
                          - duration
                          type: object
                      required:
                      - type
                      type: object
                    description:
                      description: Optional description for the step.
                      type: string
                    name:
                      description: Name of the step.
                      type: string
                    onFailure:
                      description: Policy for step failure handling.
                      enum:
                      - Fail
                      - Continue
                      - Skip
                      type: string
                    results:
                      description: Results produced by the step.
                      items:
                        description: ResultDeclaration declares what a step result
                          will expose.
                        properties:
                          description:
                            description: Optional description of the result.
                            type: string
                          name:
                            description: Name of the result field.
                            type: string
                        required:
                        - name
                        type: object
                      type: array
                    retryPolicy:
                      description: Retry policy for this step.
                      properties:
                        backoffType:
                          default: Exponential
                          description: Backoff strategy.
                          enum:
                          - Fixed
                          - Linear
                          - Exponential
                          type: string
                        initialDelay:
                          default: 1s
                          description: Initial retry delay.
                          type: string
                        maxDelay:
                          default: 60s
                          description: Maximum delay for retries.
                          type: string
                        maxRetries:
                          description: Maximum retries allowed.
                          format: int32
                          minimum: 0
                          type: integer
                        multiplier:
                          default: "2.0"
                          description: Backoff multiplier.
                          type: string
                      required:
                      - maxRetries
                      type: object
                    runAfter:
                      description: Step names to run before this step.
                      items:
                        type: string
                      type: array
                    timeout:
                      description: Timeout for this step.
                      type: string
                    when:
                      description: Conditions to evaluate before running this step.
                      items:
                        description: WhenExpression specifies a condition to evaluate
                          prior to step execution.
                        properties:
                          expression:
                            description: Expression that yields true/false.
                            type: string
                        required:
                        - expression
                        type: object
                      type: array
                  required:
                  - action
                  - name
                  type: object
                minItems: 1
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              timeout:
                default: 10m
                description: Timeout for the entire Flow execution.
                type: string
            required:
            - steps
            type: object
          status:
            description: FlowStatus defines the observed state of Flow.
            properties:
              conditions:
                description: Standard Kubernetes conditions.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              executionCount:
                description: Number of times this Flow has executed.
                format: int64
                type: integer
              lastError:
                description: Last error message if any.
                type: string
              lastExecutionTime:
                description: Timestamp of last execution.
                format: date-time
                type: string
              lastResult:
                description: Last result status string (e.g., Success/Failed).
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
