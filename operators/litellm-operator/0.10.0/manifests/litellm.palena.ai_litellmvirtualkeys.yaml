apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  creationTimestamp: null
  name: litellmvirtualkeys.litellm.palena.ai
spec:
  group: litellm.palena.ai
  names:
    kind: LiteLLMVirtualKey
    listKind: LiteLLMVirtualKeyList
    plural: litellmvirtualkeys
    shortNames:
    - lk
    singular: litellmvirtualkey
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.keyAlias
      name: Alias
      type: string
    - jsonPath: .status.isActive
      name: Active
      type: boolean
    - jsonPath: .status.synced
      name: Synced
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LiteLLMVirtualKey is the Schema for the litellmvirtualkeys API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LiteLLMVirtualKeySpec defines the desired state of LiteLLMVirtualKey.
            properties:
              budgetDuration:
                description: Budget reset duration (e.g., "30d").
                type: string
              expiresAt:
                description: Key expiration time.
                format: date-time
                type: string
              guardrails:
                description: |-
                  Guardrails to activate for this key. Each entry must match the
                  guardrailName of a LiteLLMGuardrail CR bound to the same instance.
                  Requires a LiteLLM Enterprise license.
                items:
                  type: string
                type: array
              instanceRef:
                description: Reference to the LiteLLMInstance.
                properties:
                  name:
                    description: Name of the LiteLLMInstance CR.
                    type: string
                required:
                - name
                type: object
              keyAlias:
                description: Human-readable key alias.
                type: string
              keySecretName:
                description: |-
                  Name for the Secret that stores the generated API key.
                  Defaults to "{name}-key".
                type: string
              maxBudget:
                description: Maximum budget in USD.
                type: string
              maxParallelRequests:
                description: Maximum concurrent requests for this key.
                type: integer
              metadata:
                additionalProperties:
                  type: string
                description: Custom metadata.
                type: object
              modelMaxBudget:
                additionalProperties:
                  type: string
                description: |-
                  Per-model spending limits for this key (enterprise).
                  Key: model name, Value: max budget in USD.
                type: object
              models:
                description: Models this key can access.
                items:
                  type: string
                type: array
              rpmLimit:
                description: RPM limit for this key.
                type: integer
              teamRef:
                description: Reference to a LiteLLMTeam CR that this key belongs to.
                properties:
                  name:
                    description: Name of the LiteLLMInstance CR.
                    type: string
                required:
                - name
                type: object
              tpmLimit:
                description: TPM limit for this key.
                type: integer
              userRef:
                description: Reference to a LiteLLMUser CR that this key belongs to.
                properties:
                  name:
                    description: Name of the LiteLLMInstance CR.
                    type: string
                required:
                - name
                type: object
            required:
            - instanceRef
            - keyAlias
            type: object
          status:
            description: LiteLLMVirtualKeyStatus defines the observed state of LiteLLMVirtualKey.
            properties:
              conditions:
                description: Standard conditions.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              currentSpend:
                description: Current spend on this key in USD.
                type: string
              expiresAt:
                description: Key expiration time.
                format: date-time
                type: string
              isActive:
                description: Whether the key is active.
                type: boolean
              keySecretRef:
                description: Reference to the Secret containing the API key.
                properties:
                  key:
                    description: Key within the Secret.
                    type: string
                  name:
                    description: Name of the Secret.
                    type: string
                required:
                - key
                - name
                type: object
              lastSyncTime:
                description: Last successful sync time.
                format: date-time
                type: string
              litellmKeyToken:
                description: LiteLLM-assigned key token (hashed, for reference).
                type: string
              synced:
                description: Whether the key is synced to LiteLLM.
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
