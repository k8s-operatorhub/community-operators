apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  creationTimestamp: null
  name: storagebackups.storage.simplyblock.io
spec:
  group: storage.simplyblock.io
  names:
    kind: StorageBackup
    listKind: StorageBackupList
    plural: storagebackups
    singular: storagebackup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.pvcRef.name
      name: PVC
      type: string
    - jsonPath: .status.backupID
      name: BackupID
      type: string
    - jsonPath: .status.snapshotName
      name: Snapshot
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: StorageBackup is the Schema for the storagebackups API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of StorageBackup
            properties:
              clusterName:
                description: ClusterName is the target storage cluster name.
                type: string
              pvcRef:
                description: PVCRef identifies the PVC whose backing Simplyblock volume
                  should be snapshotted and backed up.
                properties:
                  name:
                    description: Name is the PVC name.
                    type: string
                  namespace:
                    description: Namespace overrides the backup resource namespace
                      for the PVC lookup.
                    type: string
                required:
                - name
                type: object
              snapshotName:
                description: SnapshotName optionally overrides the internally-created
                  snapshot name.
                type: string
            required:
            - clusterName
            - pvcRef
            type: object
          status:
            description: status defines the observed state of StorageBackup
            properties:
              allowedHosts:
                description: AllowedHosts contains the allowed host metadata returned
                  by the backup API.
                items:
                  additionalProperties:
                    type: string
                  type: object
                type: array
              apiStatus:
                description: APIStatus is the raw status returned by the backup API.
                type: string
              backupID:
                description: BackupID is the backend backup UUID.
                type: string
              clusterUUID:
                description: ClusterUUID is the backend cluster UUID.
                type: string
              completedAt:
                description: CompletedAt is when the backup completed.
                format: date-time
                type: string
              createdAt:
                description: CreatedAt is when the backup was created.
                format: date-time
                type: string
              lvolID:
                description: LvolID is the Simplyblock volume UUID.
                type: string
              lvolName:
                description: LvolName is the backend logical volume name.
                type: string
              message:
                description: Message contains the latest reconciliation detail or
                  error.
                type: string
              nodeID:
                description: NodeID is the source storage node UUID.
                type: string
              phase:
                description: Phase is the high-level backup lifecycle shown in kubectl
                  output.
                type: string
              poolName:
                description: PoolName is the Simplyblock pool name derived from the
                  CSI volume handle.
                type: string
              poolUUID:
                description: PoolUUID is the backend pool UUID.
                type: string
              prevBackupID:
                description: PrevBackupID links the previous backup in the chain.
                type: string
              pvName:
                description: PVName is the bound PV name.
                type: string
              pvcNamespace:
                description: PVCNamespace is the resolved PVC namespace.
                type: string
              s3ID:
                description: S3ID is the backend S3 object identifier.
                format: int64
                type: integer
              size:
                description: Size is the backup size in bytes.
                format: int64
                type: integer
              snapshotID:
                description: SnapshotID is the internally-created snapshot UUID used
                  for the backup request.
                type: string
              snapshotName:
                description: SnapshotName is the snapshot name used for the backup
                  request.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
