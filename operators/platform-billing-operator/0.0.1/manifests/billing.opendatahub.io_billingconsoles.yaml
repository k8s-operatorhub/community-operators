apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  creationTimestamp: null
  name: billingconsoles.billing.opendatahub.io
spec:
  group: billing.opendatahub.io
  names:
    kind: BillingConsole
    listKind: BillingConsoleList
    plural: billingconsoles
    singular: billingconsole
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.url
      name: URL
      type: string
    - jsonPath: .status.conditions[?(@.type=="Deployed")].status
      name: Deployed
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BillingConsole is the Schema for the billingconsoles API.
          It configures operator-managed deployment of the billing console UI.
          Cluster-scoped; the controller enforces singleton semantics (name: default).
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BillingConsoleSpec defines the desired state of a BillingConsole.
            properties:
              auth:
                description: Auth configures authentication for the console.
                properties:
                  broker:
                    description: Broker configures brokered (federated IdP) authentication.
                    properties:
                      deploy:
                        description: Deploy controls whether the operator deploys
                          an embedded identity broker.
                        type: boolean
                      existingIssuerURL:
                        description: ExistingIssuerURL points to an externally managed
                          identity broker.
                        type: string
                    type: object
                  mode:
                    description: Mode selects the authentication mechanism.
                    enum:
                    - OpenShiftOAuth
                    - OIDC
                    - Brokered
                    type: string
                  oidc:
                    description: OIDC configures OIDC authentication.
                    properties:
                      clientSecretRef:
                        description: |-
                          ClientSecretRef references a Secret containing the OIDC client credentials.
                          Expected keys: "client-id" and "client-secret".
                        properties:
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                      groupsClaim:
                        default: groups
                        description: GroupsClaim is the JWT claim containing group
                          memberships.
                        type: string
                      issuerURL:
                        description: IssuerURL is the OIDC issuer URL.
                        type: string
                    required:
                    - clientSecretRef
                    - issuerURL
                    type: object
                  roles:
                    description: Roles maps IdP groups to console roles.
                    properties:
                      admin:
                        description: Admin groups have full access including walkthroughs
                          and PriceBook editing.
                        items:
                          type: string
                        type: array
                      operator:
                        description: Operator groups can manage tenants, dunning,
                          and metering health.
                        items:
                          type: string
                        type: array
                      viewer:
                        description: Viewer groups have read-only access.
                        items:
                          type: string
                        type: array
                    type: object
                type: object
              enabled:
                default: true
                description: Enabled controls whether the console Deployment is created.
                type: boolean
              image:
                default: controller:latest
                description: Image is the console container image reference.
                type: string
              replicas:
                default: 1
                description: Replicas is the number of console pod replicas.
                format: int32
                type: integer
              route:
                description: Route configures the OpenShift Route for the console.
                properties:
                  host:
                    description: |-
                      Host is the desired hostname for the console Route.
                      Empty string means auto-generated by OpenShift.
                    type: string
                  tlsSecretRef:
                    description: |-
                      TLSSecretRef references a Secret containing TLS cert/key for the Route.
                      If unset, OpenShift generates a certificate.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                type: object
              tenantPortal:
                description: TenantPortal configures the tenant-facing portal.
                properties:
                  enabled:
                    default: true
                    description: Enabled controls whether the tenant portal is served
                      at /portal.
                    type: boolean
                type: object
            type: object
          status:
            description: BillingConsoleStatus defines the observed state of a BillingConsole.
            properties:
              conditions:
                description: Conditions represent the latest available observations.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              url:
                description: URL is the externally reachable URL of the console.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
