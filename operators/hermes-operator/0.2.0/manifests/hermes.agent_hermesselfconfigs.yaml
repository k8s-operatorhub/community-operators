---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: hermesselfconfigs.hermes.agent
spec:
  group: hermes.agent
  names:
    categories:
    - hermes
    - agents
    kind: HermesSelfConfig
    listKind: HermesSelfConfigList
    plural: hermesselfconfigs
    shortNames:
    - hsc
    singular: hermesselfconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.instanceRef
      name: Instance
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - jsonPath: .status.denyReason
      name: DenyReason
      priority: 1
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: HermesSelfConfig is the Schema for the hermesselfconfigs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of HermesSelfConfig
            properties:
              addEnvVars:
                description: AddEnvVars appends environment variables to the parent's
                  .spec.env.
                items:
                  description: SelfConfigEnvVar is an environment variable entry.
                  properties:
                    name:
                      description: Name of the environment variable. Must be a C_IDENTIFIER.
                      minLength: 1
                      pattern: ^[A-Za-z_][A-Za-z0-9_]*$
                      type: string
                    value:
                      description: Value is the literal value. Mutually exclusive
                        with ValueFrom.
                      type: string
                    valueFrom:
                      description: ValueFrom selects a value from a Secret or ConfigMap
                        key.
                      properties:
                        configMapKeyRef:
                          description: SelfConfigKeySelector selects a key from a
                            Secret or ConfigMap.
                          properties:
                            key:
                              minLength: 1
                              type: string
                            name:
                              minLength: 1
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        secretKeyRef:
                          description: SelfConfigKeySelector selects a key from a
                            Secret or ConfigMap.
                          properties:
                            key:
                              minLength: 1
                              type: string
                            name:
                              minLength: 1
                              type: string
                          required:
                          - key
                          - name
                          type: object
                      type: object
                  required:
                  - name
                  type: object
                maxItems: 20
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              addProfileSnapshot:
                description: AddProfileSnapshot writes an opaque Honcho profile snapshot
                  via a one-shot Job.
                properties:
                  data:
                    description: Data is the opaque snapshot payload.
                    minLength: 1
                    type: string
                  profileID:
                    maxLength: 253
                    minLength: 1
                    type: string
                required:
                - data
                - profileID
                type: object
              addSkills:
                description: AddSkills appends skills to the parent's .spec.skills.
                items:
                  description: SelfConfigSkill names one skill to install.
                  properties:
                    source:
                      description: Source is a uv-compatible package specifier. Required.
                      maxLength: 512
                      minLength: 1
                      type: string
                    version:
                      description: Version optionally pins a version.
                      type: string
                  required:
                  - source
                  type: object
                maxItems: 20
                type: array
                x-kubernetes-list-map-keys:
                - source
                x-kubernetes-list-type: map
              addWorkspaceFiles:
                description: AddWorkspaceFiles writes files into the workspace ConfigMap.
                items:
                  description: SelfConfigWorkspaceFile is a single file to materialise
                    into the workspace.
                  properties:
                    content:
                      description: Content is the literal file body.
                      type: string
                    contentFrom:
                      description: ContentFrom reads the file body from a Secret key.
                      properties:
                        key:
                          minLength: 1
                          type: string
                        name:
                          minLength: 1
                          type: string
                      required:
                      - key
                      - name
                      type: object
                    path:
                      description: Path is the relative path under ~/.hermes/workspace/.
                      maxLength: 512
                      minLength: 1
                      pattern: ^[A-Za-z0-9._/-]+$
                      type: string
                  required:
                  - path
                  type: object
                maxItems: 50
                type: array
                x-kubernetes-list-map-keys:
                - path
                x-kubernetes-list-type: map
              instanceRef:
                description: InstanceRef is the name of the parent HermesInstance
                  in the same namespace.
                maxLength: 253
                minLength: 1
                type: string
              patchConfig:
                description: |-
                  PatchConfig is a JSON merge patch (RFC 7396) applied to the agent's
                  runtime config at ~/.hermes/config.yaml.
                x-kubernetes-preserve-unknown-fields: true
            required:
            - instanceRef
            type: object
          status:
            description: status defines the observed state of HermesSelfConfig
            properties:
              appliedAt:
                description: AppliedAt is the timestamp of the most recent successful
                  SSA write.
                format: date-time
                type: string
              appliedFields:
                description: AppliedFields lists the dotted paths SSA touched on the
                  parent.
                items:
                  type: string
                type: array
                x-kubernetes-list-type: set
              conditions:
                description: Conditions surface fine-grained state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              denyReason:
                description: DenyReason is populated when Phase=Denied.
                type: string
              observedGeneration:
                description: ObservedGeneration is the spec generation the controller
                  last processed.
                format: int64
                type: integer
              phase:
                description: Phase summarises the current state. One of Pending, Applied,
                  Denied.
                enum:
                - Pending
                - Applied
                - Denied
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
