apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  creationTimestamp: null
  name: litellmbudgets.litellm.palena.ai
spec:
  group: litellm.palena.ai
  names:
    kind: LiteLLMBudget
    listKind: LiteLLMBudgetList
    plural: litellmbudgets
    shortNames:
    - lb
    singular: litellmbudget
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.litellmBudgetId
      name: Budget ID
      type: string
    - jsonPath: .status.synced
      name: Synced
      type: boolean
    - jsonPath: .spec.instanceRef.name
      name: Instance
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LiteLLMBudget is the Schema for the litellmbudgets API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              LiteLLMBudgetSpec defines the desired state of LiteLLMBudget.

              A LiteLLMBudget is a reusable budget / rate-limit tier registered with the
              proxy via /budget/new. Other resources reference it by budget_id — e.g.
              LiteLLMVirtualKey.spec.budgetId or LiteLLMInstance.spec.defaultCustomerBudget
              — instead of repeating inline limits. Managed via the LiteLLM REST API.
            properties:
              budgetDuration:
                description: Budget reset interval (e.g., "1d", "7d", "30d").
                type: string
              budgetId:
                description: |-
                  Stable budget identifier used by other resources to reference this tier
                  (sent as budget_id to LiteLLM). Defaults to the object's name when empty.
                type: string
              instanceRef:
                description: Reference to the LiteLLMInstance this budget belongs
                  to.
                properties:
                  name:
                    description: Name of the LiteLLMInstance CR.
                    type: string
                required:
                - name
                type: object
              maxBudget:
                description: Maximum budget in USD for this tier.
                type: number
              maxParallelRequests:
                description: Maximum concurrent requests for this tier.
                type: integer
              modelMaxBudget:
                additionalProperties:
                  type: number
                description: Per-model budget caps in USD (model name -> max budget).
                type: object
              rpmLimit:
                description: Requests-per-minute limit for this tier.
                type: integer
              softBudget:
                description: Soft budget alert threshold in USD (below maxBudget;
                  does not block).
                type: number
              tpmLimit:
                description: Tokens-per-minute limit for this tier.
                type: integer
            required:
            - instanceRef
            type: object
          status:
            description: LiteLLMBudgetStatus defines the observed state of LiteLLMBudget.
            properties:
              conditions:
                description: Standard conditions.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              currentSpend:
                description: Current spend against this budget in USD, refreshed from
                  /budget/info.
                type: number
              lastSyncTime:
                description: Last successful sync time.
                format: date-time
                type: string
              litellmBudgetId:
                description: The budget_id assigned/used in LiteLLM.
                type: string
              synced:
                description: Whether the budget is synced to LiteLLM.
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
