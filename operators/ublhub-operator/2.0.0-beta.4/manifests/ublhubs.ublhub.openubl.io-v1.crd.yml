# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: ublhubs.ublhub.openubl.io
spec:
  group: ublhub.openubl.io
  names:
    kind: Ublhub
    plural: ublhubs
    singular: ublhub
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              storage:
                description: In this section you can configure the Storage.
                properties:
                  type:
                    description: Typo of chosen storage.
                    type: string
                  filesystem:
                    description: Filesystem settings.
                    properties:
                      size:
                        description: Size of the PVC to create.
                        type: string
                    type: object
                  s3:
                    description: Filesystem settings.
                    properties:
                      healthUrl:
                        description: "Only if you are using Minio, otherwise leave\
                          \ it empty"
                        type: string
                      region:
                        description: Region
                        type: string
                      bucket:
                        description: Bucket
                        type: string
                      host:
                        description: "Only if you are using Minio, otherwise leave\
                          \ it empty"
                        type: string
                      accessKeyId:
                        description: Access key id
                        type: string
                      secretAccessKey:
                        description: Secret access key
                        type: string
                    type: object
                type: object
              instances:
                description: Number of instances. Default is 1.
                type: integer
              xsender:
                description: XSender settings.
                properties:
                  enableLoggingFeature:
                    description: Enable logging feature
                    type: boolean
                type: object
              ingress:
                description: |-
                  The deployment is, by default, exposed through a basic ingress.
                  You can change this behaviour by setting the enabled property to false.
                properties:
                  enabled:
                    type: boolean
                type: object
              image:
                description: Custom image to be used.
                type: string
              imagePullSecrets:
                description: Secret(s) that might be used when pulling an image from
                  a private container image registry or repository.
                items:
                  properties:
                    name:
                      type: string
                  type: object
                type: array
              hostname:
                description: In this section you can configure hostname and related
                  properties.
                properties:
                  hostname:
                    description: Hostname for the server.
                    type: string
                type: object
              http:
                description: In this section you can configure features related to
                  HTTP and HTTPS
                properties:
                  tlsSecret:
                    description: "A secret containing the TLS configuration for HTTPS.\
                      \ Reference: https://kubernetes.io/docs/concepts/configuration/secret/#tls-secrets."
                    type: string
                type: object
              additionalOptions:
                description: |-
                  Configuration of the server.
                  expressed as a keys and values that can be either direct values or references to secrets.
                items:
                  properties:
                    secret:
                      properties:
                        optional:
                          type: boolean
                        key:
                          type: string
                        name:
                          type: string
                      type: object
                    value:
                      type: string
                    name:
                      type: string
                  type: object
                type: array
              db:
                description: In this section you can find all properties related to
                  connect to a database.
                properties:
                  passwordSecret:
                    description: The reference to a secret holding the password of
                      the database user.
                    properties:
                      optional:
                        type: boolean
                      key:
                        type: string
                      name:
                        type: string
                    type: object
                  usernameSecret:
                    description: The reference to a secret holding the username of
                      the database user.
                    properties:
                      optional:
                        type: boolean
                      key:
                        type: string
                      name:
                        type: string
                    type: object
                  url:
                    description: "The full database JDBC URL. For instance, 'jdbc:postgresql://localhost/ublhub'."
                    type: string
                type: object
              basicAuth:
                description: In this section you can configure Basic Auth settings.
                properties:
                  sessionEncryptionKeySecret:
                    description: The encryption key that is used to store persistent
                      logins (e.g. for form auth). Logins are stored in a persistent
                      cookie that is encrypted with AES-256 using a key derived from
                      a SHA-256 hash of the key that is provided here.
                    properties:
                      optional:
                        type: boolean
                      key:
                        type: string
                      name:
                        type: string
                    type: object
                  enabled:
                    description: Enable Basic Auth.
                    type: boolean
                type: object
              oidc:
                description: In this section you can configure Oidc settings.
                properties:
                  credentialsSecret:
                    description: Oidc client id.
                    properties:
                      optional:
                        type: boolean
                      key:
                        type: string
                      name:
                        type: string
                    type: object
                  enabled:
                    description: Enable Oidc Auth.
                    type: boolean
                  clientId:
                    description: Oidc client id.
                    type: string
                  serverUrl:
                    description: Oidc server url.
                    type: string
                type: object
              xbuilder:
                description: XBuilder settings.
                properties:
                  icbTasa:
                    description: Default ICB
                    type: string
                  igvTasa:
                    description: Default IGV
                    type: string
                  moneda:
                    description: Default currency
                    type: string
                type: object
            type: object
          status:
            properties:
              conditions:
                items:
                  properties:
                    status:
                      type: boolean
                    type:
                      type: string
                    message:
                      type: string
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
