apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  creationTimestamp: null
  name: krknscenarioruns.krkn.krkn-chaos.dev
spec:
  group: krkn.krkn-chaos.dev
  names:
    kind: KrknScenarioRun
    listKind: KrknScenarioRunList
    plural: krknscenarioruns
    shortNames:
    - ksr
    singular: krknscenariorun
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.totalTargets
      name: Targets
      type: integer
    - jsonPath: .status.successfulJobs
      name: Succeeded
      type: integer
    - jsonPath: .status.failedJobs
      name: Failed
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: KrknScenarioRun is the Schema for the krknscenrarioruns API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KrknScenarioRunSpec defines the desired state of KrknScenarioRun
            properties:
              customRunName:
                description: CustomRunName is a user-provided label for the run, displayed
                  in the console
                type: string
              environment:
                additionalProperties:
                  type: string
                description: Environment is a map of environment variables to set
                  in the scenario pod
                type: object
              files:
                description: Files is a list of files to mount in the scenario pod
                items:
                  description: FileMount represents a file to be mounted in the scenario
                    pod
                  properties:
                    content:
                      description: Content is the base64-encoded content of the file
                      type: string
                    fileId:
                      description: FileID is the UUID of the source ConfigMap (optional,
                        preserved for replay functionality)
                      type: string
                    mountPath:
                      description: MountPath is the absolute path where the file should
                        be mounted
                      type: string
                    name:
                      description: Name is the name of the file
                      type: string
                  required:
                  - content
                  - mountPath
                  - name
                  type: object
                type: array
              kubeconfigPath:
                default: /home/krkn/.kube/config
                description: KubeconfigPath is the path where kubeconfig will be mounted
                  in the pod
                type: string
              maxRetries:
                default: 3
                description: MaxRetries is the maximum number of times to retry failed
                  jobs
                type: integer
              ownerUserId:
                description: OwnerUserID is the email address of the user who created
                  this scenario run
                type: string
              password:
                description: Password is the password for registry authentication
                type: string
              registryName:
                description: RegistryName is the name of the saved private registry
                  to use
                type: string
              registryURL:
                description: RegistryURL is the URL of the container registry
                type: string
              retryBackoff:
                default: exponential
                description: RetryBackoff determines the backoff strategy for retries
                  (exponential or fixed)
                enum:
                - exponential
                - fixed
                type: string
              retryDelay:
                default: 10s
                description: RetryDelay is the initial delay before retrying (e.g.,
                  "10s")
                type: string
              scenarioImage:
                description: ScenarioImage is the container image for the scenario
                type: string
              scenarioName:
                description: ScenarioName is the name of the scenario to run
                type: string
              scenarioRepository:
                description: ScenarioRepository is the repository path in the registry
                type: string
              targetClusters:
                additionalProperties:
                  items:
                    type: string
                  type: array
                description: |-
                  TargetClusters is a map of provider-name to list of cluster names
                  Example: {"krkn-operator": ["cluster1", "cluster2"], "krkn-operator-acm": ["cluster3"]}
                minProperties: 1
                type: object
              targetRequestId:
                description: TargetRequestID is the reference to the KrknTargetRequest
                  CR
                type: string
              token:
                description: Token is the authentication token for the registry
                type: string
              username:
                description: Username is the username for registry authentication
                type: string
            required:
            - scenarioImage
            - scenarioName
            - targetClusters
            - targetRequestId
            type: object
          status:
            description: KrknScenarioRunStatus defines the observed state of KrknScenarioRun
            properties:
              clusterJobs:
                description: ClusterJobs contains the status of each cluster job
                items:
                  description: ClusterJobStatus represents the status of a scenario
                    job for a specific cluster
                  properties:
                    cancelRequested:
                      description: CancelRequested indicates if the user has requested
                        cancellation
                      type: boolean
                    clusterApiUrl:
                      description: ClusterAPIURL is the API URL of the cluster for
                        permission checks
                      type: string
                    clusterName:
                      description: ClusterName is the name of the target cluster
                      type: string
                    completionTime:
                      description: CompletionTime is when the job completed
                      format: date-time
                      type: string
                    containerImage:
                      description: ContainerImage is the full container image path
                        (registry/repository:tag) being run
                      type: string
                    failureReason:
                      description: FailureReason contains a categorized failure reason
                        (OOMKilled, ContainerError, etc.)
                      type: string
                    jobId:
                      description: JobID is the unique identifier for this job
                      type: string
                    lastRetryTime:
                      description: LastRetryTime is when the last retry was initiated
                      format: date-time
                      type: string
                    maxRetries:
                      description: MaxRetries is the maximum number of retries allowed
                        for this job
                      type: integer
                    message:
                      description: Message contains additional information about the
                        job status
                      type: string
                    phase:
                      description: Phase is the current phase of the job (Pending,
                        Running, Succeeded, Failed, Retrying, Cancelled, MaxRetriesExceeded)
                      enum:
                      - Pending
                      - Running
                      - Succeeded
                      - Failed
                      - Retrying
                      - Cancelled
                      - MaxRetriesExceeded
                      type: string
                    podName:
                      description: PodName is the name of the pod running the scenario
                      type: string
                    providerName:
                      description: ProviderName is the name of the provider that owns
                        this cluster
                      type: string
                    retryCount:
                      description: RetryCount is the number of times this job has
                        been retried
                      type: integer
                    startTime:
                      description: StartTime is when the job started
                      format: date-time
                      type: string
                  required:
                  - clusterName
                  - jobId
                  - phase
                  - providerName
                  type: object
                type: array
              conditions:
                description: Conditions represent the latest available observations
                  of the scenario run's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              failedJobs:
                description: FailedJobs is the number of failed jobs
                type: integer
              phase:
                description: Phase is the overall phase of the scenario run
                enum:
                - Pending
                - Running
                - Succeeded
                - PartiallyFailed
                - Failed
                type: string
              resiliencyScores:
                description: |-
                  ResiliencyScores contains per-cluster resiliency scores for this scenario run.
                  Each entry represents the score calculated from the pod logs of a specific cluster.
                  When a scenario runs on multiple clusters, this array will contain one entry per cluster.
                  Populated only when the parent KrknGraphRun has Spec.ResiliencyScoreEnabled set to true.
                items:
                  description: ClusterResiliencyScore represents the resiliency score
                    for a specific cluster
                  properties:
                    clusterName:
                      description: ClusterName is the name of the cluster this score
                        applies to
                      type: string
                    score:
                      description: Score is the calculated resiliency score for this
                        cluster (0-100)
                      type: number
                  required:
                  - clusterName
                  - score
                  type: object
                type: array
              runningJobs:
                description: RunningJobs is the number of currently running jobs
                type: integer
              successfulJobs:
                description: SuccessfulJobs is the number of successfully completed
                  jobs
                type: integer
              totalTargets:
                description: TotalTargets is the total number of target clusters
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
