apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  creationTimestamp: null
  name: plans.billing.opendatahub.io
spec:
  group: billing.opendatahub.io
  names:
    kind: Plan
    listKind: PlanList
    plural: plans
    singular: plan
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.targetRef.kind
      name: TargetKind
      priority: 1
      type: string
    - jsonPath: .spec.targetRef.name
      name: TargetName
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          Plan is the Schema for the plans API.
          It defines a billing plan with rate limits and model scopes that compiles
          down to Kuadrant TokenRateLimitPolicy, RateLimitPolicy, and AuthPolicy CRs.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PlanSpec defines the desired state of a Plan.
            properties:
              geoScopes:
                default:
                - '*'
                description: GeoScopes is a list of geographic scopes. ["*"] means
                  all.
                items:
                  type: string
                type: array
              limits:
                description: Limits configures the rate limits for this plan.
                properties:
                  gpuQuota:
                    description: |-
                      GPUQuota is the maximum number of GPUs available for Kueue workloads
                      under this plan. When set, the KueueBackend manages a per-tenant
                      ClusterQueue with this nominalQuota for nvidia.com/gpu resources.
                    format: int32
                    minimum: 0
                    type: integer
                  maxConcurrency:
                    description: |-
                      MaxConcurrency is the concurrent request limit.
                      This field is not compilable to Kuadrant and is stored only.
                    format: int32
                    minimum: 0
                    type: integer
                  requestsPerMinute:
                    description: RequestsPerMinute is the request rate limit per minute.
                    format: int32
                    minimum: 0
                    type: integer
                  tokensPerDay:
                    description: TokensPerDay is the token rate limit per day.
                    format: int64
                    minimum: 0
                    type: integer
                  tokensPerMinute:
                    description: TokensPerMinute is the token rate limit per minute.
                    format: int64
                    minimum: 0
                    type: integer
                type: object
              modelScopes:
                description: |-
                  ModelScopes is a list of glob patterns for model matching.
                  Compiled into AuthPolicy CEL predicates.
                items:
                  type: string
                type: array
              priceBookRef:
                description: PriceBookRef is the name of the PriceBook to use for
                  rating.
                type: string
              queueClass:
                description: QueueClass is an informational annotation passed to the
                  serving layer.
                type: string
              targetRef:
                description: |-
                  TargetRef identifies the Gateway API resource this Plan attaches to.
                  Uses the standard Gateway API policy attachment pattern.
                properties:
                  group:
                    description: Group is the group of the target resource.
                    maxLength: 253
                    pattern: ^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                    type: string
                  kind:
                    description: Kind is kind of the target resource.
                    maxLength: 63
                    minLength: 1
                    pattern: ^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$
                    type: string
                  name:
                    description: Name is the name of the target resource.
                    maxLength: 253
                    minLength: 1
                    type: string
                  sectionName:
                    description: |-
                      SectionName is the name of a section within the target resource. When
                      unspecified, this targetRef targets the entire resource. In the following
                      resources, SectionName is interpreted as the following:

                      * Gateway: Listener name
                      * HTTPRoute: HTTPRouteRule name
                      * Service: Port name

                      If a SectionName is specified, but does not exist on the targeted object,
                      the Policy must fail to attach, and the policy implementation should record
                      a `ResolvedRefs` or similar Condition in the Policy's status.
                    maxLength: 253
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                    type: string
                required:
                - group
                - kind
                - name
                type: object
            required:
            - limits
            - targetRef
            type: object
          status:
            description: PlanStatus defines the observed state of a Plan.
            properties:
              compiledPolicies:
                description: CompiledPolicies lists the managed Kuadrant CRs produced
                  by the PolicyCompiler.
                items:
                  description: CompiledPolicyRef identifies a Kuadrant CR managed
                    by the PolicyCompiler.
                  properties:
                    group:
                      description: Group is the API group of the compiled CR.
                      type: string
                    kind:
                      description: Kind is the kind of the compiled CR.
                      type: string
                    name:
                      description: Name is the name of the compiled CR.
                      type: string
                    namespace:
                      description: Namespace is the namespace of the compiled CR.
                      type: string
                    upToDate:
                      description: UpToDate indicates whether the compiled CR matches
                        the desired state.
                      type: boolean
                  required:
                  - group
                  - kind
                  - name
                  - namespace
                  - upToDate
                  type: object
                type: array
              conditions:
                description: Conditions represent the latest available observations
                  of the Plan's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
